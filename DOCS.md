# مستند فنی کامل — دستیار هوشمند یکپارچه‌سازی و تحلیل تعاملات مشتریان

**پروژه:** هکاتون پل سوم · کارخانه هوش مصنوعی ایران · دادهٔ صنعتی نفیس نخ
**دامنهٔ داده:** ۱۶ شیت ERP/CRM · ۶۴۴ مشتری · ۵۲٬۹۳۵ خط فروش · بازهٔ آذر ۱۳۹۸ تا خرداد ۱۴۰۱
**نسخهٔ مستند:** برای کد فعلی مخزن، همهٔ اعداد از کش زنده استخراج شده‌اند

---

## فهرست

| بخش | موضوع |
|---|---|
| ۱ | صورت‌مسئله و سه اصل معماری |
| ۲ | نقشهٔ کلی و جریان داده |
| ۳ | لایهٔ داده: ۱۶ شیت و ۷ قاعدهٔ یکپارچه‌سازی |
| ۴ | پنج دام داده و اصلاح هرکدام |
| ۵ | موتور «در تاریخ» (as-of) |
| ۶ | شاخص قیمت و تعدیل تورم |
| ۷ | مهندسی ویژگی — ۸۴ ویژگی |
| ۸ | فرهنگ متریک‌ها با فرمول کامل |
| ۹ | RFM |
| ۱۰ | امتیاز ماندگاری و کارت مدل |
| ۱۱ | ارزش طول عمر (LTV) |
| ۱۲ | چهارخانهٔ حاشیه در برابر ریسک |
| ۱۳ | نرخ‌های پایهٔ تجربی |
| ۱۴ | سیگنال‌ها (۱۸ سیگنال، ۷ حوزه) |
| ۱۵ | پیش‌بینی تصمیم بعدی (۶ تصمیم) |
| ۱۶ | ریسک‌ها (۱۵ قاعده) |
| ۱۷ | فرصت‌ها (۱۰ قاعده) و نگاشت دلیل آفر |
| ۱۸ | اقدام بعدی و فرمول اولویت |
| ۱۹ | سامانهٔ رفرنس‌دهی |
| ۲۰ | الگوهای سنجیده: دو تأیید، سه رد |
| ۲۱ | داشبورد — تب به تب، پنل به پنل |
| ۲۲ | نمودارها و قواعد طراحی |
| ۲۳ | لایهٔ دستیار و ۱۳ ابزار |
| ۲۴ | API |
| ۲۵ | راهنمای کد فایل به فایل |
| ۲۶ | آزمون‌ها |
| ۲۷ | راه‌اندازی |
| ۲۸ | تله‌های فنی که با آن‌ها برخوردیم |
| ۲۹ | محدودیت‌ها و مسیر ادامه |
| ۳۰ | موتور دوره — طراحی، پیش‌بینی و اعتبارسنجی |
| ۳۱ | تب خلاصه و دستیار شناور |
| ۳۲ | هزینهٔ پول و حاشیهٔ واقعی |
| ۳۳ | فهرست تمرکز — کدام حساب‌ها توجه کمتری بگیرند |
| ۳۴ | اعتبارسنجی دستی و نگاشت به راهنمای داوران |
| ۳۵ | کارتابل — کار امروز، دستور کار تماس و جلسه |
| پیوست الف | جدول مرجع همهٔ اعداد |
| پیوست ب | واژه‌نامهٔ فنی |

---

# ۱. صورت‌مسئله و سه اصل معماری

## ۱.۱ مسئله

در شرکت‌های B2B، ارزشمندترین دانش مشتری در اکسل و داشبورد مدیریتی نیست؛ در تماس‌های
تلفنی، گزارش‌های متنی کارشناسان فروش، شکایت‌های ثبت‌شده، درخواست‌های توسعه محصول و
ذهن کارشناسان است. در این داده، اندازهٔ آن دانش پراکنده قابل شمارش است:

- ۱۶ شیت در دو فایل، با ۵ سامانهٔ منبع مختلف (`ERP_SALES`، `ERP_COLLECTIONS`، `MDM`،
  `CRM_ESTIMATES`، `MARKET_3C`)
- **۴٬۵۶۵ رکورد متن آزاد** — متن شکایت، خلاصهٔ گزارش کارشناس، شرح نیاز فنی — که در هیچ
  گزارش ساختاریافته‌ای دیده نمی‌شود
- هیچ جدولی که «تصویر کامل یک مشتری» را بدهد

## ۱.۲ سه اصل معماری

هر تصمیم فنی این پروژه از یکی از این سه اصل می‌آید.

### اصل یکم — مدل زبانی هیچ‌گاه محاسبه نمی‌کند

مدل به داده دسترسی مستقیم ندارد. فقط ۱۳ تابع پایتونی را صدا می‌زند و رشتهٔ برگشتی
آن‌ها را روایت می‌کند. زیر هر پاسخ، فهرست ابزارهای فراخوانی‌شده نمایش داده می‌شود.

**چرا:** یک عدد اشتباه که مدل تولید کند، اعتماد کارشناس فروش را برای همیشه از بین
می‌برد. جلوگیری از توهم را نمی‌توان به پرامپت سپرد؛ باید ساختاری باشد. اگر مدل عدد
نسازد، نمی‌تواند عدد اشتباه بسازد.

### اصل دوم — هر ادعا رفرنس دارد

در کل سبد **۱۶٬۷۹۱ رفرنس** وجود دارد. هر سیگنال، ریسک، فرصت، اقدام و پیش‌بینی
می‌گوید از کدام شیت، کدام شناسهٔ رکورد، کدام تاریخ و کدام فیلد آمده. جایی که ادعا از
تجمیع یا نرخ پایهٔ تجربی می‌آید نه یک رکورد خاص، **همین را صریح می‌نویسد**.

**چرا:** کارشناس فروش باید بتواند بپرسد «از کجا؟» و در سه ثانیه به رکورد منبع برسد.
بدون این، هر تحلیلی «جعبهٔ سیاه» است و در جلسهٔ تصمیم رد می‌شود.

آزمون `verify.py` تضمین می‌کند هیچ ادعایی بدون رفرنس نمی‌ماند — این یک قاعدهٔ اجرایی
است نه یک وعده.

### اصل سوم — هر پروفایل «در تاریخ مشخص» ساخته می‌شود

`build_profiles(as_of=...)` کل API است. همهٔ شیت‌ها ستون `Available_At` دارند؛
ردیف‌های در دسترس‌نبوده حذف و نتایج آن‌زمان‌نامعلوم سانسور می‌شوند.

**چرا:** این تنها راه سنجش واقعی است. پروفایل‌ها را در دی ۱۳۹۹ می‌سازیم، از مدل
می‌پرسیم کدام مشتری راکد می‌شود، و با واقعیت ۱۲ ماه بعد مقایسه می‌کنیم. همین گذاشت
امتیاز ماندگاری را با AUC واقعی گزارش کنیم، نه با ادعا.

---

# ۲. نقشهٔ کلی و جریان داده

```
DATASET.xlsx ──┐
               ├──► pipeline.py
METADATA.xlsx ─┘    ├─ load_raw()      خواندن ۱۶ شیت، ترجمهٔ نام و مقدار
                    ├─ clean()         ۷ قاعده + ۵ دام + شاخص قیمت
                    ├─ as_of_view()    برش زمانی + سانسور نتیجه
                    ├─ build_profiles() ۶۴۴ پروفایل خام
                    └─ portfolio_stats() آمار سبد، بهار، ماه ناقص
                             │
        ┌────────────────────┼────────────────────┐
        ▼                    ▼                    ▼
  features.py          signals.py           insights.py
  ۸۴ ویژگی             ۱۸ سیگنال            ۱۵ ریسک
  RFM (۱۱ بخش)         ۶ پیش‌بینی            ۱۰ فرصت
  ماندگاری کالیبره     رفرنس‌ساز             اقدام بعدی
  LTV تجزیه‌شده                              نگاشت دلیل آفر
  چهارخانه
        └────────────────────┼────────────────────┘
                             ▼
                       store.py — ۱۰ ابزار
                       نمایهٔ متن (۴٬۵۶۵ رکورد)
                       رندر فارسی پروفایل
                       کش JSON
                             │
              ┌──────────────┴──────────────┐
              ▼                             ▼
        app.py (FastAPI)            copilot.py (Gemini)
        ۱۰ نقطهٔ پایانی              function calling
              │                      + حالت قطعی پشتیبان
              ▼                             │
     static/index.html ◄────────────────────┘
     + charts.js (۸ نوع نمودار)
     + md.js (رندر Markdown)
```

## ۲.۱ ترتیب اجرا

۱. `load_raw()` — ۱۶ شیت را می‌خواند، نام ستون‌ها را به انگلیسی و مقادیر دسته‌ای را
   نرمال می‌کند (۱۹ ثانیه)
۲. `clean()` — قواعد و دام‌ها را اعمال می‌کند، شاخص قیمت می‌سازد
۳. `build_features()` + `add_scores()` — ۸۴ ویژگی، RFM، ماندگاری، LTV، چهارخانه
۴. `build_profiles()` — ۶۴۴ پروفایل خام در تاریخ برش
۵. `add_complaint_impact()` — پنجرهٔ ۹۰ روزهٔ پیش/پس از نخستین شکایت
۶. `enrich_all()` — سیگنال، پیش‌بینی، ریسک، فرصت، اقدام، خلاصه به هر پروفایل
۷. `portfolio_stats()` — آمار سبد
۸. کش در `cache/profiles.json` و `cache/portfolio.json` (کل فرآیند ~۵۸ ثانیه)

سرور کش را می‌خواند، پس راه‌اندازی مجدد چند ثانیه است. برای بازسازی: `python store.py`

---

# ۳. لایهٔ داده: ۱۶ شیت و ۷ قاعدهٔ یکپارچه‌سازی

## ۳.۱ شیت‌ها

| شیت (فارسی) | کلید داخلی | ردیف | دانه (grain) | کلید اصلی |
|---|---|---|---|---|
| مشتریان | `customers` | ۶۴۴ | یک ردیف هر مشتری | `Customer_ID` |
| محصولات | `products` | ۶۴۶ | یک ردیف هر کد کالا | `Product_ID` |
| فاکتورها | `invoices` | ۱۴٬۴۲۳ | هدر فاکتور | شماره فاکتور |
| فروش | `sales` | ۵۲٬۹۸۷ | خط فروش | `Sales_Line_ID` |
| اجزای_هزینه_تحقق | `realized_cost` | ۱۷٬۰۸۱ | هزینهٔ بسته‌شدهٔ خط فروش | `Cost_Record_ID` |
| وصول | `collections` | ۱۵٬۶۵۲ | رویداد وصول | `Collection_ID` |
| شکایات | `complaints` | ۵۲۰ | شکایت و نتیجه | `Complaint_ID` |
| اتصال_شکایت | `complaint_links` | ۵۹۷ | پل شکایت ↔ خط فروش | ترکیبی |
| تعاملات_CRM | `crm` | ۴٬۱۸۴ | تعامل + نسخه | ترکیبی |
| درخواست_توسعه | `dev_requests` | ۸۰۰ | درخواست توسعه محصول | `Request_ID` |
| کیفیت_لات | `lab` | ۱۳٬۸۶۵ | اندازه‌گیری آزمایشگاه | `Quality_Record_ID` |
| همبافت_لات | `hembaft_lots` | ۵۲ | نگاشت همبافت ↔ لات | `Hembaft_Lot_Key` |
| آفرها | `offers` | ۲٬۵۰۰ | پیشنهاد تجاری و نتیجه | `Offer_ID` |
| سهم_سبد | `wallet_share` | ۷٬۴۸۸ | مشتری × ماه | ترکیبی |
| سیگنال_بازار | `market_signals` | ۱۳۰ | هفته | `Week_ID` |
| برآورد_هزینه_ماهانه | `estimated_cost` | ۸٬۵۴۶ | محصول × ماه | `Estimate_ID` |

## ۳.۲ هفت قاعدهٔ یکپارچه‌سازی متادیتا و پیاده‌سازی هرکدام

فایل METADATA هفت قاعده اعلام می‌کند. هرکدام در کد به یک نقطهٔ مشخص نگاشت می‌شود:

| قاعده | متن متادیتا | پیاده‌سازی |
|---|---|---|
| ۱ | همهٔ جداول مشتری‌محور مستقیماً با `Customer_ID` به مشتریان وصل شوند | `build_features` و `build_profiles` همه‌جا با `Customer_ID` گروه می‌کنند |
| ۲ | فروش و وصول مستقیماً Join نشوند؛ هر دو به فاکتورها وصل شوند | در `build_features` و `build_profiles`، مطالبات در سطح `invoice_no` جمع می‌شود؛ هیچ join مستقیم فروش↔وصول وجود ندارد |
| ۳ | شکایت از طریق `اتصال_شکایت` به خطوط فروش وصل شود | `complaint_links` تنها مسیر رسیدن شکایت به خط فروش است |
| ۴ | رکورد فقط از `Available_At` به بعد قابل استفاده است | `as_of_view()` |
| ۵ | برای هر `Interaction_ID` آخرین `Record_Version` استفاده شود | `clean()`: `groupby("Interaction_ID").tail(1)` — ۴٬۱۸۴ → ۳٬۸۶۵ ردیف |
| ۶ | هزینهٔ تحقق‌یافته و برآوردی هرکدام ستون مستقل؛ اولویت با تحقق‌یافته | `clean()`: `unit_cost = realized.fillna(estimated)` + پرچم `cost_basis` |
| ۷ | `Hembaft_ID` و `Lot_ID` یکی نیستند؛ اتصال فقط با `Hembaft_Lot_Key` | هیچ join‌ای روی `Hembaft_ID` یا `Lot_ID` انجام نمی‌شود |

## ۳.۳ یکپارچگی ارجاعی

هر ۲۶ رابطهٔ اعلام‌شده در متادیتا آزمایش شد. **صفر کلید یتیم** — با یک تبصره: پس از
جدا کردن ۵۲ ردیف ردیابی از `sales` (دام ۱)، سه شیت `realized_cost` و `lab` و
`complaint_links` به آن ۵۲ شناسه ارجاع می‌دهند. این ارجاع درست است، پس آزمون
شناسهٔ خط فروش را در «فروش ∪ ردیف‌های ردیابی» حل می‌کند.

## ۳.۴ سه ستونی که متادیتا اعلام می‌کند و در داده نیست

`validate_against_metadata()` این را گزارش می‌دهد:

- `Channel` در `تعاملات_CRM`
- `Priority` در `درخواست_توسعه`
- `Source_Type` در `شکایات`

هیچ کدی به این ستون‌ها تکیه نمی‌کند.

---

# ۴. پنج دام داده و اصلاح هرکدام

هر پنج مورد در کد با `assert` یا آزمون قفل شده‌اند: اگر دادهٔ تازه‌ای بیاید که فرض را
نقض کند، سیستم **خطا می‌دهد** نه نتیجهٔ غلط.

## دام ۱ — ۵۲ ردیف در شیت فروش، فروش نیستند

**شاهد:** شیت `فروش` ۵۲٬۹۸۷ ردیف دارد، ولی ۵۲ ردیف آن:

- شناسهٔ `SL-CMP-000001` تا `SL-CMP-000052` دارند (بقیه `SL-00000001` به بالا)
- تاریخ ۱۴۰۴/۰۱/۰۵ تا ۱۴۰۵/۰۴/۳۱ دارند، در حالی که بقیه تا خرداد ۱۴۰۱ تمام می‌شوند
- **تنها ردیف‌هایی هستند که `Hembaft_Lot_Key` دارند** (۵۲ از ۵۲)
- **همه در پل شکایت حاضرند** (۵۲ از ۵۲)
- ۰٫۷۱٪ فروش را حمل می‌کنند در حالی که ۰٫۱٪ ردیف‌ها هستند

**تفسیر:** این‌ها رکورد ردیابی شکایت در سطح خط فروش‌اند، از منبعی دیگر تزریق شده‌اند.
یادداشت کیفیت دادهٔ متادیتا هم می‌گوید «۵۲٬۹۳۵ خط فایل ERP حفظ شده و خطوط مرتبط با
شکایت نیز در همان grain ثبت شده‌اند» — یعنی ۵۲٬۹۸۷ − ۵۲٬۹۳۵ = ۵۲.

**اصلاح:** در `clean()` به `traceability_lines` منتقل می‌شوند (دور ریخته **نمی‌شوند**،
چون برای ردیابی شکایت لازم‌اند). `assert s.date.max() < ERP_WINDOW_END` قفل می‌کند.

**اگر اصلاح نشود:** هر سری زمانی یک شکاف سه‌سالهٔ جعلی پیدا می‌کند و نمودارها با
دنبالهٔ بی‌معنی تمام می‌شوند.

## دام ۲ — رشد فروش اسمی، تورم است

**شاهد — مقایسهٔ فصل بهار سال‌های شمسی** (هر سه پنجره از ۱ فروردین تا ۱۶ خرداد بریده
شده‌اند تا دقیقاً هم‌ارز باشند؛ ۱۶ خرداد آخرین روز دادهٔ فروش است):

| شاخص | بهار ۱۳۹۹ | بهار ۱۴۰۰ | بهار ۱۴۰۱ |
|---|---|---|---|
| حجم فروش | ۱٬۷۳۶٬۴۱۶ کیلوگرم (۱۰۰) | ۱٬۶۳۸٬۶۲۰ (۹۴) | **۱٬۱۸۰٬۳۸۶ (۶۸)** |
| تعداد مشتری فعال | ۲۴۳ (۱۰۰) | ۲۴۷ (۱۰۲) | **۱۹۰ (۷۸)** |
| قیمت میانگین واحد | ۹۷٫۳ (۱۰۰) | ۱۵۹٫۰ (۱۶۳) | **۷۷۷٫۸ (۷۹۹)** |
| فروش اسمی | ۱۶۸٫۹ م (۱۰۰) | ۲۶۰٫۵ م (۱۵۴) | **۹۱۸٫۱ م (۵۴۴)** |
| حاشیه سود | ۱۰٫۴۵٪ | ۱۰٫۶۹٪ | ۸٫۴۸٪ |

فروش اسمی ۵٫۴ برابر شده، در حالی که **حجم ۳۲٪ و تعداد مشتری فعال ۲۲٪ کاهش یافته** و
قیمت واحد ۸ برابر شده.

**چرا بهار و نه نیمهٔ سال میلادی:** پنجرهٔ شمسی هم برای مخاطب طبیعی است و هم از
فصلی‌بودن مصون. بریدن هر سه پنجره در یک روز یکسان، ماه‌های ناقص مرزی را هم دور می‌زند.

**اصلاح:** شاخص قیمت ماهانه از خود داده ساخته می‌شود و هر پروفایل **هم فروش اسمی و هم
فروش حقیقی** دارد. تحلیل روند همیشه روی حجم یا فروش حقیقی است.

## دام ۳ — فیلد وضعیت مشتری نشتی است

**شاهد — جدول توافقی:**

| | «فعال» در منبع | «غیرفعال» در منبع |
|---|---|---|
| خریدار (رکود ≤ ۱۸۰ روز) | ۲۴۳ | **۰** |
| راکد (رکود > ۱۸۰ روز) | ۳۰ | ۳۷۱ |

هم‌خطی ۹۵٪ با رکود ۱۸۰ روزه. یعنی این فیلد تقریباً همان رکود است با برچسب دیگر.

**اصلاح:** به `source_status_LEAKY` تغییر نام یافته و در **هیچ** محاسبه‌ای استفاده
نمی‌شود. در پروفایل نگه داشته می‌شود ولی با نامی که هر خواننده‌ای را متوقف می‌کند.
پرامپت سیستم هم به مدل می‌گوید هرگز آن را به‌عنوان شاهد نقل نکند.

**اگر اصلاح نشود:** مدل‌سازی ریزش با این فیلد، استدلال دایره‌وار است — رکود را با
رکود پیش‌بینی می‌کنید و AUC نزدیک ۱ می‌گیرید.

## دام ۴ — مبنای بهای تمام‌شده جواب را عوض می‌کند

| مبنا | سهم خطوط | حاشیه سود |
|---|---|---|
| هزینهٔ تحقق‌یافته | ۳۲٫۲٪ | **۶٫۸۷٪** |
| هزینهٔ برآوردی (جانشین) | ۶۷٫۸٪ | **۱۱٫۶۳٪** |
| ترکیبی (کل) | ۱۰۰٪ | ۱۰٫۰۹٪ |

روی ۱۷٬۰۴۷ خطی که هر دو مبنا را دارند، هزینهٔ برآوردی از تحقق‌یافته بین ۱۹٫۶٪− تا
۳۱٫۵٪+ (صدک ۱۰ و ۹۰) فاصله دارد. **مبنای برآوردی حاشیه را حدود ۵ واحد درصد
خوش‌بینانه‌تر نشان می‌دهد.**

**اصلاح:** ستون `cost_basis` در سطح خط فروش، و ویژگی `realized_cost_share` در سطح
مشتری. هر عدد حاشیه در هر پروفایل، سهم مبنای خودش را همراه دارد. پرامپت سیستم هم
مدل را وادار می‌کند این را قید کند.

## دام ۵ — تفکیک معوق از سررسیدنشده

**شاهد:** مانده باز کل ۷۲۳٫۵ میلیون است، ولی ۱۶۶٫۴ میلیون آن **فقط هنوز سررسید
نشده** — فاکتوری که هفتهٔ گذشته صادر شده و با مهلت ۹۰ روزه هنوز موعدش نرسیده.

**اصلاح:** تاریخ سررسید = تاریخ فاکتور + مهلت پرداخت مشتری. مانده باز به دو بخش
تقسیم می‌شود:

- **معوق** (`uncollected_overdue`) = سررسیدگذشته → ریسک اعتباری
- **سررسیدنشده** (`uncollected_not_yet_due`) = سرمایه در گردش → ریسک نیست

**اثر:** بدون تفکیک، ریسک اعتباری ۳۰٪ بیش‌برآورد می‌شود (۷۲۳ در برابر ۵۵۷ میلیون).
بزرگ‌ترین مشتری (`C_937594`) از مشارکت خالص منفی ۱۰۵ میلیون به **مثبت ۴٫۶ میلیون**
می‌رسد.

**تبصره:** ۵۲۶ از ۶۴۴ مشتری مهلت پرداخت صفر دارند، پس برای بیشتر سبد سررسید = تاریخ
فاکتور. عدد «۲۳ روز تأخیر» چرخهٔ عادی پرداخت است نه نکول.

---

# ۵. موتور «در تاریخ» (as-of)

## ۵.۱ دو عملیات متفاوت

اشتباه رایج، قاطی کردن این دو است:

### الف) پالایش ردیف

هر شیت یک ستون در دسترس‌بودن دارد. ردیف‌هایی که `Available_At > as_of` هستند حذف
می‌شوند.

```python
AVAIL_COL = {
  "invoices": "Available_At", "sales": "Available_At", "collections": "Available_At",
  "complaints": "Available_At", "complaint_links": "Link_Available_At",
  "crm": "Available_At", "dev_requests": "Available_At", "lab": "Available_At",
  "offers": "Available_At", "wallet_share": "Available_At",
  "market_signals": "Available_At", "hembaft_lots": "Available_At",
}
```

تأخیر در دسترس‌بودن در این داده ۱ تا ۲ روز است (میانه ۱، صدک ۹۵ برابر ۲)، پس پالایش
ردیف اثر کمی دارد.

### ب) سانسور نتیجه (right-censoring)

اینجاست که اثر واقعی است. ردیف نگه داشته می‌شود ولی **فیلدهای نتیجه** که در آن تاریخ
معلوم نبوده‌اند خالی می‌شوند:

```python
CENSOR_RULES = {
  "complaints":   (["Resolved_At", "Resolution_Text"], "Resolution_Available_At",
                   {"Complaint_Status": "under_review"}),
  "offers":       (["Decision_At", "Result"], "Decision_Available_At",
                   {"Result": "pending"}),
  "dev_requests": (["Decision_At", "Outcome_Text", "Status"], "Decision_At",
                   {"Status": "under_review"}),
}
```

**مثال:** شکایتی که در فروردین ۱۴۰۱ باز بوده و در خرداد بسته شده، در پروفایل «در
تاریخ فروردین» **باز** دیده می‌شود، نه «رسیدگی‌شده». در تاریخ برش پیش‌فرض،
۱۴۹ شکایت و ۵۴۸ آفر به این شکل سانسور می‌شوند.

هر ردیف سانسورشده پرچم `outcome_censored` می‌گیرد، پس قابل شمارش و آزمون است.

## ۵.۲ چرا این مهم‌ترین تصمیم پروژه است

بدون سانسور نتیجه، هر مدلی که بسازید نشتی دارد: شکایتی که «رسیدگی‌شده» است یعنی
آینده را دیده‌اید. با آن، می‌توانید:

۱. پروفایل‌ها را در ۱۳۹۹/۱۰/۱۱ بسازید
۲. امتیاز ماندگاری را حساب کنید
۳. نگاه کنید چه کسی در ۱۲ ماه بعد واقعاً خرید کرد
۴. AUC واقعی گزارش کنید

دقیقاً همین کار در `explore3.py` و `explore4.py` انجام شده.

## ۵.۳ ماه‌های ناقص مرزی

نخستین و آخرین ماه بازه ناقص‌اند: آذر ۱۳۹۸ فقط ۱۶ روز و تیر ۱۴۰۱ فقط ۶ روز داده
دارد. اگر در نمودار بمانند، هر سری زمانی با یک سقوط جعلی تمام می‌شود.

`portfolio_stats` ماهی را ناقص می‌شمارد که نخستین یا آخرین رکوردش بیش از ۶ روز از
ابتدا/انتهای ماه تقویمی فاصله داشته باشد. نتیجه: `['2019-12', '2022-06']`.

نمودارها این دو ماه را حذف می‌کنند؛ **جدول داده آن‌ها را با برچسب «ناقص» نگه می‌دارد**
(حذف بی‌صدا بدتر از نمایش با هشدار است).

---

# ۶. شاخص قیمت و تعدیل تورم

## ۶.۱ ساخت شاخص

شاخص قیمت ماهانه از خود داده ساخته می‌شود — هیچ منبع بیرونی لازم نیست:

```
قیمت میانگین وزنی ماه t = Σ(قیمت واحد × مقدار) / Σ(مقدار)
شاخص قیمت ماه t         = قیمت میانگین ماه t / قیمت میانگین فروردین ۱۳۹۹ × ۱۰۰
```

وزن با **مقدار** است نه با تعداد خط، چون میانگین ساده به سفارش‌های کوچک وزن بیش از
حد می‌دهد.

شاخص در بازهٔ داده از ۱۰۰ به ۱٬۱۷۴ می‌رسد (۱۱٫۷ برابر در ۳۱ ماه). عدد سنجیده‌تر برای
گزارش، ۸ برابر است که از مقایسهٔ هم‌ارز بهار به بهار می‌آید — چون بازهٔ ماهانه شامل
ماه‌های ناقص و نویز فصلی است.

## ۶.۲ فروش حقیقی

```
فروش حقیقی خط = مبلغ کل خط ÷ (شاخص قیمت ماه ÷ ۱۰۰)
```

کل سبد: فروش اسمی ۴٬۳۹۱ میلیون → فروش حقیقی **۱٬۷۶۱ میلیون**.

## ۶.۳ کجا اسمی و کجا حقیقی

| کاربرد | مبنا | چرا |
|---|---|---|
| روند فروش | حجم (کیلوگرم) | کاملاً مصون از قیمت |
| مقایسهٔ دوره‌ای | فروش حقیقی | قابل جمع، تعدیل‌شده |
| ارزش پولی RFM | فروش حقیقی | با اسمی، هر مشتری تازه‌تر خودکار پرارزش‌تر می‌شد |
| حاشیه سود | درصد، پس بی‌اثر | نسبت است |
| مطالبات و LTV | اسمی | مبلغ نقدی واقعی است که وصول می‌شود |

**تبصرهٔ مهم:** واحد پول در فایل منبع مشخص نشده. همهٔ مبالغ در همان مقیاس منبع
گزارش می‌شوند. نتیجه‌گیری‌های اصلی روی **نسبت‌ها** بنا شده‌اند (حاشیه، شاخص، برابری
معوق به سود) که از واحد مستقل‌اند.

---

# ۷. مهندسی ویژگی — ۸۴ ویژگی

`features.build_features(D, as_of)` یک جدول ۶۴۴ × ۸۴ می‌سازد. ویژگی‌ها به هفت گروه
تقسیم می‌شوند. ستون «چرا» دلیل وجود ویژگی است، نه توضیح فرمول.

## ۷.۱ هویت و مستر (۴)

| ویژگی | منبع | چرا |
|---|---|---|
| `segment` | مشتریان | بخش A/B/C، مبنای مقایسهٔ نسبی |
| `terms` | مشتریان | مهلت پرداخت — برای محاسبهٔ سررسید لازم است |
| `credit_limit` | مشتریان | مبنای سنجش عبور از سقف اعتبار |
| `rep` | مشتریان | کارشناس مسئول، برای مسیریابی اقدام |

## ۷.۲ بنیادی تجاری (۱۲)

| ویژگی | فرمول | چرا |
|---|---|---|
| `revenue` | Σ مبلغ کل | اندازهٔ اسمی |
| `revenue_real` | Σ مبلغ کل تعدیل‌شده | اندازهٔ قابل مقایسه بین دوره‌ها |
| `volume` | Σ مقدار (کیلوگرم) | اندازهٔ فیزیکی، مصون از تورم |
| `gp` | Σ (قیمت واحد − بهای واحد) × مقدار | سود ناخالص |
| `lines` | تعداد خط فروش | شدت فعالیت |
| `invoices` | تعداد فاکتور یکتا | تعداد سفارش واقعی |
| `months` | تعداد ماه یکتای دارای خرید | **عمق رابطه** — مهم‌ترین کنترل آماری پروژه |
| `products` | تعداد کد کالای یکتا | تنوع در سطح کد |
| `families` | تعداد گروه کالای یکتا | **تنوع در سطح گروه — پیش‌بین سنجیدهٔ ماندگاری** |
| `quality_classes` | تعداد کلاس کیفی یکتا | گسترهٔ کیفی سبد |
| `first` / `last` | min/max تاریخ | مرز رابطه |

## ۷.۳ مشتق: نسبت و شدت (۱۶)

| ویژگی | فرمول | چرا |
|---|---|---|
| `margin` | `gp / revenue × 100` | حاشیه سود ناخالص |
| `recency` | `as_of − last` (روز) | **قوی‌ترین پیش‌بین تک‌متغیره، AUC ۰٫۸۵۲** |
| `tenure` | `as_of − first` | سابقهٔ کل |
| `span` | `last − first` | طول فعال رابطه |
| `avg_line` | `revenue / lines` | اندازهٔ سفارش |
| `lines_per_invoice` | `lines / invoices` | پیچیدگی سفارش |
| `gp_monthly` | `gp / months` | **نرخ سودآوری — ورودی LTV** |
| `vol_monthly` | `volume / months` | نرخ حجمی |
| `rev_monthly_real` | `revenue_real / months` | نرخ درآمد حقیقی |
| `active_ratio` | `months / (span/30.44)` | نظم حضور: ۱ = هر ماه خرید |
| `neg_lines` | تعداد خط با سود منفی | زیان‌دهی در سطح خط |
| `neg_line_pct` | `neg_lines / lines × 100` | شدت زیان‌دهی |
| `gp_destroyed` | Σ سود منفی | مبلغ زیان انباشته |
| `price_cv` | انحراف معیار ÷ میانگین قیمت واحد | پراکندگی قیمت |
| `qty_cv` | انحراف معیار ÷ میانگین مقدار | پراکندگی اندازهٔ سفارش |
| `top_family_share` | سهم بزرگ‌ترین گروه کالا (٪) | تمرکز سبد؛ >۹۰٪ = وابستگی تک‌محصولی |
| `realized_cost_share` | سهم خطوط با هزینهٔ تحقق‌یافته (٪) | **کیفیت مبنای حاشیه** |

## ۷.۴ الگوی سفارش‌گذاری (۳)

| ویژگی | فرمول | چرا |
|---|---|---|
| `order_gap` | میانگین فاصلهٔ **تاریخ‌های متمایز** سفارش | ریتم طبیعی خرید مشتری |
| `order_gap_cv` | انحراف معیار ÷ میانگین فاصله | نظم یا بی‌نظمی ریتم |
| `silence_ratio` | `recency / order_gap` (تنها اگر `order_gap ≥ 3`) | **چند برابر ریتم خودش ساکت است** |

> **نکتهٔ حیاتی:** فاصلهٔ سفارش باید روی تاریخ‌های **متمایز** حساب شود. یک فاکتور چند
> خط دارد و همهٔ خطوطش یک تاریخ دارند، پس `diff()` در سطح خط صفر می‌دهد. در نسخهٔ اول
> این اشتباه شد و `silence_ratio` برای مشتری‌ای با رکود ۲۴ روز، عدد ۱۱۲ داد.

`silence_ratio` از `recency` خام آگاه‌تر است: مشتری‌ای که هر ۹ روز سفارش می‌داد و ۶۰
روز ساکت است (نسبت ۶٫۷) نگران‌کننده‌تر از مشتری‌ای است که هر ۹۰ روز سفارش می‌داد و ۶۰
روز ساکت است (نسبت ۰٫۷).

## ۷.۵ روند (۷)

| ویژگی | فرمول | چرا |
|---|---|---|
| `v6` | حجم ۱۸۲ روز اخیر | فعالیت جاری |
| `v6_prior` | حجم ۱۸۲ روز پیش از آن | مبنای مقایسه |
| `vol_trend` | `(v6 − v6_prior) / v6_prior × 100` | **جهت حجم، مستقل از قیمت** |
| `gp6` / `gp6_prior` | سود ناخالص همان دو پنجره | جهت سودآوری |
| `margin6` | حاشیه شش ماه اخیر | حاشیهٔ جاری |
| `margin_drift` | `margin6 − margin` | جابه‌جایی حاشیه نسبت به تاریخ |

## ۷.۶ مطالبات (۱۰)

| ویژگی | فرمول | چرا |
|---|---|---|
| `invoiced` | Σ مبلغ فاکتور | مبلغ فاکتورشده |
| `collected` | Σ مبلغ وصول | مبلغ نقد شده |
| `overdue` | Σ مانده باز فاکتورهای سررسیدگذشته | **ریسک اعتباری واقعی** |
| `collection_rate` | `collected / invoiced × 100` | کیفیت وصول |
| `net_contrib` | `gp − overdue` | **مشارکت خالص** |
| `days_late` | میانگین روز تأخیر | سرعت پرداخت |
| `bounced` | تعداد چک برگشتی | نشانهٔ تنش نقدی |
| `credit_util` | `overdue / credit_limit × 100` | عبور از سقف اعتبار |
| `oldest_overdue_days` | عمر قدیمی‌ترین معوق | مطالبات مشکوک‌الوصول |

## ۷.۷ دامنه‌های دیگر (۳۲)

**شکایت:** `complaints`، `cmp_open`، `cmp_severe`، `cmp_last_days`

**تعاملات CRM:** `interactions`، `last_touch`، `touch_per_month`، `open_actions`، و
شمارش به تفکیک نوع: `chase_n` (وصول)، `quality_n` (کیفیت)، `price_talk_n` (قیمت)،
`plan_n` (برنامه خرید)، `sample_n` (نمونه)، به‌علاوهٔ `chase_share`

> `chase_share` یعنی چند درصد تعاملات این مشتری صرف پیگیری وصول شده. عدد بالا یعنی
> وقت کارشناس فروش به‌جای فروش، صرف وصول می‌شود.

**توسعه محصول:** `dev_reqs`، `dev_approved`، `dev_rejected`، `dev_pending`

**آفر:** `offers`، `off_accepted`، `off_rejected`، `off_pending`، `off_accept_rate`،
`off_discount`

**سهم از سبد:** `wallet` (میانگین)، `wallet_last` (آخرین ماه)، `wallet_trend`
(آخرین − میانگین)، `est_purchase` (خرید برآوردی مشتری)، `wallet_months`

**آزمایشگاه:** `lab_n`، `lab_fail`، `cv_evenness`

**بازار:** `market_signals`، `price_pressure_signals`

**مقایسهٔ نسبی:** `segment_avg_margin`، `segment_avg_wallet`، `margin_vs_segment`

> این سه ویژگی مقایسهٔ نسبی را ممکن می‌کنند: «حاشیهٔ ۷٪» بی‌معناست تا وقتی ندانیم
> میانهٔ بخش چقدر است.

---

# ۸. فرهنگ متریک‌ها با فرمول کامل

هر متریکی که در داشبورد یا پاسخ دستیار دیده می‌شود، اینجا تعریف دقیق دارد.

## ۸.۱ متریک‌های فروش

### فروش اسمی (`revenue_nominal`)

```
Σ مبلغ کل خطوط فروش
```
**واحد:** واحد پول منبع (نامشخص). **تفسیر:** اندازهٔ اسمی. **هشدار:** برای روند
استفاده نکنید؛ ۸ برابر تورم قیمت در آن نهفته است.

### فروش حقیقی (`revenue_real`)

```
Σ (مبلغ کل خط ÷ شاخص قیمت ماه × ۱۰۰)
```
**تفسیر:** فروش به قیمت‌های فروردین ۱۳۹۹. قابل جمع و قابل مقایسه بین دوره‌ها.

### حجم (`volume`)

```
Σ مقدار (کیلوگرم)
```
**تفسیر:** مبنای درست هر تحلیل روند. کاملاً مصون از تورم.

### روند حجم شش‌ماهه (`volume_trend_pct`)

```
(حجم ۱۸۲ روز اخیر − حجم ۱۸۲ روز پیش از آن) ÷ حجم ۱۸۲ روز پیش × ۱۰۰
```
**واحد:** درصد. **مقدار `None`** وقتی پنجرهٔ قبلی صفر باشد (مشتری تازه).

## ۸.۲ متریک‌های سودآوری

### بهای تمام‌شدهٔ واحد (`unit_cost`)

```
اگر هزینهٔ تحقق‌یافته موجود بود  →  همان
وگرنه                            →  هزینهٔ برآوردی محصول-ماه
```
پرچم `cost_basis` می‌گوید کدام. پوشش کل: ۱۰۰٪.

### سود ناخالص (`gross_profit`)

```
Σ (قیمت واحد − بهای تمام‌شدهٔ واحد) × مقدار
```

### حاشیه سود ناخالص (`gross_margin_pct`)

```
سود ناخالص ÷ فروش اسمی × ۱۰۰
```
**تفسیر:** نسبت است، پس از تورم مصون. **هشدار الزامی:** همیشه با
`realized_cost_share_pct` بخوانید. مبنای برآوردی حاشیه را ~۵ واحد درصد خوش‌بینانه‌تر
نشان می‌دهد.

### سود ماهانه (`gp_monthly`)

```
سود ناخالص ÷ تعداد ماه فعال
```
**تفسیر:** نرخ سودآوری. ورودی مستقیم LTV و ارزش نجات.

### خطوط زیان‌ده (`negative_margin_lines` و `negative_margin_line_pct`)

تعداد و درصد خطوطی که سود ناخالص منفی دارند. **چرا مهم:** حاشیهٔ کل مثبت می‌تواند
از میانگین‌گیری بیاید در حالی که یک‌سوم خطوط پول از دست می‌دهد.

### زیان انباشته (`gross_profit_destroyed`)

```
Σ سود ناخالص خطوطی که سودشان منفی است
```
عددی منفی. در سبد کل: −۵۰٫۶ میلیون، معادل ۱۰٪ سود ناخالص مثبت.

## ۸.۳ متریک‌های مطالبات

### تاریخ سررسید (محاسبه‌شده)

```
تاریخ فاکتور + مهلت پرداخت مشتری (روز)
```
**تبصره:** ۵۲۶ از ۶۴۴ مشتری مهلت صفر دارند.

### مانده باز (`uncollected`)

```
max(مبلغ فاکتورشده − مبلغ وصول‌شده، ۰)
```

### معوق (`uncollected_overdue`)

```
Σ مانده باز فاکتورهایی که سررسیدشان ≤ تاریخ برش است
```
**تفسیر: ریسک اعتباری.** کل سبد: ۵۵۷٫۱ میلیون = ۱۲٫۷٪ فروش = **۱٫۲۶ برابر کل سود
ناخالص**.

### سررسیدنشده (`uncollected_not_yet_due`)

```
Σ مانده باز فاکتورهایی که سررسیدشان > تاریخ برش است
```
**تفسیر: سرمایه در گردش، نه ریسک.** کل سبد: ۱۶۶٫۴ میلیون.

### نرخ وصول (`collection_rate_pct`)

```
وصول‌شده ÷ فاکتورشده × ۱۰۰
```

### عمر قدیمی‌ترین معوق (`oldest_overdue_days`)

```
تاریخ برش − قدیمی‌ترین تاریخ سررسید دارای مانده باز
```
**آستانهٔ اقدام:** بیش از ۳۶۵ روز → ارجاع به کمیته اعتباری.

### مشارکت خالص (`net_contribution`)

```
سود ناخالص − معوق
```
**تفسیر:** مهم‌ترین متریک بازآرایی سبد. کل سبد: **منفی ۱۱۳٫۹ میلیون**. ۳۱۰ مشتری
(۵۴٫۹٪ فروش) مشارکت منفی دارند.

> **چرا فقط معوق کسر می‌شود:** سررسیدنشده هنوز بدهی معوق نیست. کسر کردنش، رابطهٔ
> سالم را زیان‌ده نشان می‌دهد.

### مصرف سقف اعتبار (`credit_limit_utilisation_pct`)

```
معوق ÷ سقف اعتبار × ۱۰۰
```
بالای ۱۰۰٪ یعنی کنترل اعتباری در عمل اجرا نشده.

## ۸.۴ متریک‌های ارزش

### ارزش طول عمر محقق‌شده (`ltv_historic`)

```
سود ناخالص − معوق
```
(همان مشارکت خالص، با نام دیگر در بستر LTV)

### ضریب تنزیل (`DISCOUNT_FACTOR`)

```
Σ (۱ ÷ (۱ + ۰٫۰۱۵)^i)  برای i از ۱ تا ۲۴  =  ۲۰٫۰۳
```
افق ۲۴ ماه، نرخ تنزیل ماهانه ۱٫۵٪ (سالانه ~۱۹٦٪ مرکب — متناسب با نرخ‌های محیط).

### ضریب وصول (`ltv_collection_factor`)

```
min(نرخ وصول ÷ ۱۰۰، ۱)   [پیش‌فرض ۰٫۹ اگر نرخ وصول نامعلوم]
```

### ارزش آیندهٔ تنزیل‌شده (`ltv_future`)

```
سود ماهانه × احتمال ماندگاری × ۲۰٫۰۳ × ضریب وصول
```

### ارزش طول عمر کل (`ltv_total`)

```
ltv_historic + ltv_future
```
**کل سبد:** ۲۴۲٫۱ میلیون = منفی ۱۱۳٫۹ محقق‌شده + ۳۵۶٫۰ آیندهٔ تنزیل‌شده.
**۲۰۶ مشتری LTV منفی دارند.**

> **چرا رتبهٔ LTV با رتبهٔ فروش یکی نیست:** ۱۰ نفر از ۲۰ مشتری اول فروش در ۲۰ اول LTV
> نیستند. ویژگی `rank_gap = revenue_rank − ltv_rank` این فاصله را می‌سنجد؛ عدد منفی
> بزرگ یعنی «بزرگ به نظر می‌رسد ولی ارزشش کم است».

### ارزش نجات (`rescue_value`)

```
سود ماهانه × (۱ − احتمال ماندگاری) × ۱۲
```
**تفسیر:** اگر این مشتری برود، سالانه چقدر سود از دست می‌رود. مبنای رتبه‌بندی فهرست
«نجات فوری».

## ۸.۵ متریک‌های رابطه

### سهم از سبد (`wallet_share`)

```
خرید از ما ÷ خرید کل برآوردی مشتری × ۱۰۰
```
**هشدار:** این **برآورد کارشناس** است نه اندازه‌گیری. منابع برآورد: اظهار مشتری،
کارشناس فروش، برآورد بازدید. ۸۰٫۵٪ ردیف‌ها خرید صفر دارند. عدد را سقف بدانید نه واقعیت.

### نسبت سکوت (`silence_ratio`)

```
رکود ÷ فاصلهٔ معمول سفارش
```
فقط برای مشتریانی با `order_gap ≥ 3` محاسبه می‌شود (۴۸۲ از ۶۲۴). میانه: ۴٫۳۲.

### اثر شکایت بر خرید (`purchase_impact`)

```
حجم ۹۰ روز پیش از نخستین شکایت  در برابر  حجم ۹۰ روز پس از آن
```
پرچم `window_complete` می‌گوید آیا پنجرهٔ «بعد» کامل در بازهٔ داده هست.
۵۸ مشتری افت بیش از ۲۵٪ دارند.

> **هشدار تفسیری الزامی:** این شاهد است نه اثبات علیت. در سطح **کل سبد** آزمودیم و
> شکایت پیش‌بین ریزش **نبود** (بخش ۲۰). این متریک برای بررسی موردی است.

---

# ۹. RFM

## ۹.۱ سه بُعد

| بُعد | متریک | جهت |
|---|---|---|
| **R** (تازگی) | `recency` — روز از آخرین خرید | کمتر بهتر |
| **F** (تکرار) | `months` — تعداد ماه فعال | بیشتر بهتر |
| **M** (ارزش پولی) | `revenue_real` — **فروش حقیقی** | بیشتر بهتر |

هر بُعد به پنج‌شکِ (quintile) ۱ تا ۵ تبدیل می‌شود.

> **تصمیم کلیدی:** M بر پایهٔ فروش **حقیقی** است نه اسمی. با فروش اسمی، هر مشتری که
> اخیراً خرید کرده خودکار پرارزش‌تر می‌شد — چون قیمت‌ها ۸ برابر شده‌اند. این یک اشتباه
> ساختاری در RFM روی دادهٔ تورمی است.
>
> F بر پایهٔ **تعداد ماه فعال** است نه تعداد فاکتور، چون تعداد فاکتور با اندازهٔ مشتری
> همبسته است و F را به M نزدیک می‌کند.

## ۹.۲ یازده بخش

منطق نگاشت (`features.rfm_segment`) بر پایهٔ R و میانگین (F+M)/2:

| بخش | شرط | تفسیر |
|---|---|---|
| قهرمانان | R≥۴ و FM≥۴ | اخیراً خرید کرده، پرتکرار، پرارزش — ستون فروش |
| وفادار | R≥۳ و FM≥۳ | خرید منظم و ارزش خوب |
| وفادار بالقوه | R≥۴ و FM≥۲ | اخیراً فعال، ارزش در حال ساخت |
| تازه‌وارد | R≥۴ و FM<۲ | خرید تازه ولی سابقهٔ کم |
| نیازمند توجه | R=۳ | میانهٔ همه‌چیز؛ بدون اقدام به سمت ریزش می‌رود |
| نمی‌توان از دست داد | R=۲ و FM≥۴ | پرارزش ولی مدتی نیامده — اولویت بازیابی |
| در معرض ریزش | R=۲ و FM≥۲ | ارزش متوسط و رکود در حال رشد |
| در حال خواب | R=۲ و FM<۲ | فعالیت کم و در حال کاهش |
| ازدست‌رفتهٔ باارزش | R=۱ و FM≥۴ | پرارزش بوده و رفته — بیشترین زیان نهفته |
| خفته | R=۱ و FM≥۲ | مدت‌ها بی‌خرید، ارزش متوسط |
| ازدست‌رفته | R=۱ و FM<۲ | بی‌خرید و کم‌ارزش |

## ۹.۳ توزیع واقعی سبد

| بخش | مشتری | R | F | M | سود ناخالص | LTV | رکود میانه |
|---|---|---|---|---|---|---|---|
| قهرمانان | ۱۳۷ | ۴٫۵۸ | ۴٫۶۸ | ۴٫۵۸ | ۳۶۹٫۴ م | ۱۷۷٫۱ م | ۶۶ روز |
| وفادار | ۱۲۶ | ۳٫۶۰ | ۳٫۷۶ | ۳٫۳۸ | ۴۷٫۷ م | ۲۵٫۸ م | ۲۱۳ روز |
| نمی‌توان از دست داد | ۲۰ | ۲٫۰۰ | ۳٫۹۰ | ۴٫۶۰ | ۸٫۹ م | ۲٫۳ م | ۴۶۸ روز |
| ازدست‌رفتهٔ باارزش | ۵ | ۱٫۰۰ | ۳٫۶۰ | ۵٫۰۰ | ۶٫۳ م | ۴٫۳ م | ۶۸۲ روز |
| در معرض ریزش | ۷۲ | ۲٫۰۰ | ۲٫۶۵ | ۲٫۸۸ | ۴٫۸ م | ۱۲٫۰ م | ۴۵۱ روز |
| خفته | ۷۱ | ۱٫۰۰ | ۲٫۱۳ | ۲٫۹۲ | ۳٫۲ م | ۶٫۶ م | ۷۲۴ روز |
| وفادار بالقوه | ۳۴ | ۴٫۲۱ | ۲٫۲۶ | ۲٫۱۲ | ۱٫۵ م | ۱۳٫۵ م | ۱۱۸ روز |
| نیازمند توجه | ۴۴ | ۳٫۰۰ | ۲٫۰۹ | ۱٫۶۸ | ۱٫۳ م | ۱٫۳ م | ۲۷۲ روز |
| ازدست‌رفته | ۵۱ | ۱٫۰۰ | ۱٫۳۱ | ۱٫۲۴ | ۶۴ هزار | ۱۶۱ هزار | ۷۴۵ روز |
| در حال خواب | ۴۱ | ۲٫۰۰ | ۱٫۲۷ | ۱٫۲۲ | ۷ هزار | −۱۷۱ هزار | ۴۵۱ روز |
| **تازه‌وارد** | ۲۳ | ۴٫۲۶ | ۱٫۳۵ | ۱٫۲۲ | **−۳۴ هزار** | **−۷۱۶ هزار** | ۹۴ روز |

**یافتهٔ قابل اقدام:** بخش «تازه‌وارد» سود ناخالص **منفی** دارد. جذب مشتری جدید با
قیمت زیر بهای تمام‌شده انجام می‌شود. قیمت‌گذاری سفارش نخست باید بازبینی شود.

**یافتهٔ دوم:** «قهرمانان» ۱۳۷ مشتری با ۳۶۹ میلیون سود ناخالص دارند، ولی
۴۵۵ میلیون معوق — یعنی حتی قهرمانان هم معوقشان از سودشان بیشتر است.

---

# ۱۰. امتیاز ماندگاری و کارت مدل

## ۱۰.۱ ساختار امتیاز

امتیاز **افزایشی** است: از پایهٔ ۰٫۵۰ شروع می‌شود و هر نشانه سهم مثبت یا منفی
می‌گذارد. سهم هر نشانه در پروفایل قابل نمایش است.

| نشانه | شرط | سهم |
|---|---|---|
| **رکود خرید** | ≤۴۵ روز | +۰٫۲۲ |
| | ۴۶–۹۰ | +۰٫۱۲ |
| | ۹۱–۱۸۰ | ۰٫۰۰ |
| | ۱۸۱–۳۶۵ | −۰٫۲۲ |
| | >۳۶۵ | −۰٫۳۴ |
| **روند حجم ۶ماهه** | >+۲۵٪ | +۰٫۱۲ |
| | −۲۵٪ تا +۲۵٪ | +۰٫۰۴ |
| | −۶۰٪ تا −۲۵٪ | −۰٫۰۸ |
| | <−۶۰٪ | −۰٫۱۴ |
| **تنوع گروه کالا** | ≥۳ گروه | +۰٫۱۰ |
| | ۲ گروه | +۰٫۰۵ |
| | ۱ گروه | −۰٫۰۵ |
| **سهم از سبد** | ≥۳۰٪ | +۰٫۱۰ |
| | ۱۰–۳۰٪ | +۰٫۰۴ |
| | <۱۰٪ | −۰٫۰۴ |
| **درخواست توسعه** | ≥۳ | +۰٫۰۶ |
| | ۱–۲ | +۰٫۰۳ |
| **فاصله از آخرین تعامل** | >۲۷۰ روز | −۰٫۰۸ |
| **شکایت باز** | ≥۱ | −۰٫۰۶ |
| **نرخ وصول** | <۷۰٪ | −۰٫۰۸ |
| | ۷۰–۸۵٪ | −۰٫۰۴ |

امتیاز خام در بازهٔ [۰٫۰۲، ۰٫۹۷] کلمپ می‌شود.

## ۱۰.۲ کالیبراسیون

امتیاز خام محافظه‌کار بود — در همهٔ باندها نرخ واقعی بازگشت از امتیاز بیشتر بود. پس
روی **پنجرهٔ آموزش** کالیبره شد:

| باند امتیاز خام | میانگین امتیاز | نرخ واقعی بازگشت |
|---|---|---|
| ۰٫۰۰–۰٫۲۰ | ۰٫۰۸۷ | ۰٫۲۸۷ |
| ۰٫۲۰–۰٫۳۵ | ۰٫۲۸۸ | ۰٫۳۵۳ |
| ۰٫۳۵–۰٫۵۰ | ۰٫۴۳۹ | ۰٫۵۱۸ |
| ۰٫۵۰–۰٫۶۵ | ۰٫۵۷۹ | ۰٫۶۱۱ |
| ۰٫۶۵–۰٫۸۰ | ۰٫۷۱۶ | ۰٫۸۱۵ |
| ۰٫۸۰–۱٫۰۰ | ۰٫۸۵۷ | ۰٫۹۶۶ |

درون‌یابی خطی بین این نقاط، امتیاز خام را به احتمال کالیبره تبدیل می‌کند:

```python
CAL_X = [0.10, 0.275, 0.425, 0.575, 0.725, 0.905]
CAL_Y = [0.287, 0.353, 0.518, 0.611, 0.815, 0.966]
```

**نتیجه:** میانگین خطای مطلق کالیبراسیون روی پنجرهٔ **آزمون** از ۰٫۰۹۸ به **۰٫۰۴۰**
رسید.

## ۱۰.۳ کارت مدل

| مورد | مقدار |
|---|---|
| هدف | خرید مشتری در ۱۲ ماه پس از تاریخ برش |
| روش | امتیاز افزایشی با وزن‌های دستی، کالیبره‌شده روی پنجرهٔ قدیمی‌تر |
| پنجرهٔ آموزش | ویژگی در ۱۳۹۹/۱۰/۱۱ — ۴۴۸ مشتری، نرخ بازگشت ۶۲٫۷٪ |
| پنجرهٔ آزمون | ویژگی در ۱۴۰۰/۰۴/۰۹ — ۵۲۶ مشتری، نرخ بازگشت ۵۴٫۲٪ |
| **AUC امتیاز افزایشی** | **۰٫۸۳۱** |
| AUC رکود تنها | **۰٫۸۵۲** |
| AUC رگرسیون لجستیک | **۰٫۸۶۸** |
| خطای کالیبراسیون | ۰٫۰۹۸ → ۰٫۰۴۰ |

### نکتهٔ صادقانه

**رکود تنها (AUC ۰٫۸۵۲) از امتیاز ترکیبی ما (۰٫۸۳۱) بهتر پیش‌بینی می‌کند** و رگرسیون
لجستیک تا ۰٫۸۶۸ می‌رسد. این را پنهان نکردیم — در داشبورد، در تب «الگوها و مدل»،
نوشته شده.

امتیاز افزایشی را نگه داشتیم چون **سهم هر نشانه در آن قابل نمایش و بازرسی است**. برای
تصمیم انسانی، توضیح‌پذیری بر ۰٫۰۴ واحد AUC ارجحیت دارد. ولی این یک **انتخاب** است، نه
یک برتری فنی.

### محدودیت

سهم از سبد در پنجرهٔ اعتبارسنجی قابل استفاده نبود، چون دادهٔ آن از تیر ۱۴۰۰ شروع
می‌شود — یعنی **پس از** تاریخ برش آموزش. پس وزن آن نشانه، تنها بخش امتیاز است که
اعتبارسنجی خارج‌از‌زمان ندارد.

## ۱۰.۴ ارزش عملی

اگر بر پایهٔ امتیاز اولویت‌بندی کنیم:

- ۲۰ مشتری با پایین‌ترین امتیاز: نرخ ریزش واقعی **۸۵٪** (در برابر ۴۵٫۸٪ کل جامعه)
- ۵۰ مشتری: **۸۰٪**
- ۱۰۰ مشتری: **۸۲٪**

و فهرست «نجات فوری» (امتیاز × سود ماهانه): ۲۰ مشتری اول، نرخ ریزش واقعی ۶۰٪ و
۳۴ میلیون سود آیندهٔ ازدست‌رفته.

---

# ۱۱. ارزش طول عمر (LTV)

## ۱۱.۱ فرمول کامل

```
LTV = ارزش محقق‌شده  +  ارزش آیندهٔ تنزیل‌شده

ارزش محقق‌شده      = سود ناخالص انباشته − معوق
ارزش آیندهٔ تنزیل‌شده = سود ماهانه × احتمال ماندگاری × ۲۰٫۰۳ × ضریب وصول

که در آن:
  سود ماهانه       = سود ناخالص ÷ تعداد ماه فعال
  احتمال ماندگاری  = امتیاز کالیبره‌شده (بخش ۱۰)
  ۲۰٫۰۳            = Σ ۱/(۱٫۰۱۵)^i برای i=۱..۲۴   (افق ۲۴ ماه، نرخ ۱٫۵٪ ماهانه)
  ضریب وصول        = min(نرخ وصول/۱۰۰، ۱)
```

## ۱۱.۲ چرا هر جزء

| جزء | چرا لازم است |
|---|---|
| کسر معوق از بخش محقق‌شده | سودی که وصول نشده، سود نیست |
| احتمال ماندگاری | سود آینده مشروط به ماندن مشتری است |
| ضریب تنزیل | یک ریال دو سال بعد کمتر از یک ریال امروز ارزش دارد |
| ضریب وصول | مشتری‌ای که ۸۰٪ وصول دارد، ۸۰٪ سود آینده‌اش نقد می‌شود |

## ۱۱.۳ کل سبد

```
LTV کل  =  ۲۴۲٫۱ میلیون
        =  (−۱۱۳٫۹ محقق‌شده)  +  (۳۵۶٫۰ آیندهٔ تنزیل‌شده)
```

بخش محقق‌شده **منفی** است چون معوق کل سبد (۵۵۷ میلیون) از سود ناخالص کل
(۴۴۳ میلیون) بیشتر است. **۲۰۶ مشتری LTV منفی دارند.**

## ۱۱.۴ نمونهٔ تجزیه‌شده — `C_245948`

```
سود ناخالص انباشته              ۵۰٫۵ میلیون  (حاشیه ۱۶٫۱٪)
معوق                          − ۲۳٫۷ میلیون
─────────────────────────────────────────────
ارزش محقق‌شده                    ۲۶٫۷ میلیون

سود ماهانه                       ۱٫۹ میلیون
× احتمال ماندگاری                     ۰٫۹۷
× ضریب تنزیل                         ۲۰٫۰۳
× ضریب وصول                           ۰٫۸۱
─────────────────────────────────────────────
ارزش آیندهٔ تنزیل‌شده             ۳۰٫۳ میلیون

LTV کل                          ۵۷٫۰ میلیون   ← رتبه ۱ از ۶۴۴
رتبهٔ فروش ۲ / رتبهٔ LTV ۱ → اختلاف +۱ پله
```

## ۱۱.۵ محدودیت‌های LTV

- **افق ۲۴ ماه دلخواه است.** با افق ۳۶ ماه، همهٔ اعداد بزرگ‌تر می‌شوند ولی رتبه‌بندی
  تقریباً ثابت می‌ماند.
- **نرخ تنزیل ۱٫۵٪ ماهانه** یک فرض است. در داشبورد کنار عدد نوشته شده.
- **سود ماهانه از میانگین گذشته می‌آید**، پس برای مشتری با روند نزولی شدید، خوش‌بینانه
  است. `vol_trend` این را جبران نمی‌کند — بهبود ممکن برای دور بعد.

---

# ۱۲. چهارخانهٔ حاشیه در برابر ریسک

## ۱۲.۱ دو محور

| محور | متریک | آستانه |
|---|---|---|
| عمودی | حاشیه سود ناخالص | میانهٔ سبد = **۸٫۹۱٪** |
| افقی | احتمال ماندگاری کالیبره | **۰٫۵۰** |

## ۱۲.۲ چهار خانه

| خانه | شرط | مشتری | سود ناخالص | LTV | حاشیه میانه | ماندگاری میانه |
|---|---|---|---|---|---|---|
| **رشد بده** | حاشیه ≥ میانه، ماندگاری ≥ ۰٫۵ | ۱۰۱ | ۳۰۲٫۰ م | ۲۷۳٫۷ م | ۱۲٫۲۸٪ | ۰٫۷۰ |
| **نجات فوری** | حاشیه ≥ میانه، ماندگاری < ۰٫۵ | **۲۱۱** | ۶۹٫۸ م | ۵۵٫۹ م | ۱۳٫۴۶٪ | ۰٫۲۹ |
| **اصلاح قیمت** | حاشیه < میانه، ماندگاری ≥ ۰٫۵ | ۱۲۴ | ۵۷٫۲ م | −۷۷٫۸ م | ۶٫۰۲٪ | ۰٫۷۴ |
| **بازبینی رابطه** | حاشیه < میانه، ماندگاری < ۰٫۵ | ۱۸۸ | ۱۴٫۲ م | −۹٫۶ م | ۵٫۱۰٪ | ۰٫۲۹ |

## ۱۲.۳ اقدام هر خانه

| خانه | تفسیر | اقدام پیشنهادی |
|---|---|---|
| رشد بده | حاشیه خوب، ریسک کم | سرمایه‌گذاری برای رشد حجم و سهم از سبد؛ قرارداد بلندمدت |
| **نجات فوری** | **حاشیه خوب ولی در حال از دست رفتن — گران‌ترین خانه** | تماس مدیر فروش در هفتهٔ جاری؛ ریشهٔ قطع خرید را پیدا کنید |
| اصلاح قیمت | پایدار ولی حاشیه زیر میانه | بازنگری قیمت یا حذف کدهای زیان‌ده؛ رابطه تحمل اصلاح را دارد |
| بازبینی رابطه | حاشیه زیر میانه و در حال رفتن | هزینهٔ بازیابی را با ارزش بسنجید؛ اولویت آخر منابع فروش |

## ۱۲.۴ چگونه فهرست عملیاتی می‌شود

۲۱۱ مشتری در «نجات فوری» فهرست عملیاتی نیست. ستون **ارزش نجات** آن را مرتب می‌کند:

```
ارزش نجات = سود ماهانه × (۱ − احتمال ماندگاری) × ۱۲
```

بیست مشتری اول این فهرست، فهرست تماس هفتهٔ جاری است. `rescue_rank` در پروفایل و جدول
موجود است.

---

# ۱۳. نرخ‌های پایهٔ تجربی

## ۱۳.۱ چرا نرخ پایه

هر عددی که در این سامانه به‌عنوان «احتمال» نمایش داده می‌شود، از **شمارش رویداد واقعی
در همین داده** می‌آید — نه از حس کارشناسی، نه از عدد گردی مثل «۵۰٪». روش کار در
`calibrate_rates.py`:

۱. ۲۱ تاریخ برش با فاصلهٔ یک ماه انتخاب شد.
۲. در هر تاریخ برش، `as_of_view()` صدا زده شد تا فقط دادهٔ آن لحظه دیده شود.
۳. ویژگی‌های هر مشتری در آن لحظه محاسبه شد (رکود، سابقهٔ شکایت، …).
۴. سپس در **آیندهٔ همان مشتری** بررسی شد که آیا رویداد رخ داد یا نه.
۵. نرخ = تعداد رخداد ÷ تعداد مشاهده، در هر باکت.

مجموع مشاهدات: **۱۰٬۳۳۹ مشاهدهٔ مشتری-ماه** برای سفارش مجدد و **۱٬۳۶۱ مشاهدهٔ
مشتری-فصل** برای شکایت و درخواست توسعه.

## ۱۳.۲ سفارش مجدد در ۹۰ روز، بر پایهٔ رکود

`REORDER_BY_RECENCY` در `signals.py`:

| بازهٔ رکود | نرخ سفارش مجدد | تعداد مشاهده |
|---|---|---|
| ۰ تا ۳۰ روز | **۷۸٫۴٪** | ۳٬۲۷۲ |
| ۳۱ تا ۶۰ روز | ۵۵٫۰٪ | ۱٬۱۹۲ |
| ۶۱ تا ۹۰ روز | ۴۳٫۸٪ | ۸۶۱ |
| ۹۱ تا ۱۵۰ روز | ۳۱٫۶٪ | ۱٬۲۵۸ |
| ۱۵۱ تا ۲۴۰ روز | ۱۵٫۰٪ | ۱٬۲۳۰ |
| ۲۴۱ تا ۳۶۵ روز | ۹٫۵٪ | ۱٬۱۳۲ |
| بیش از ۳۶۵ روز | **۴٫۹٪** | ۱٬۳۹۴ |

منحنی یکنوا و شیب‌دار است: بین باکت اول و آخر **۱۶ برابر** اختلاف. همین جدول توضیح
می‌دهد چرا رکود قوی‌ترین ویژگی مدل ماندگاری است (بخش ۱۰).

## ۱۳.۳ شکایت جدید در ۱۸۰ روز، بر پایهٔ سابقهٔ شکایت

`COMPLAINT_BY_HISTORY`:

| سابقه | نرخ شکایت جدید | تعداد مشاهده |
|---|---|---|
| بدون شکایت پیشین | ۶٫۵٪ | ۱٬۰۰۱ |
| ۱ شکایت | ۱۵٫۳٪ | ۱۷۷ |
| ۲ تا ۳ شکایت | ۳۶٫۰٪ | ۱۱۴ |
| ۴ شکایت یا بیشتر | **۷۱٫۰٪** | ۶۹ |

**تفسیر مهم:** این «شکایت باعث ریزش می‌شود» نیست. این «شاکی سابق، شاکی آینده است»
است — یعنی شکایت یک ویژگی پایدار مشتری/محصول است. در بخش ۲۰ نشان می‌دهیم که اثر
شکایت بر ماندگاری، پس از کنترل عمق رابطه، از بین می‌رود.

## ۱۳.۴ درخواست توسعهٔ جدید در ۱۸۰ روز

`DEV_BY_HISTORY`:

| سابقه | نرخ درخواست جدید | تعداد مشاهده |
|---|---|---|
| بدون درخواست پیشین | ۱۰٫۸٪ | ۸۸۸ |
| ۱ درخواست | ۱۸٫۲٪ | ۲۲۵ |
| ۲ تا ۳ درخواست | ۴۵٫۳٪ | ۱۳۷ |
| ۴ درخواست یا بیشتر | **۷۴٫۸٪** | ۱۱۱ |

## ۱۳.۵ پذیرش آفر — نمونه‌ای از یک آزمون که نتیجهٔ منفی داد

`OFFER_ACCEPT_BY_REASON`:

| دلیل آفر | نرخ پذیرش |
|---|---|
| تسویه سریع | ۳۷٫۶٪ |
| معرفی محصول جدید | ۳۶٫۴٪ |
| افزایش حجم سفارش | ۳۳٫۷٪ |
| آزمون محصول | ۳۳٫۳٪ |
| افزایش سهم از سبد | ۳۲٫۰٪ |
| حفظ مشتری کلیدی | ۳۰٫۰٪ |
| رقابت قیمتی | ۲۹٫۵٪ |
| **کل** | **۳۳٫۳٪** |

آزمون کای‌دو: **۱۰٫۶ با ۱۲ درجه آزادی** (آستانهٔ معناداری ۵٪ ≈ ۱۷). یعنی **دلیل آفر
پذیرش را پیش‌بینی نمی‌کند.** دامنهٔ ۲۹٫۵٪ تا ۳۷٫۶٪ در برابر خطای نمونه‌گیری نمی‌ایستد.

این نتیجه معماری بخش فرصت‌ها را عوض کرد: دلیل آفر تعیین می‌کند **«اقدام درست چیست و
مسئولش کیست»**، نه **«چقدر احتمال موفقیت دارد»**. نگاشت `OFFER_REASON_PLAY` (بخش ۱۷)
از همین جا آمد.

## ۱۳.۶ تأخیر پرداخت — نمونهٔ دوم نتیجهٔ منفی

نرخ تأخیر بالای میانه (میانهٔ سبد ۲۳ روز):

| سابقهٔ پرداخت | احتمال تأخیر بالای میانه |
|---|---|
| سابقهٔ پاک | ۳۹٫۷٪ |
| نرخ پایهٔ کل سبد | **۵۱٪** |
| سابقهٔ بد (چک برگشتی یا وصول ناقص) | ۵۷٫۹٪ |

دامنه فقط ۱۸ واحد درصد است، در حالی که برای سفارش مجدد ۷۳ واحد بود. یعنی **تأخیر
پرداخت در این سبد تقریباً ساختاری است** — ویژگی صنعت و شرایط اقتصادی، نه ویژگی مشتری
خاص. `LATE_PAYMENT_NOTE` همین را در UI به کارشناس می‌گوید، و اطمینان پیش‌بینی تأخیر
صریحاً «پایین» علامت خورده است.

## ۱۳.۷ تنها نرخ پایهٔ غیرتجربی

از ۶ پیش‌بینی، پنج مورد نرخ پایهٔ تجربی دارند. یکی — **درخواست تخفیف** — ندارد، چون
رویداد «مشتری تخفیف خواست» به‌صورت مستقل در داده ثبت نمی‌شود (فقط آفر صادرشده ثبت
می‌شود، که خروجی فرایند است نه ورودی آن). نرخ پایهٔ ۲۰٪ فرضی است و در UI با اطمینان
«پایین» و یادداشت صریح نمایش داده می‌شود:

> برخلاف بقیهٔ پیش‌بینی‌ها، نرخ پایهٔ این مورد از داده استخراج نشد … عدد را نسبی بخوانید.

---

# ۱۴. سیگنال‌ها — ۱۸ سیگنال در ۷ حوزه

## ۱۴.۱ ساختار یک سیگنال

`extract_signals(p, f)` در `signals.py` برمی‌گرداند:

| کلید | معنا |
|---|---|
| `code` | شناسهٔ یکتای قاعده |
| `domain` | یکی از ۷ حوزه |
| `name` | عنوان فارسی |
| `direction` | `منفی` / `هشدار` / `مثبت` |
| `strength` | شدت ۰ تا ۱، از فرمول مشخص، بریده‌شده در بازه |
| `value` | مقدار عددی خوانا |
| `interpretation` | **تفسیر** — چرا این مهم است |
| `references` | فهرست رفرنس |

مرتب‌سازی: منفی → هشدار → مثبت، و داخل هر گروه بر پایهٔ شدت نزولی. این ترتیب عمدی
است: کارشناس فروش اول باید بدترین خبر را ببیند.

## ۱۴.۲ فهرست کامل با شرط فعال‌شدن و فرمول شدت

| # | کد | حوزه | جهت | شرط فعال‌شدن | فرمول شدت | تعداد مشتری |
|---|---|---|---|---|---|---|
| ۱ | `silence_gap` | خرید | منفی | `silence_ratio ≥ 2.5` و رکود > ۶۰ و `order_gap ≥ 3` | `min(1, نسبت سکوت/۱۰)` | ۲۸۳ |
| ۲ | `recent_buyer` | خرید | مثبت | رکود ≤ ۴۵ روز | ثابت ۰٫۸ | ۳۸ |
| ۳ | `volume_trend` | خرید | هر دو | `قدرمطلق روند حجم ≥ 25٪` | `min(1, قدرمطلق روند / ۱۰۰)` | ۲۹۳ |
| ۴ | `purchase_plan` | خرید | مثبت | ≥۱ تعامل نوع «برنامه خرید» | ثابت ۰٫۶ | ۳۸۴ |
| ۵ | `price_talk` | قیمت | هشدار | ≥۱ تعامل «قیمت و تخفیف» | `min(1, تعداد/۴)` | ۴۲۲ |
| ۶ | `market_price_pressure` | قیمت | هشدار | ≥۱ سیگنال بازار فشار قیمتی | ثابت ۰٫۵ | ۸ |
| ۷ | `margin_gap` | قیمت | هر دو | `قدرمطلق (حاشیه − میانهٔ سبد) ≥ 3` واحد | `min(1, فاصله/۱۰)` | ۳۶۴ |
| ۸ | `margin_drift` | قیمت | هر دو | `قدرمطلق (حاشیهٔ ۶ماه − حاشیهٔ تاریخی) ≥ 2` | `min(1, رانش/۸)` | ۱۱۱ |
| ۹ | `complaint_history` | کیفیت | منفی | ≥۱ شکایت | `min(1, تعداد/۶)` | ۱۴۲ |
| ۱۰ | `quality_purchase_link` | کیفیت | منفی | افت ≥۲۵٪ حجم پس از نخستین شکایت **و پنجرهٔ کامل** | ثابت ۰٫۹ | ۵۸ |
| ۱۱ | `lab_evenness` | کیفیت | هشدار | `CV یکنواختی > 1.9٪` | `min(1, (CV−۱٫۵)/۱)` | ۵۲ |
| ۱۲ | `quality_talk` | کیفیت | هشدار | ≥۲ تعامل «کیفیت محصول» | `min(1, تعداد/۴)` | ۱۲۶ |
| ۱۳ | `overdue` | پرداخت | منفی | معوق > ۰ | `min(1, معوق / قدرمطلق سود ناخالص)` | ۴۰۷ |
| ۱۴ | `chase` | پرداخت | منفی | ≥۲ تعامل «پیگیری وصول» | `min(1, تعداد/۴)` | ۱۳۵ |
| ۱۵ | `bounced` | پرداخت | منفی | ≥۱ چک برگشتی | `min(1, تعداد/۳)` | ۶۴ |
| ۱۶ | `dev_history` | توسعه | مثبت | ≥۱ درخواست توسعه | `min(1, تعداد/۶)` | ۲۰۱ |
| ۱۷ | `wallet_gap` | رقابت | منفی | سهم ما < میانهٔ بخش − ۳ واحد | `min(1, شکاف/۳۰)` | ۳۷۸ |
| ۱۸ | `wallet_falling` | رقابت | منفی | `wallet_trend ≤ −5` واحد | `min(1, قدرمطلق روند / ۲۵)` | ۲۴۳ |
| ۱۹ | `no_crm` | ارتباط | منفی | ۰ تعامل ثبت‌شده | ثابت ۰٫۷ | ۲۰ |
| ۲۰ | `uncontacted_buyer` | ارتباط | هشدار | آخرین تماس > ۱۸۰ روز **و** رکود < ۱۲۰ | ثابت ۰٫۶ | ۵۱ |
| ۲۱ | `open_actions` | ارتباط | هشدار | ≥۱ اقدام بعدی بازمانده | `min(1, تعداد/۸)` | ۶۲۴ |

(۲۱ کد از ۱۸ قاعده تولید می‌شود، چون سه قاعده — `volume_trend`، `margin_gap`،
`margin_drift` — بر پایهٔ علامت، دو عنوان و دو جهت مختلف می‌سازند و
`silence_gap`/`recent_buyer` دو شاخهٔ یک قاعده‌اند.)

## ۱۴.۳ چهار نکتهٔ طراحی که در جدول دیده نمی‌شود

**۱. تفسیر، نه فقط عدد.** هر سیگنال جملهٔ «چرا این مهم است» را دارد. مثال
`volume_trend` منفی:

> حجم — نه فروش اسمی — مبنای درست روند است، چون از تورم ۸ برابری قیمت مصون است.

این همان جایی است که دانش تحلیلی به کارشناس فروش منتقل می‌شود.

**۲. سیگنال شدت خودش را از داده می‌گیرد.** هیچ شدتی دستی نیست؛ همه از یک نسبت
محاسبه می‌شوند و در `[0,1]` بریده می‌شوند.

**۳. سیگنالی که نتیجهٔ منفی آزمون را حمل می‌کند.** `bounced` و
`quality_purchase_link` هر دو در متن تفسیرشان می‌گویند اثرشان در آزمون سبد تأیید
نشده. یعنی سامانه سیگنال را نشان می‌دهد ولی **ادعای پیش‌بینی نمی‌کند**:

> شاهد است نه اثبات علیت. توجه: در سطح کل سبد، شکایت پیش‌بین ریزش نیست (آزمون‌شده و
> رد شده)؛ ولی در سطح این مشتری خاص، همزمانی معنادار است.

**۴. `no_crm` دقیقاً روی همان ۲۰ مشتری فعال می‌شود که فروش ندارند.** بررسی شد:
مجموعهٔ «۰ تعامل CRM» و مجموعهٔ «۰ فروش» **کاملاً یکسان** است. پس ریسک
`no_crm_history` (که شرط `revenue > 0` دارد) برای هیچ مشتری فعال نمی‌شود — و این
خودش یافته است: در این داده هیچ مشتری فعالی بدون رکورد CRM وجود ندارد.

---

# ۱۵. پیش‌بینی تصمیم بعدی — ۶ تصمیم

## ۱۵.۱ ساختار

`predict_decisions(p, f, signals)` هر پیش‌بینی را این‌گونه می‌سازد:

```
احتمال = نرخ پایهٔ تجربی + Σ تعدیل‌ها      (بریده در بازهٔ ۰٫۰۱ تا ۰٫۹۹)
```

و هر تعدیل سه چیز دارد: `name` (چه چیزی)، `delta` (چند واحد) و `why` (بر چه مبنایی).
UI هر سه را نشان می‌دهد. یعنی کارشناس می‌بیند «۴۳٪ پایه بود، برنامهٔ خرید ۷ واحد
اضافه کرد، سکوت ۱۰ واحد کم کرد».

## ۱۵.۲ فهرست کامل

| # | کد | سؤال | نرخ پایه | تعدیل‌ها | اطمینان | تعداد |
|---|---|---|---|---|---|---|
| ۱ | `reorder_90d` | آیا در ۹۰ روز آینده سفارش می‌دهد؟ | جدول رکود (۴٫۹٪ تا ۷۸٫۴٪) | سکوت ≥۴× **−۱۰**؛ ۲+ گروه کالا **+۸**؛ برنامهٔ خرید **+۷**؛ آفر باز **+۵** | بالا اگر رکود ≤۲۴۰ | ۶۲۴ |
| ۲ | `churn` | آیا رابطه قطع می‌شود؟ | `۱ − احتمال ماندگاری کالیبره` | اجزای امتیاز ماندگاری با علامت معکوس | بالا | ۶۲۴ |
| ۳ | `new_complaint` | آیا شکایت جدید ثبت می‌کند؟ | جدول سابقهٔ شکایت (۶٫۵٪ تا ۷۱٪) | گفت‌وگوی کیفیت **+۸**؛ CV بالا **+۵** | بالا/متوسط | ۶۴۴ |
| ۴ | `new_dev_request` | آیا درخواست فنی جدید می‌دهد؟ | جدول سابقهٔ توسعه (۱۰٫۸٪ تا ۷۴٫۸٪) | تعامل نمونهٔ محصول **+۶** | متوسط | ۶۴۴ |
| ۵ | `late_payment` | آیا پرداخت بعدی با تأخیر بالای میانه است؟ | ۵۱٪ تجربی | نرخ وصول <۸۰٪ **+۶**؛ چک برگشتی **+۳**؛ پیگیری وصول **+۴** | **پایین** | ۶۲۴ |
| ۶ | `discount_request` | آیا درخواست تخفیف می‌دهد؟ | **۲۰٪ فرضی** | هر تعامل قیمت **+۱۰** (سقف ۳۰)؛ هر آفر ردشده **+۵** (سقف ۲۰)؛ فشار بازار **+۱۰**؛ حاشیه بالای میانه **+۵** | **پایین** | ۵۲۸ |

## ۱۵.۳ سه تصمیم طراحی

**الف) `churn` مدل جدا ندارد.** عمداً مکمل احتمال ماندگاری کالیبره است. اگر مدل جدا
می‌ساختیم، دو عدد ناسازگار به کارشناس می‌دادیم. یک منبع حقیقت، دو نمایش.

**ب) تعدیل‌ها جمع‌شدنی‌اند نه ضرب‌شدنی.** جمع، قابل توضیح است («۷ واحد اضافه کرد»)؛
ضرب نیست. با ۴ تعدیل حداکثری، بیشترین جابه‌جایی ۳۰ واحد است — کمتر از دامنهٔ ۷۳ واحدی
خود نرخ پایه. یعنی **نرخ پایه همیشه حرف اول را می‌زند** و تعدیل‌ها فقط تنظیم ظریف‌اند.
این عمدی است: نرخ پایه از ۱۰٬۳۳۹ مشاهده آمده، تعدیل از قضاوت.

**ج) `+۸` واحد برای «۲+ گروه کالا» تنها تعدیلی است که پشتوانهٔ آزمون دارد.** الگوی
عرض سبد در دو پنجرهٔ اعتبارسنجی مستقل تکرار شد و پس از کنترل عمق رابطه هم زنده ماند
(بخش ۲۰). بقیهٔ تعدیل‌ها قضاوت کارشناسی محتاطانه‌اند و اندازه‌شان کوچک نگه داشته شده.

---

# ۱۶. ریسک‌ها — ۱۵ قاعده

## ۱۶.۱ ساختار یک ریسک

`find_risks(p, f)` هر ریسک را با شش جزء می‌سازد:

| جزء | نقش |
|---|---|
| `severity` | `critical` / `high` / `medium` / `low` → وزن فوریت ۱٫۰ / ۰٫۷ / ۰٫۴ / ۰٫۲ |
| `evidence` | **شاهد عددی** — چه چیزی در داده دیده شد |
| `logic` | **استدلال** — چرا این ریسک است |
| `action` | اقدام مشخص و اجراپذیر |
| `owner` | واحد مسئول (۷ واحد) |
| `value_at_stake` | **مبلغ در معرض خطر** — ورودی فرمول اولویت |

جدایی `evidence` از `logic` تصمیم آگاهانه است: شاهد قابل انکار نیست، استدلال قابل
بحث است. کارشناس باید بتواند با استدلال مخالف باشد ولی شاهد را بپذیرد.

## ۱۶.۲ فهرست کامل

| # | کد | شدت | شرط | مبلغ در معرض | مسئول | تعداد |
|---|---|---|---|---|---|---|
| ۱ | `overdue_exceeds_gp` | **بحرانی** | معوق > سود ناخالص > ۰ | معوق | وصول | ۲۵۳ |
| ۲ | `quality_linked_decline` | **بحرانی** | افت >۲۵٪ حجم پس از نخستین شکایت، پنجرهٔ کامل | ارزش نجات | کیفیت | ۵۸ |
| ۳ | `overdue_aged` | زیاد | قدیمی‌ترین معوق > ۳۶۵ روز | معوق | کمیتهٔ اعتباری | ۳۵۸ |
| ۴ | `bounced_cheques` | زیاد | ≥۱ چک برگشتی | معوق | کمیتهٔ اعتباری | ۶۴ |
| ۵ | `over_credit_limit` | زیاد | مصرف سقف اعتبار > ۱۰۰٪ | معوق − سقف اعتبار | کمیتهٔ اعتباری | ۹۲ |
| ۶ | `dormant` | زیاد اگر رتبه ≤۱۵۰، وگرنه متوسط | رکود > ۱۸۰ روز | ارزش نجات | فروش | ۳۸۱ |
| ۷ | `volume_collapse` | زیاد | روند حجم < −۵۰٪ | ارزش نجات | فروش | ۲۳۲ |
| ۸ | `open_severe_complaint` | زیاد | ≥۱ شکایت باز با شدت زیاد/بحرانی | ۰ | کیفیت | ۴۰ |
| ۹ | `thin_margin` | زیاد | حاشیه < ۳٪ | قدرمطلق سود از‌دست‌رفتهٔ خطوط زیان‌ده | قیمت‌گذاری | ۹۷ |
| ۱۰ | `low_collection_rate` | متوسط | نرخ وصول < ۸۰٪ | معوق | وصول | ۱۱۱ |
| ۱۱ | `many_negative_lines` | متوسط | سهم خطوط زیان‌ده > ۳۰٪ (و حاشیه ≥۳٪) | قدرمطلق سود از‌دست‌رفته | قیمت‌گذاری | ۳۹ |
| ۱۲ | `competitor_dominant` | متوسط | سهم ما < ۲۵٪ و رقیب گزارش‌شده | ۰ | فروش | ۲۸۵ |
| ۱۳ | `uncontacted_active` | متوسط | آخرین تماس > ۱۸۰ روز و رکود < ۱۲۰ | ۰ | فروش | ۵۱ |
| ۱۴ | `no_crm_history` | متوسط | ۰ تعامل و فروش > ۰ | ۰ | فروش | **۰** |
| ۱۵ | `single_family_dependency` | کم | سهم یک گروه کالا > ۹۰٪ | ۰ | فروش | ۴۲۶ |

جمع: **۲٬۴۸۷ ریسک** در ۶۴۴ مشتری. مرتب‌سازی: شدت، سپس مبلغ در معرض نزولی.

## ۱۶.۳ چند قاعده که پشت‌شان یک تصمیم تحلیلی است

**`overdue_exceeds_gp` چرا بحرانی است.** مقایسهٔ معوق با **سود ناخالص** است نه با
فروش. اگر مشتری ۱۰۰ فروش و ۱۰ سود دارد و ۱۵ معوق، آن مشتری در عمل **زیان‌ده** است.
مقایسه با فروش این را پنهان می‌کند. در سبد: معوق ۵۵۷ میلیون در برابر سود ناخالص ۴۴۳
میلیون — یعنی نسبت **۱٫۲۶ برابر** در سطح کل سبد.

**`bounced_cheques` استدلالش نتیجهٔ منفی آزمون را حمل می‌کند:**

> چک برگشتی نشانهٔ تنش نقدی است. توجه: در آزمون دادهٔ همین سبد، چک برگشتی تأخیر بیشتر
> در آینده را پیش‌بینی نکرد — پس آن را نشانهٔ وضعیت بدانید، نه پیش‌بین.

ریسک را نگه داشتیم (چون تصمیم اعتباری لازم دارد) ولی ادعای پیش‌بینی را حذف کردیم.

**`open_severe_complaint` اقدامش از شدت شکایت می‌آید.** `COMPLAINT_SEVERITY_PLAY`:
بحرانی → مالک مشخص و مهلت ۴۸ ساعت؛ زیاد → مهلت یک‌هفته‌ای و گزارش اقدام اصلاحی؛
متوسط → جلسهٔ هفتگی کیفیت؛ کم → پاسخ کتبی و رصد تکرار در همان کد کالا. یک ریسک، چهار
اقدام متفاوت.

**`dormant` شدتش به اهمیت مشتری وابسته است.** رکود ۲۰۰ روزه برای مشتری رتبهٔ ۱۲ و
رتبهٔ ۶۰۰ یک چیز نیست. شرط `revenue_rank ≤ 150` شدت را از متوسط به زیاد می‌برد.

**چهار ریسک `value_at_stake = 0` دارند.** `competitor_dominant`،
`uncontacted_active`، `single_family_dependency`، `open_severe_complaint`. عمدی است:
برای این‌ها مبلغ قابل دفاعی نمی‌توان ساخت. صفر گذاشتن بهتر از عدد ساختن است — و
فرمول اولویت با کف ۰٫۴۵ (بخش ۱۸) تضمین می‌کند که این ریسک‌ها به‌خاطر صفر بودن مبلغ
کاملاً حذف نشوند.

---

# ۱۷. فرصت‌ها — ۱۰ قاعده و نگاشت دلیل آفر

## ۱۷.۱ فهرست کامل

| # | کد | پتانسیل | شرط | مبلغ فرصت | مسئول | تعداد |
|---|---|---|---|---|---|---|
| ۱ | `cross_sell` | متوسط | ≥۱ گروه کالای فروخته‌نشده | سود ماهانه × ۶ | فروش | ۶۳۱ |
| ۲ | `wallet_share_gap` | زیاد | سهم ما < میانهٔ بخش و خرید برآوردی موجود | `کیلوگرم شکاف × قیمت × حاشیه × ۱۲` | فروش | ۴۰۹ |
| ۳ | `pending_dev_request` | متوسط | ≥۱ درخواست در بررسی/توسعه | ۰ | از نوع درخواست | ۱۵۰ |
| ۴ | `approved_sample_idle` | زیاد | ≥۱ نمونهٔ تأییدشده | سود ماهانه × ۳ | تحقیق‌وتوسعه | ۸۱ |
| ۵ | `offer_*` (۷ نوع) | زیاد اگر منقضی، وگرنه متوسط | آفر باز، حداکثر ۳ | ۰ | از دلیل آفر | ۲۹۳ |
| ۶ | `recovered_trust` | کم | شکایت داشته، همه بسته، روند حجم > −۱۰٪ | ۰ | فروش | ۳۵ |
| ۷ | `growing` | زیاد | روند حجم > +۲۵٪ | سود ماهانه × ۶ | برنامه‌ریزی تولید | ۳۴ |
| ۸ | `offer_responsive` | متوسط | نرخ پذیرش ≥۵۰٪ و ≥۵ آفر | ۰ | فروش | ۲۳ |
| ۹ | `win_back` | زیاد | رکود > ۱۸۰ روز و رتبهٔ فروش ≤۱۰۰ | ارزش نجات | فروش | ۲۰ |
| ۱۰ | `repricing_upside` | زیاد | ۰ < حاشیه < ۸٪ و فروش > ۵۰ میلیون | `فروش × (۸ − حاشیه)/۱۰۰` | قیمت‌گذاری | ۲ |

جمع: **۱٬۶۷۸ فرصت**. وزن فوریت فرصت‌ها **× ۰٫۸۵** می‌شود (بخش ۱۸) — یعنی در برابری
شرایط، کاهش ریسک بر بهره‌گیری از فرصت مقدم است.

## ۱۷.۲ فرمول مبلغ شکاف سهم از سبد

بزرگ‌ترین عدد فرصت‌ها از این قاعده می‌آید، پس فرمولش صریح:

```
کیلوگرم ماهانهٔ شکاف = خرید کل برآوردی × (میانهٔ بخش − سهم ما) / ۱۰۰
قیمت واحد            = فروش اسمی / حجم        ← قیمت مؤثر واقعی همین مشتری
مبلغ سالانه          = کیلوگرم ماهانه × قیمت واحد × حاشیهٔ فعلی × ۱۲
```

و در متن `logic` **صریحاً سقف اعلام می‌شود**:

> سهم از سبد برآورد کارشناس است، نه اندازه‌گیری — پس عدد را سقف بدانید.

این جمله عمدی است. عدد از `Estimated_Total_Purchase` می‌آید که برآورد انسانی است؛
منبع برآورد هم در رفرنس ذکر می‌شود (`estimate_sources`).

## ۱۷.۳ نگاشت دلیل آفر → اقدام (الزام ۱ کاربر)

هفت دلیل آفر در داده وجود دارد. برای هرکدام `OFFER_REASON_PLAY` چهار چیز تعریف
می‌کند: **اقدام**، **مسئول**، **استدلال**، **گام فوری**.

| دلیل | اقدام | مسئول | گام فوری |
|---|---|---|---|
| **تسویه سریع** | پیگیری وصول همراه با یادآوری شرط تخفیف: تخفیف تنها با تسویه در مهلت اعتبار | وصول | تماس وصول در ۴۸ ساعت و ثبت تعهد پرداخت مکتوب |
| **افزایش حجم سفارش** | ارائهٔ پلهٔ حجمی مشخص: قیمت در برابر تعهد تناژ، با سقف زمانی | فروش | تعیین پله‌های تناژ و ارسال پیشنهاد کتبی |
| **افزایش سهم از سبد** | مقایسهٔ فنی و قیمتی با رقیب اصلی همان مشتری و پیشنهاد جایگزینی تدریجی | فروش | استخراج شکاف سهم از داشبورد و تدوین پیشنهاد جایگزینی |
| **حفظ مشتری کلیدی** | بازدید حضوری مدیر فروش پیش از انقضای آفر، با محور ریشهٔ نارضایتی | فروش | تعیین وقت بازدید و آماده کردن سابقهٔ شکایت و کیفیت پیش از جلسه |
| **رقابت قیمتی** | تحلیل بهای تمام‌شده پیش از پاسخ قیمتی؛ تعیین کف قیمت قابل قبول | قیمت‌گذاری | محاسبهٔ حاشیه در سطح کد کالا و تعیین کف قیمت |
| **معرفی محصول جدید** | ارسال نمونه با معرفی فنی و تعیین بازهٔ آزمون در خط مشتری | تحقیق‌وتوسعه | ارسال نمونه و تعیین تاریخ بازخورد فنی |
| **آزمون محصول** | پیگیری نتیجهٔ آزمون و ثبت بازخورد فنی مکتوب مشتری | تحقیق‌وتوسعه | تماس فنی برای گرفتن نتیجهٔ آزمون و ثبت آن در CRM |

**نکتهٔ کلیدی معماری:** توجه کنید که مسئول از دلیل می‌آید. آفر «تسویه سریع» کار
**وصول** است نه فروش. همین نگاشت باعث می‌شود ۳۸۴ اقدام به کمیتهٔ اعتباری و ۲۷۹ به واحد
وصول برود، نه همه به فروش.

و چون آزمون نشان داد دلیل آفر پذیرش را پیش‌بینی نمی‌کند (بخش ۱۳.۵)، متن `logic` هر
آفر **نرخ پذیرش تاریخی را ذکر می‌کند و بلافاصله هشدار می‌دهد که تفاوت معنادار نیست**:

> نرخ پذیرش تاریخی این دلیل ۳۷٪ است. دامنهٔ نرخ پذیرش بین دلایل مختلف ۲۹.۵٪ تا ۳۷.۶٪
> است و آزمون کای‌دو … معنادار نیست.

**آفر منقضی، پتانسیل بالاتر می‌گیرد.** `expired = روزهای گذشته > روز اعتبار` →
شدت `high`. آفر منقضی‌شده هم فرصت از‌دست‌رفته است و هم اهرم مذاکره‌ای که باطل شده.

## ۱۷.۴ نگاشت نوع درخواست توسعه → اقدام

`DEV_TYPE_PLAY` — شش نوع درخواست، هر یک با اقدام فنی مشخص و واحد مسئول:

| نوع | مسئول | گام |
|---|---|---|
| بهبود استحکام | کنترل کیفیت | تعیین هدف عددی استحکام و آزمون در سه بچ متوالی |
| بهبود شید رنگ | کنترل کیفیت | بررسی پایداری شید بین بوبین‌ها و ارائهٔ گزارش کنترل کیفیت |
| کاهش پرز | کنترل کیفیت | بازبینی پارامتر روغن‌زنی و آزمون پرز در نمونهٔ جدید |
| تغییر دنیر | برنامه‌ریزی تولید | سنجش امکان تولید در برنامهٔ خط و اعلام حداقل تناژ اقتصادی |
| تغییر تعداد فیلامنت | برنامه‌ریزی تولید | سنجش امکان فنی خط و اعلام زمان‌بندی نمونه |
| بسته‌بندی اختصاصی | برنامه‌ریزی تولید | برآورد هزینهٔ بسته‌بندی و تعیین حداقل سفارش |

قاعده روی **پرتکرارترین نوع** درخواست‌های باز اجرا می‌شود، و **واحد مسئول اعلام‌شده
در خود داده** (`Owner_Unit`) هم در متن اقدام ذکر می‌شود تا اگر با نگاشت ما اختلاف
داشت، کارشناس ببیند.

---

# ۱۸. اقدام بعدی و فرمول اولویت

## ۱۸.۱ مسئله

هر مشتری تا ۱۵ ریسک و ۱۰ فرصت دارد. کارشناس فروش نمی‌تواند ۲۵ کار انجام دهد. باید
**۴ کار** بیرون بیاید، مرتب، با مسئول مشخص.

## ۱۸.۲ فرمول

`next_best_actions()` ریسک‌ها و فرصت‌ها را در یک استخر می‌ریزد و امتیاز می‌دهد:

```
فوریت    = URGENCY[شدت]                 ← بحرانی ۱٫۰ | زیاد ۰٫۷ | متوسط ۰٫۴ | کم ۰٫۲
           فرصت‌ها × ۰٫۸۵
سهم مبلغ = مبلغ این مورد / بیشترین مبلغ همین مشتری     ← بازهٔ ۰ تا ۱
امتیاز   = فوریت × (۰٫۴۵ + ۰٫۵۵ × سهم مبلغ)
```

## ۱۸.۳ چرا این شکل

**چرا کف ۰٫۴۵؟** اگر امتیاز فقط `فوریت × سهم مبلغ` بود، هر موردی که مبلغ صفر دارد
امتیاز صفر می‌گرفت و هرگز دیده نمی‌شد. چهار ریسک و پنج فرصت عمداً مبلغ صفر دارند
(بخش ۱۶.۲). کف ۰٫۴۵ می‌گوید: **۴۵٪ اولویت از شدت می‌آید و ۵۵٪ از مبلغ.** یک شکایت
باز بحرانی بدون مبلغ، امتیاز `۱٫۰ × ۰٫۴۵ = ۰٫۴۵` می‌گیرد — بالاتر از یک فرصت متوسط
با مبلغ حداکثری (`۰٫۴ × ۰٫۸۵ × ۱٫۰ = ۰٫۳۴`).

**چرا سهم نسبی و نه مبلغ مطلق؟** مبلغ‌ها بین مشتری‌ها چند مرتبهٔ بزرگی فرق دارند.
اگر مبلغ مطلق را در فرمول می‌گذاشتیم، اولویت‌بندی **داخل** یک مشتری بی‌معنا می‌شد.
نرمال‌سازی روی حداکثر همان مشتری، فرمول را «چه کاری برای این مشتری مهم‌تر است»
می‌کند. مرتب‌سازی بین‌مشتری کار جدول سبد و ارزش نجات است، نه این فرمول.

**چرا ضرب ۰٫۸۵ برای فرصت‌ها؟** در تساوی شدت، پول در معرض از‌دست‌رفتن مقدم بر پول
احتمالی است. عدد کوچک نگه داشته شد تا فرصت‌های زیاد بر ریسک‌های متوسط بچربند
(`۰٫۷ × ۰٫۸۵ = ۰٫۵۹۵ > ۰٫۴`).

## ۱۸.۴ تنوع مسئول

مرتب‌سازی خام می‌تواند ۴ کار به یک واحد بدهد. قاعدهٔ تنوع:

```python
if x["owner"] in seen and len(out) >= 2:
    continue          # از سومین اقدام به بعد، مسئول تکراری رد می‌شود
```

دو اقدام اول را رها می‌گذارد (اگر واقعاً وصول دو کار اضطراری دارد، هر دو می‌آیند)
ولی از اقدام سوم تنوع را اجبار می‌کند. نتیجه در سبد: **۱٬۸۷۵ اقدام** با پخش
واقعی زیر —

| واحد مسئول | تعداد اقدام |
|---|---|
| واحد فروش | ۸۱۵ |
| کمیتهٔ اعتباری | ۳۸۴ |
| واحد وصول مطالبات | ۲۷۹ |
| کمیتهٔ قیمت‌گذاری | ۱۴۹ |
| کنترل کیفیت | ۱۰۷ |
| تحقیق‌وتوسعه | ۸۱ |
| برنامه‌ریزی تولید | ۶۰ |

ترکیب: ۱٬۲۸۰ کاهش ریسک و ۵۹۵ بهره‌گیری از فرصت.

## ۱۸.۵ فرمول در UI به‌صورت نسبت نوشته می‌شود

```python
x["score_formula"] = (f"فوریت {x['weight']:.2f} × (۰٫۴۵ + ۰٫۵۵ × سهم مبلغ "
                      f"{share:.2f}) = {x['score']:.3f}")
```

عدد بزرگ (مثل ۴۲٬۸۳۱٬۵۰۰) در فرمول نوشته نمی‌شود، چون در متن راست‌به‌چپ الگوریتم
دوجهته اعداد بزرگ را جابه‌جا می‌کند. نسبت `۰٫۷۳` این مشکل را ندارد. مبلغ خام در
`score_components` جدا نگه داشته می‌شود و در UI داخل `<span class="ltr">` نمایش
داده می‌شود.

---

# ۱۹. سامانهٔ رفرنس‌دهی

## ۱۹.۱ اصل

هر ادعا — سیگنال، پیش‌بینی، ریسک، فرصت، اقدام — فهرست رفرنس دارد. تابع `ref()`:

```python
def ref(sheet, record_id=None, date=None, fields=None, note=""):
    return {"sheet": SHEET_FA.get(sheet, sheet),   # نام فارسی شیت
            "sheet_key": sheet,                     # کلید انگلیسی برای فیلتر
            "record_id": record_id,                 # شناسهٔ رکورد منبع
            "date": date, "date_fa": jalali.fmt(date),
            "fields": [{"name": k, "name_fa": FIELD_FA.get(k, k), "value": str(v)}
                       for k, v in (fields or {}).items()],
            "note": note}                           # توضیح مبنا وقتی رکورد کافی نیست
```

پنج چیز پاسخ داده می‌شود: **از کدام شیت؟ کدام رکورد؟ چه تاریخی؟ کدام فیلد؟ چه
مقداری؟** و در صورت لزوم، **بر چه مبنایی**.

## ۱۹.۲ مسئلهٔ ادعاهای بدون رکورد و راه‌حل `with_fallback`

سه دسته ادعا وجود دارد که رکورد منفرد ندارند:

۱. **شمارش تجمیعی** — «۵ تعامل قیمت» وقتی آن ۵ تعامل قدیمی‌تر از ۱۰ تعامل اخیرِ
   نگه‌داشته‌شده در پروفایل هستند.
۲. **نرخ پایهٔ تجربی** — «۶٫۵٪ احتمال شکایت» برای مشتری‌ای که **هیچ** شکایتی ندارد.
   منبع ادعا، ۱٬۰۰۱ مشاهدهٔ سبد است نه رکورد این مشتری.
۳. **مقدار محاسبه‌شده** — حاشیه، روند حجم، امتیاز ماندگاری؛ ساختهٔ ما هستند.

پاسخ:

```python
def with_fallback(refs: list[dict], fallback: dict) -> list[dict]:
    """هیچ ادعایی بدون رفرنس نمی‌ماند."""
    return refs if refs else [fallback]
```

و `fallback` **صریح می‌گوید منبع چیست**، مثلاً:

> این مشتری شکایتی ثبت نکرده؛ عدد کاملاً نرخ پایهٔ تجربی گروه «بدون شکایت پیشین» است
> از ۱٬۰۰۱ مشاهدهٔ مشتری-فصل

این از «رفرنس خالی» بهتر است و از «رفرنس جعلی» بی‌نهایت بهتر. شیت مجازی
`derived`/«محاسبه‌شده» برای همین ساخته شد.

## ۱۹.۳ آمار واقعی

| سنجه | مقدار |
|---|---|
| رفرنس در سیگنال + پیش‌بینی + ریسک + فرصت | **۱۶٬۷۹۱** |
| با اقدام‌های بعدی (که رفرنس منبع را ارث می‌برند) | ۱۹٬۹۹۷ |
| میانگین رفرنس به‌ازای هر مشتری | ۲۶٫۱ |
| ادعای بدون رفرنس | **۰** (آزمون‌شده در `verify.py`) |

پخش رفرنس‌ها روی شیت‌ها:

| شیت | تعداد رفرنس |
|---|---|
| فاکتورها | ۵٬۲۰۹ |
| محاسبه‌شده | ۴٬۴۳۹ |
| تعاملات CRM | ۳٬۸۲۸ |
| فروش | ۱٬۷۵۷ |
| سهم سبد | ۱٬۴۱۷ |
| درخواست توسعه | ۱٬۰۹۱ |
| شکایات | ۸۹۲ |
| وصول | ۷۶۹ |
| آفرها | ۴۳۴ |
| مشتریان | ۱۰۱ |
| کیفیت لات | ۵۲ |
| سیگنال بازار | ۸ |

نکته: «محاسبه‌شده» دومین منبع بزرگ است (۲۲٪). این یعنی **یک‌پنجم ادعاها به مقدار
ساختهٔ ما تکیه دارند** — و همین است که آن‌ها را با برچسب متفاوت نشان می‌دهیم. پنهان
کردنش ساده بود؛ نشان دادنش درست است.

## ۱۹.۴ نمایش در UI

هر ادعا در داشبورد دکمهٔ «منبع» دارد. کلیک روی آن رفرنس‌ها را باز می‌کند: نام شیت با
رنگ متمایز، شناسهٔ رکورد، تاریخ شمسی، و جفت‌های فیلد/مقدار. اگر `note` وجود داشته
باشد، در پایین با فونت کوچک‌تر می‌آید. رفرنس‌های `derived` نشان‌گر بصری متفاوت دارند
تا با رکورد واقعی اشتباه نشوند.

---

# ۲۰. الگوهای سنجیده: دو تأیید، سه رد

## ۲۰.۱ روش

`VALIDATED_PATTERNS` در `features.py` نگه داشته می‌شود و در داشبورد نمایش داده
می‌شود — **هم تأییدها هم ردها**. روش سنجش هر الگو:

۱. دو پنجرهٔ زمانی مستقل (آموزش: ۱۳۹۹/۱۰/۱۰؛ آزمون: ۱۴۰۰/۰۴/۰۹).
۲. ویژگی در تاریخ برش، نتیجه در آیندهٔ همان مشتری.
۳. **کنترل عمق رابطه** — همیشه. تعداد ماه‌های فعال، متغیر مخدوش‌کنندهٔ اصلی است.
۴. الگو فقط وقتی «تأیید» می‌شود که در **هر دو** پنجره و **پس از کنترل** زنده بماند.

## ۲۰.۲ الگوی تأییدشدهٔ یکم — عرض سبد کالا

**ادعا:** مشتری‌ای که از ۲ گروه کالا یا بیشتر می‌خرد، به‌طور معنادار بیشتر برمی‌گردد.

| پنجره | ۱ گروه | ۲+ گروه | تفاوت |
|---|---|---|---|
| آموزش | نرخ بازگشت پایه | +حدود ۲۰ واحد درصد | تأیید |
| آزمون | نرخ بازگشت پایه | +حدود ۲۰ واحد درصد | تأیید |

پس از کنترل عمق رابطه، اثر **زنده ماند**. این تنها الگویی است که به مدل پیش‌بینی راه
پیدا کرد (`+۰٫۰۸` در `reorder_90d`) و به فرصت `cross_sell` مبنا داد.

**چرا باور کردنی است:** مشتری‌ای که دو گروه کالا از ما می‌خرد، دو نقطهٔ اتصال فنی و
تجاری دارد. جایگزین کردن ما با یک رقیب برایش دو برابر پرهزینه‌تر است. مکانیزم علّی
قابل روایت است، نه فقط همبستگی.

## ۲۰.۳ الگوی تأییدشدهٔ دوم — رکود

**ادعا:** رکود قوی‌ترین پیش‌بین بازگشت است. جدول بخش ۱۳.۲ با AUC ۰٫۸۵۲ در پنجرهٔ
آزمون. یکنوا در هر ۷ باکت، در هر دو پنجره.

## ۲۰.۴ الگوی ردشدهٔ یکم — شکایت و ریزش

**ادعای اولیه:** «مشتری‌ای که شکایت ثبت کرده، بیشتر می‌رود.»

**در نگاه اول درست بود.** ولی بررسی نشان داد مشتری‌های شاکی، مشتری‌های **قدیمی‌تر و
بزرگ‌تر**اند — کسی که ۳۰ ماه خریده، فرصت بیشتری برای شکایت داشته و پایدارتر هم هست.
پس از کنترل تعداد ماه‌های فعال، **اثر شکایت بر ماندگاری از بین رفت**.

**تصمیم:** شکایت در مدل ماندگاری **استفاده نشد**. به‌جایش، شکایت در جای درستش
ماند — پیش‌بینی شکایت آینده (که نرخ پایهٔ قوی ۶٫۵٪→۷۱٪ دارد) و ریسک کیفی.

سیگنال `quality_purchase_link` سطح مشتری هنوز نمایش داده می‌شود ولی متنش می‌گوید:
«در سطح کل سبد، شکایت پیش‌بین ریزش نیست (آزمون‌شده و رد شده)».

## ۲۰.۵ الگوی ردشدهٔ دوم — درخواست توسعه و ماندگاری

**ادعای اولیه:** «مشتری‌ای که درخواست توسعه می‌دهد، درگیرتر است و بیشتر می‌ماند.»

**همان مخدوش‌کننده.** درخواست توسعه هم با عمق رابطه همبسته است. پس از کنترل، اثر
مستقل باقی نماند.

**تصمیم:** درخواست توسعه از مدل ماندگاری حذف شد و به‌عنوان **فرصت فروش** بازتعریف شد.
متن قاعدهٔ `pending_dev_request` این را صریح می‌گوید:

> در آزمون داده، درخواست توسعه پیش‌بین مستقل ماندگاری نبود — پس آن را فرصت فروش
> بدانید، نه شاخص ریسک.

## ۲۰.۶ الگوی ردشدهٔ سوم — چک برگشتی و تأخیر آینده

چک برگشتی، تأخیر بیشتر در پرداخت‌های آینده را پیش‌بینی نکرد (دامنهٔ کل ۳۹٫۷٪ تا
۵۷٫۹٪ حول پایهٔ ۵۱٪). ریسک نگه داشته شد چون تصمیم اعتباری لازم دارد، ولی وزن
پیش‌بینی گرفته شد و اطمینان `late_payment` روی «پایین» تنظیم شد.

## ۲۰.۷ چرا ردها را نمایش می‌دهیم

سه دلیل:

۱. **صداقت فنی.** اگر فقط تأییدها را نشان دهیم، داوری هوشمند می‌پرسد «چه چیزی را
   آزمودید و کار نکرد؟» و پاسخ نداشتن، بی‌اعتبار می‌کند.
۲. **جلوگیری از اقدام غلط.** اگر کارشناس فروش باور کند «شکایت یعنی ریزش»، منابع را
   به مشتری اشتباه می‌دهد.
۳. **نشان دادن روش.** سه الگوی رد، نشانهٔ اجرای درست کنترل مخدوش‌کننده است. الگوریتمی
   که هیچ‌وقت هیچ فرضیه‌ای را رد نمی‌کند، احتمالاً آزمون نمی‌کند.

---

# ۲۱. داشبورد — تب به تب، پنل به پنل

## ۲۱.۰ اصول طراحی رابط

| اصل | اجرا |
|---|---|
| راست‌به‌چپ کامل | `dir="rtl"` روی `<html>`، همهٔ فاصله‌ها با `margin-inline-*` و `padding-inline-*` |
| دستیار همه‌جا حاضر | تب دستیار حذف شد؛ دکمهٔ شناور در هر شش تب، با پنل شناور نه تمام‌صفحه (بخش ۳۱) |
| هیچ کتابخانهٔ نمودار | ۸ نوع نمودار با SVG دستی در `charts.js` (۳۳۶ خط) — دمو نباید به CDN وابسته باشد |
| تک‌فایل | یک `index.html` + دو اسکریپت کوچک؛ بدون build step، بدون npm |
| فشرده به‌جای پرگو | متن‌های طولانی به نمودار تبدیل شدند؛ جزئیات در بخش‌های بازشدنی (`.det`) پنهان است |
| تاریخ شمسی | همه‌جا؛ تبدیل در `jalali.py` و `jshort`/`jtxt` در سمت کلاینت |
| دو تم | روشن و تیره، با پالت متفاوت برای هر یک |

**سرصفحه** سه نشان دارد: تعداد کل رفرنس‌ها، تاریخ برش داده (۹ تیر ۱۴۰۱)، و **حالت
دستیار** (`gemini` یا `deterministic`) — یعنی کاربر همیشه می‌داند پاسخ‌ها از مدل
زبانی می‌آید یا از موتور قاعده‌محور.

## ۲۱.۰-الف شش تب

ترتیب تب‌ها: **کارتابل** (پیش‌فرض) · خلاصه و تمرکز · نمای کل سبد · پروفایل مشتری ·
بخش‌بندی و ارزش · الگوها، مدل و اعتبارسنجی · جست‌وجوی متن. تب خلاصه و تب فهرست
تمرکز در یک صفحه ادغام شده‌اند (بخش ۳۵.۵). دستیار دیگر تب نیست — دکمهٔ شناور است و در همهٔ تب‌ها
در دسترس (بخش ۳۱.۲). تب خلاصه در بخش ۳۱.۱ جداگانه شرح داده شده است.

## ۲۱.۱ تب دوم — نمای کل سبد

### شش کارت KPI

| کارت | مقدار اصلی | زیرنویس | چرا این زیرنویس |
|---|---|---|---|
| فروش اسمی | ۴٫۳۹ میلیارد | حقیقی: ۱٫۷۶ میلیارد | تفاوت ۲٫۵ برابری، درجا نشان می‌دهد تورم چه کرده |
| حجم فروش | تن | ۶۲۴ مشتری با سابقهٔ خرید | تفاوت ۶۴۴ و ۶۲۴ را صریح می‌کند |
| سود ناخالص | ۴۴۳ میلیون | حاشیه ۱۰٫۰۹٪ · ۳۲٪ مبنای تحقق‌یافته | حاشیه بدون سهم مبنای واقعی، گمراه‌کننده است |
| مطالبات معوق | ۵۵۷ میلیون | ۱۲٫۷٪ فروش · **۱٫۲۶ برابر سود** | نسبت به سود، نه به فروش |
| ارزش طول عمر سبد | ۲۴۲ میلیون | محقق‌شده ‎−۱۱۴ میلیون + آینده ۳۵۶ میلیون | علامت مخالف دو جزء، مهم‌ترین خبر سبد است |
| مشتری راکد | ۴۰۱ نفر | ۲۰۶ مشتری LTV منفی · تمرکز ۱۰ اول | سه عدد ریسک تمرکز در یک کارت |

**توضیح یک ناهمخوانی ظاهری:** کارت «مشتری راکد» عدد ۴۰۱ را نشان می‌دهد ولی ریسک
`dormant` روی ۳۸۱ مشتری فعال می‌شود. تفاوت دقیقاً همان **۲۰ مشتری بدون هیچ فروش**
است: در آمار سبد، رکود آن‌ها `9999` روز حساب می‌شود (`days_since_last_purchase or 9999`)
پس راکد شمرده می‌شوند؛ ولی قاعدهٔ ریسک شرط `فروش > ۰` دارد، چون «راکد» برای مشتری‌ای
که هرگز نخریده بی‌معناست. هر دو عدد درست‌اند و معنای متفاوتی دارند.

### کارت «رشد فروش واقعی نیست — تورم است»

مقایسهٔ **بهار ۱۳۹۹ با بهار ۱۴۰۱** (`half_years`) با چهار شاخص: فروش اسمی، حجم،
قیمت واحد، حاشیه. متن زیرش خودکار تولید می‌شود. این کارت اول صفحه است چون **بزرگ‌ترین
تلهٔ این داده** را خنثی می‌کند: هر تحلیلی که روی فروش اسمی بنا شود، غلط است.

### دو نمودار خطی

۱. **شاخص فروش اسمی در برابر شاخص حجم** — دو خط که از ۱۰۰ شروع می‌شوند و واگرا
   می‌شوند. تصویری‌ترین اثبات مسئلهٔ تورم.
۲. **حاشیه سود ناخالص ماهانه** — روند حاشیه در ۳۱ ماه.

هر دو ماه‌های ناقص مرزی را **حذف می‌کنند** (`filter(r => !r.is_partial)`).

### چهارخانهٔ حاشیه در برابر ریسک + وزن هر خانه

نمودار پراکندگی ۶۲۱ مشتری. چهار بُعد در یک تصویر:

| بُعد | نگاشت |
|---|---|
| افقی | احتمال ماندگاری (۰ تا ۱)، خط‌چین در ۰٫۵ |
| عمودی | حاشیه سود ٪، خط‌چین در میانهٔ سبد ۸٫۹۱٪ |
| اندازهٔ نقطه | سود ناخالص |
| رنگ نقطه | LTV، روی رمپ آبی چهارپله |

کلیک روی نقطه → تب مشتری با همان مشتری انتخاب‌شده. کنارش نمودار میلهٔ افقی «وزن هر
خانه» با تعداد مشتری، سود و LTV هر خانه؛ کلیک روی میله جدول را پالایش می‌کند.

**نکتهٔ راست‌به‌چپ:** چون محور افقی راست‌به‌چپ است، ماندگاری بالا **سمت چپ** می‌نشیند.
بنابراین «رشد بده» بالا-چپ و «نجات فوری» بالا-راست است. این در نسخهٔ اول برعکس بود و
اصلاح شد (بخش ۲۸).

### دو نمودار میلهٔ افقی — ریسک و فرصت سبد

۱۴ ریسک و ۱۶ کد فرصت با تعداد مشتری. زیرنویس هر میله مبلغ مرتبط را نشان می‌دهد
(معوق برای ریسک، فروش برای فرصت). **کلیک روی هر میله → تب مشتری با فیلتر همان کد.**
این پیوند، داشبورد را از «گزارش» به «ابزار» تبدیل می‌کند.

### کارت هشدارهای داده

فهرست شماره‌دار `data_caveats` از `/api/meta`. تولید در `_caveats(s)` در `app.py` و
اعداد از کش زنده می‌آید. این کارت **آخر** صفحه است ولی حذف نشدنی: پنج تلهٔ داده،
سهم مبنای هزینهٔ برآوردی، فیلد نشتی، و محدودیت برآورد سهم از سبد.

## ۲۱.۲ تب سوم — پروفایل مشتری

### نوار فیلتر — ۸ کنترل

جست‌وجوی شناسه · مرتب‌سازی · بخش (A/B/C) · چهارخانه · بخش RFM · **کد ریسک** ·
**کد فرصت** · **کد سیگنال**. سه فیلتر آخر مستقیماً روی ستون‌های `risk_codes`،
`opp_codes`، `signal_codes` در فریم کار می‌کنند. یعنی «همهٔ مشتریانی که چک برگشتی
دارند و در نجات فوری‌اند» یک انتخاب دو کلیکی است.

### جدول — ۱۵ ستون

| ستون | نکته |
|---|---|
| شناسه، بخش | هویت |
| **LTV** | منفی با رنگ قرمز |
| فروش، سود ناخالص، حاشیه | تجاری |
| **ماندگاری** | <۴۰٪ قرمز، >۷۰٪ سبز |
| چهارخانه، بخش RFM | برچسب رنگی |
| معوق | اعتباری |
| **ارزش نجات** | فهرست عملیاتی هفته |
| روز از خرید، احتمال قطع | ریزش |
| **سیگنال اصلی**، **اقدام بعدی** | خلاصهٔ عملیاتی در همان سطر |

مرتب‌سازی با کلیک روی سرستون. کلیک روی سطر → پروفایل کامل زیر جدول ساخته می‌شود.

### سرصفحهٔ پروفایل

شناسه، برچسب چهارخانه، برچسب RFM، رتبهٔ LTV، و **خلاصهٔ وضعیت** — یک پاراگراف
تولیدشده در `summarise()` که هشت چیز را در چند جمله می‌گوید: رتبه، فروش، حجم، ماه
فعال، RFM، چهارخانه، حاشیه در برابر میانه، LTV با تجزیه، معوق، محتمل‌ترین تصمیم
بعدی، مهم‌ترین ریسک، مهم‌ترین فرصت، و اقدام بعدی با مسئول.

### چهار پنل — پاسخ مستقیم به الزام ۷ کاربر

**پنل ۱ — بینش مشتری.** شش سطر کلید/مقدار (آغاز همکاری، بازهٔ خرید، رکود با فاصلهٔ
معمول سفارش، ماه فعال و فاکتور، گروه کالا و کد کالا، سهم از سبد با میانهٔ بخش) و
سپس **نمودار واگرا از اجزای امتیاز ماندگاری** — هر نشانه با سهم مثبت یا منفی‌اش و
توضیح در tooltip. پایینش یادداشت: امتیاز خام، پایهٔ ۰٫۵۰، و AUC ۰٫۸۳۱.

**پنل ۲ — سیگنال‌ها و تفسیر.** سیگنال‌ها **گروه‌بندی‌شده بر حوزه**. هر سطر: نام،
نوار شدت رنگی (قرمز/زرد/سبز بر پایهٔ جهت)، مقدار عددی. **کلیک باز می‌کند** →
تفسیر + بلوک رفرنس. فشرده در حالت بسته، کامل در حالت باز.

**پنل ۳ — اقدام بعدی و رفرنس.** حداکثر ۴ اقدام، هر یک با شمارهٔ رتبه، متن اقدام،
سه برچسب فرا‌داده (مسئول، نوع، مبلغ در معرض)، خط **«چرا: …»** با استدلال، خط
**امتیاز اولویت** با فرمول کامل، و بلوک رفرنس.

**پنل ۴ — کامرشال، ارزش به زبان پول.** نمودار انباشتهٔ تجزیهٔ LTV (سود محقق‌شده |
معوق کسرشده | آیندهٔ تنزیل‌شده) و ۹ سطر عددی: فروش اسمی/حقیقی، سود ناخالص با حاشیه،
سود ماهانه، معوق/سررسیدنشده، مشارکت خالص (رنگی بر علامت)، LTV کل، رتبهٔ فروش در
برابر رتبهٔ LTV با اختلاف پله، ارزش نجات، مجموع ارزش فرصت‌ها، زیان خطوط زیان‌ده.
پایینش فرمول کامل آیندهٔ تنزیل‌شده با اعداد همان مشتری.

### تصمیم‌های احتمالی دورهٔ بعد + حجم خرید ماهانه

شش پیش‌بینی، هر یک با نوار احتمال رنگی (≥۶۰٪ قرمز، ≥۳۵٪ زرد، وگرنه سبز)، برچسب
اطمینان و متن نرخ پایه. **کلیک باز می‌کند** → نمودار واگرای تعدیل‌ها + یادداشت
احتیاطی + رفرنس. کنارش نمودار میله‌ای حجم خرید ماهانه (ماه‌های ناقص حذف‌شده).

### ریسک‌ها و فرصت‌ها — دو کارت فشرده و بازشدنی

هر مورد در حالت بسته: برچسب شدت + عنوان + مبلغ. باز: **شاهد** / **استدلال** /
**اقدام با مسئول و مبلغ** / **رفرنس**. حاشیهٔ رنگی سمت شروع، شدت را نشان می‌دهد.

### محصولات، ترکیب سبد، شرایط پرداخت

سه کارت: جدول محصولات اصلی (حاشیهٔ منفی قرمز)، میلهٔ افقی ترکیب گروه کالا با
یادداشت **گروه‌های فروخته‌نشدهٔ هم‌بخشی‌ها** (فرصت فروش مکمل)، و میلهٔ افقی ترکیب
شرایط پرداخت با فهرست رقبای گزارش‌شده و تعداد ماه.

### تاریخچهٔ یکپارچه — قلب صورت‌مسئله

سه منبع در یک خط زمان: **شکایت‌ها + گزارش‌های کارشناس فروش + درخواست‌های توسعه**،
مرتب نزولی بر تاریخ، با رنگ متمایز هر نوع، تاریخ شمسی و متن کامل. بسته به‌صورت
پیش‌فرض با دکمهٔ «نمایش تاریخچهٔ یکپارچه (N رویداد)»؛ ۲۰ رویداد اخیر نشان داده
می‌شود و تعداد بقیه اعلام می‌شود.

**چرا این مهم‌ترین کارت پروژه است:** این دقیقاً همان چیزی است که امروز در سه سامانهٔ
جدا پراکنده است و هیچ گزارشی آن را کنار هم نمی‌گذارد.

### کیفیت آزمایشگاهی و اثر شکایت

پنج شاخص آزمایشگاه (استحکام، کشش، یکنواختی CV، جذب روغن، تعداد رد) و کارت مقایسهٔ
حجم ۹۰ روز پیش و پس از نخستین شکایت — با **هشدار صریح** که شاهد است نه اثبات علیت،
و اگر پنجره کامل نباشد، هشدار دوم.

### متن کامل پروفایل

همان متنی که به مدل زبانی داده می‌شود، با Markdown رندر شده. **شفافیت کامل:** کاربر
می‌تواند دقیقاً ببیند مدل چه چیزی می‌بیند.

## ۲۱.۳ تب چهارم — بخش‌بندی و ارزش

چهار KPI (LTV کل، آیندهٔ تنزیل‌شده با پارامترها، تعداد LTV منفی، ماندگاری میانه) و
چهار پنل:

۱. **نقشهٔ حرارتی RFM ۵×۵** — تازگی در برابر تکرار، تیرگی با تعداد مشتری. یادداشت
   صریح: بُعد پولی روی **فروش حقیقی تعدیل‌شده با تورم** است نه اسمی.
۲. **سود ناخالص و LTV هر بخش RFM** — میلهٔ افقی مرتب بر سود؛ بخش‌های با سود منفی
   قرمز. کلیک → پالایش جدول مشتریان.
۳. **جدول کامل بخش‌ها** با تفسیر هر بخش زیر نامش.
۴. **ترکیب ارزش طول عمر کل سبد** — نمودار انباشته با یادداشت پارامترها.

## ۲۱.۴ تب پنجم — الگوها و مدل

این تب برای پاسخ به پرسش «چطور می‌دانید؟» ساخته شده.

۱. **کارت الگوهای آزموده‌شده** — شامل ردشده‌ها. حاشیهٔ رنگی: سبز تأیید، قرمز رد،
   زرد ضعیف. متن روش در بالا: «هر فرضیه با کنترل مخدوش‌کننده … گزارش الگوهای ردشده
   هم بخشی از صداقت تحلیل است.»
۲. **کارت مدل** — پنجرهٔ آموزش و آزمون، AUC، خطای کالیبراسیون، و **اعتراف صریح** که
   رکود تنها بهتر از مدل ترکیبی است.
۳. **نرخ پایهٔ سفارش مجدد** — ۷ میله با تعداد مشاهده کنار هر میله.
۴. **نرخ پایهٔ شکایت و درخواست توسعه** — دو مجموعهٔ ۴ میله‌ای.
۵. **پذیرش آفر بر پایهٔ دلیل — سیگنالی ندارد.** عنوان کارت خودش نتیجه را می‌گوید.
   ۷ میلهٔ تقریباً هم‌ارتفاع + متن آزمون کای‌دو.

## ۲۱.۵ تب ششم — جست‌وجوی متن

جست‌وجو در **۴٬۵۶۵ رکورد متن آزاد**: **۳٬۶۵۲ گزارش کارشناس فروش در CRM + ۵۵۰ شرح
نیاز فنی + ۳۶۳ متن شکایت**. فیلتر نوع (شکایت / تعامل CRM / درخواست توسعه). نتایج با شناسهٔ مشتری قابل کلیک،
تاریخ شمسی، نوع، و متن کامل. نرمال‌سازی ZWNJ و «ی/ک» عربی در `norm()` تا جست‌وجوی
فارسی واقعاً کار کند.

## ۲۱.۶ دستیار — دیگر تب نیست

ناحیهٔ گفت‌وگو از تب بیرون آمد و به **پنل شناور** تبدیل شد که در هر شش تب با یک
دکمه باز می‌شود. جزئیات در بخش ۳۱.۲.

---

# ۲۲. نمودارها و قواعد طراحی

## ۲۲.۱ چرا SVG دستی

هیچ کتابخانهٔ نمودار استفاده نشده. `charts.js` در ۳۳۶ خط، ۸ نوع نمودار می‌سازد.

| دلیل | توضیح |
|---|---|
| **دمو نباید بمیرد** | وابستگی به CDN یعنی یک قطعی اینترنت در سالن ارائه، داشبورد را سفید می‌کند |
| راست‌به‌چپ | کتابخانه‌های نمودار RTL را نصفه‌نیمه پشتیبانی می‌کنند؛ کنترل کامل لازم بود |
| پالت | رنگ‌ها از متغیرهای CSS خوانده می‌شوند تا تم تیره/روشن یک منبع داشته باشد |
| اندازه | کل فایل کوچک‌تر از هر کتابخانهٔ نموداری است |

## ۲۲.۲ هشت نوع نمودار

| تابع | کاربرد در داشبورد |
|---|---|
| `line(host, {series, labels, …})` | شاخص فروش/حجم، حاشیهٔ ماهانه |
| `bar(host, {labels, data, …})` | حجم خرید ماهانهٔ مشتری، نرخ‌های پایه |
| `hbar(host, rows, {fmt, sub, onClick, max})` | ریسک/فرصت سبد، ترکیب گروه کالا، شرایط پرداخت، وزن خانه‌ها، بخش‌های RFM |
| `diverging(host, rows, {fmt})` | اجزای امتیاز ماندگاری، تعدیل‌های پیش‌بینی |
| `meters(host, rows)` | نوارهای درصدی |
| `stack(host, parts, {total, fmt})` | تجزیهٔ LTV مشتری و سبد |
| `scatter(host, pts, {xKey, yKey, xMid, yMid, quadLabels, sizeKey, colorKey, …})` | چهارخانهٔ حاشیه-ریسک |
| `heatmap(host, cells, {xKey, yKey, vKey, …})` | نقشهٔ RFM ۵×۵ |

هر تابع `onClick` می‌پذیرد؛ همین است که نمودارها به فیلتر جدول وصل می‌شوند.

## ۲۲.۳ پالت رنگ

از مهارت دادگان‌نگاری گرفته و برای دو تم جدا تعیین شد:

| نقش | تم روشن | تم تیره |
|---|---|---|
| سری یکم | `#2a78d6` | `#3987e5` |
| سری دوم | `#eb6834` | `#d95926` |
| بحرانی / منفی | قرمز وضعیتی | همان با روشنایی تنظیم‌شده |
| هشدار | زرد-نارنجی وضعیتی | همان |
| مثبت / خوب | سبز وضعیتی | همان |
| رمپ ترتیبی | چهار پلهٔ آبی `--seq-100` تا `--seq-600` | همان |

**قاعده:** هیچ رنگی در JS هاردکد نشده. همه با `cs('--series-1')` از CSS خوانده
می‌شوند تا تعویض تم، نمودارها را هم عوض کند.

## ۲۲.۴ تابع `niceTicks` — و اشکالی که در آن بود

```javascript
/* تیک‌ها باید همیشه بازهٔ داده را بپوشانند. */
function niceTicks(min, max, n = 4) {
  const span = (max - min) || Math.abs(max) || 1;
  const raw = span / n, mag = Math.pow(10, Math.floor(Math.log10(raw)));
  const step = [1, 2, 2.5, 5, 10].map(m => m * mag).find(s => s >= raw) || 10 * mag;
  const lo = Math.floor(min / step) * step;      // ← رو به پایین
  const hi = Math.ceil(max / step) * step;       // ← رو به بالا
  const out = [];
  for (let v = lo; v <= hi + step * 1e-6; v += step) out.push(+v.toFixed(10));
  return out;
}
```

`Math.floor` و `Math.ceil` **حیاتی‌اند**. نسخهٔ پیشین گرد می‌کرد و برای داده‌ای که
از ۰٫۰۲ تا ۰٫۹۷ پخش بود، تیک‌های ۰٫۲۵ تا ۰٫۷۵ می‌ساخت — یعنی نقاط بیرون از ناحیهٔ
ترسیم می‌افتادند. شرح کامل در بخش ۲۸.۷.

## ۲۲.۵ قواعد راست‌به‌چپ در SVG

سه قاعدهٔ کشف‌شده که در هیچ مستندی صریح نیست:

**۱. محور افقی معکوس است.** در `dir="rtl"`، مقدار کم سمت راست و مقدار زیاد سمت چپ
می‌نشیند. هر برچسب گوشه‌ای باید بر همین اساس نگاشت شود.

**۲. `text-anchor` معکوس عمل می‌کند.**

```javascript
// در متن راست‌به‌چپ، «end» لبهٔ چپ را لنگر می‌کند و «start» لبهٔ راست را
s += put(quadLabels.tr, x1 - 6, y0 + 13, 'start');
s += put(quadLabels.tl, x0 + 6, y0 + 13, 'end');
```

**۳. اعداد بزرگ باید در `unicode-bidi: isolate` محصور شوند.** الگوریتم دوجهته
عدد لاتین داخل متن فارسی را جابه‌جا می‌کند. کلاس `.ltr{direction:ltr;unicode-bidi:isolate}`
برای همین ساخته شد و روی همهٔ فرمول‌ها و اعداد بلند اعمال می‌شود.

## ۲۲.۶ رندر Markdown سبک — `md.js`

۱۲۶ خط، بدون وابستگی. پشتیبانی: تیتر `##`، تأکید `**`، فهرست `-`، جدول Markdown،
کد درون‌خطی. سه جا استفاده می‌شود: خلاصهٔ وضعیت، پاسخ دستیار، و متن کامل پروفایل.

**چرا لازم شد:** پاسخ‌های مدل و متن‌های تولیدی حاوی Markdown بودند که به‌صورت متن خام
نمایش داده می‌شد (الزام ۱۰ کاربر). `MD.esc()` هم برای فرار از HTML در همهٔ ورودی‌های
غیرمطمئن استفاده می‌شود.

---

# ۲۳. لایهٔ دستیار و ۱۳ ابزار

## ۲۳.۱ معماری

```
پرسش کاربر
   ↓
Copilot.ask()
   ├─ اگر کلید API هست → _ask_gemini()   ← function calling، حداکثر ۶ دور
   │     └─ در صورت خطا → _ask_deterministic()  + اعلام دلیل به کاربر
   └─ اگر نیست        → _ask_deterministic()   ← تطبیق قاعده‌محور
   ↓
هر دو مسیر: همان ۱۰ ابزار Store را صدا می‌زنند
   ↓
پاسخ + trace (فهرست ابزارها و پارامترها)
```

**مدل هیچ‌گاه به داده دسترسی مستقیم ندارد.** فقط اسم ابزار و پارامتر تولید می‌کند؛
عدد از پایتون می‌آید. این ساختاری است، نه پرامپتی.

## ۲۳.۲ سیزده ابزار

| ابزار | پارامترها | چه می‌دهد |
|---|---|---|
| `portfolio_summary` | — | آمار کل سبد: فروش، سود، حاشیه، معوق، تمرکز، راکد، بخش‌ها |
| `get_customer_profile` | `customer_id` | پروفایل کامل فارسی رندرشده |
| `get_risks_and_actions` | `customer_id` | ریسک‌ها و فرصت‌ها با شاهد، استدلال، اقدام، مسئول، رفرنس |
| `get_signals_and_predictions` | `customer_id` | سیگنال‌ها با تفسیر + پیش‌بینی‌ها با نرخ پایه و تعدیل |
| `get_value_breakdown` | `customer_id` | تجزیهٔ کامل LTV، حاشیه، معوق، ارزش نجات |
| `segment_overview` | `kind` (`quadrant`/`rfm`) | آمار هر خانه یا هر بخش RFM |
| `data_patterns` | — | الگوهای تأییدشده و **ردشده** + کارت مدل |
| `search_customers` | `sort_by`, `ascending`, `segment`, `has_risk`, `has_opportunity`, `quadrant`, `rfm`, `limit` | جدول فیلترشده و مرتب |
| `search_text` | `query`, `limit`, `kind` | جست‌وجو در ۴٬۵۶۵ رکورد متن آزاد |
| `compare_customers` | `customer_ids` | مقایسهٔ کنار‌هم چند مشتری |
| `period_summary` | `period` | خلاصهٔ دوره، مقایسه، پیش‌بینی با خطای بک‌تست، فهرست رسیدگی و سه مشکل پرتکرار |
| `focus_list` | `limit` | هزینهٔ پول، سود واقعی، آزمون قیمت‌گذاری اعتبار، چهار خانه و فهرست تمرکز با دلیل هر حذف |
| `work_card` | `customer_id`, `family`, `limit` | کارتابل: هدف، هویت، کانال، کارها، «همین حالا»، و دستور کار تماس یا جلسه |

## ۲۳.۳ پرامپت سیستم — چه چیزی در آن قفل شده

پرامپت سیستم فقط «فارسی جواب بده» نیست. شش دستهٔ قید در آن هست:

**۱. ممنوعیت محاسبه.** «هیچ عددی را خودت محاسبه یا حدس نزن … اگر ابزار عددی نداد،
بگو در دسترس نیست.»

**۲. نگاشت پرسش به ابزار.** هفت قاعدهٔ صریح — «برای پرسش کلی از `portfolio_summary`
استفاده کن، نه جمع‌زدن پروفایل‌ها».

**۳. الزام رفرنس.** «ابزارها زیر هر ادعا خط `↳ منبع:` می‌گذارند … این خطوط را نگه
دار … اگر ابزار رفرنس نداد، بگو منبع این عدد در خروجی ابزار نیامده.»

**۴. سه هشدار دادهٔ همیشگی** — قفل‌شده در پرامپت، نه اختیاری:

- فروش اسمی است؛ قیمت واحد در مقایسهٔ هم‌ارز حدود ۸ برابر شده
- حاشیه ترکیبی است؛ ۳۲٪ تحقق‌یافته، بقیه برآوردی و حدود ۵ واحد خوش‌بینانه‌تر
- `source_status_LEAKY` همان رکود ۱۸۰ روزه با برچسب دیگر است — هرگز شاهد نگیر

**۵. دو استثنای احتمال.** «احتمال درخواست تخفیف نرخ پایهٔ تجربی ندارد و نسبی است؛
تأخیر پرداخت تقریباً ساختاری است و سابقه خبر کمی دارد.»

**۶. تصحیح فعال باور غلط.** «دو الگو آزموده و **رد** شدند … اگر کاربر خلافش را فرض
کرد، **اصلاح کن**.» یعنی دستیار وظیفه دارد پیش‌فرض غلط کاربر را تصحیح کند.

و مرز زمانی: «بازهٔ داده تا ۹ تیر ۱۴۰۱ است. هرگز طوری پاسخ نده که گویی از بعد از آن
خبر داری.»

## ۲۳.۴ حالت قطعی — پشتیبان بدون شبکه

`_ask_deterministic()` همان ۱۰ ابزار را با تطبیق قاعده‌محور صدا می‌زند:

۱. **استخراج شناسهٔ مشتری** با `\bC[_-]?(\d{4,6})\b` — سپس بر پایهٔ کلیدواژه یکی از
   چهار ابزار مشتری‌محور.
۲. **کلیدواژه‌های موضوعی** — الگو/مدل، RFM/چهارخانه، نجات، LTV، آفر باز، تک‌گروه.
۳. **نگاشت کلیدواژه به کد ریسک** — چک برگشتی، سقف اعتبار، راکد، معوق، حاشیه، ریزش
   حجم. مرتب از خاص به عام.
۴. **کلیدواژه‌های فرصت** — سهم از سبد، نمونه، رشد، فروش مکمل.
۵. **کلیدواژه‌های متنی** — شید رنگ، پرز، استحکام، بسته‌بندی، دنیر، فیلامنت.
۶. **پیش‌فرض هوشمند:** بدترین مشارکت خالص. «چون همیشه پرسش درست کسب‌وکار است.»

همهٔ تطبیق‌ها روی متن **نرمال‌شده** (`norm()`) انجام می‌شود: حذف ZWNJ، یکسان‌سازی
«ی/ک» عربی و فارسی، کوچک‌سازی.

**سه لایهٔ محافظت:** بدون کلید API → حالت قطعی. با کلید ولی خطای شبکه → حالت قطعی +
اعلام دلیل. سقف ۶ دور → پیام صریح. **دموی هکاتون در هیچ حالتی سفید نمی‌شود.**

## ۲۳.۵ فهرست مدل‌ها

```python
MODELS = ["gemini-2.5-flash", "gemini-2.0-flash", "gemini-flash-latest"]
```

قابل بازنویسی با `GEMINI_MODEL`. کلید از `GEMINI_API_KEY` یا `GOOGLE_API_KEY`.
`temperature=0.2` — چون کار روایت عدد است، نه خلاقیت.

---

# ۲۴. API

سرور FastAPI با ۱۳ نقطهٔ پایانی.

| متد | مسیر | پارامترها | خروجی |
|---|---|---|---|
| GET | `/` | — | `index.html` |
| GET | `/api/meta` | — | فرا‌داده: تعداد مشتری، تاریخ برش، گزینه‌های مرتب‌سازی، کدهای فیلتر، **هشدارهای داده**، حالت دستیار، پرسش‌های نمونه |
| GET | `/api/portfolio` | — | آمار سبد، سری ماهانه، مقایسهٔ بهار، شمارش ریسک/فرصت، ماه‌های ناقص |
| GET | `/api/customers` | `q`, `sort_by`, `ascending`, `segment`, `risk`, `opportunity`, `signal`, `rfm`, `quadrant`, `limit` | ۴۲ ستون برای هر سطر |
| GET | `/api/customer/{id}` | — | پروفایل کامل غنی‌شده + متن رندرشده |
| GET | `/api/segments` | — | چهارخانه، RFM، نقاط پراکندگی، ماتریس RFM |
| GET | `/api/worklist` | `limit` | کارت‌های مشکل و سطرهای کارتابل با دستور کار |
| GET | `/api/focus` | — | فهرست تمرکز، چهار خانه، هزینهٔ پول، آزمون قیمت، منحنی وصول، اعتبارسنجی دستی |
| GET | `/api/period` | `key` (۱w تا ۱y؛ خالی = دورهٔ پیشنهادی) | خلاصه، مقایسه، پیش‌بینی، بک‌تست، رسیدگی، سه مشکل، جدول انتخاب دوره |
| GET | `/api/patterns` | — | `VALIDATED_PATTERNS` + `MODEL_CARD` + نرخ‌های پایه |
| GET | `/api/search_text` | `q`, `limit`, `kind` | نتایج جست‌وجوی متن با خلاصه |
| POST | `/api/chat` | `{question, history}` | `{answer, trace, mode, model?, fallback_reason?}` |
| GET | `/api/health` | — | وضعیت سرور و کش |

مجموع: **۱۳ نقطهٔ پایانی**.

**دو نکتهٔ فنی:**

`_clean()` مقادیر `NaN` و `Inf` را به `None` تبدیل می‌کند. بدون آن، `NaN` در JSON
تولیدشده نامعتبر است و `JSON.parse` در مرورگر خطا می‌دهد — و چون سریال‌سازی pandas
این را سکوت می‌کند، اشکال فقط در مرورگر ظاهر می‌شود.

`_caveats(s)` هشدارهای داده را **از اعداد زندهٔ کش** می‌سازد نه از متن ثابت. اگر داده
عوض شود، هشدارها هم عوض می‌شوند.

---

# ۲۵. راهنمای کد فایل به فایل

## ۲۵.۱ نقشهٔ فایل‌ها

| فایل | خط | نقش | وابستگی |
|---|---|---|---|
`pipeline.py` | ۷۴۲ | بارگذاری، پاکسازی، موتور as-of، ساخت پروفایل، آمار سبد | pandas, jalali
`features.py` | ۴۸۸ | ۸۴ ویژگی، RFM، ماندگاری، LTV، چهارخانه، کارت مدل | pipeline
`signals.py` | ۵۳۲ | ۱۸ سیگنال، ۶ پیش‌بینی، `ref()`، نرخ‌های پایه | pipeline, jalali
`insights.py` | ۶۷۴ | ۱۵ ریسک، ۱۰ فرصت، اقدام بعدی، نگاشت‌های اقدام، بلوک کامرشال | signals, features
`store.py` | ۷۳۰ | ۱۱ ابزار، نمایهٔ متن، رندر پروفایل فارسی، کش | insights, features, period
`copilot.py` | ۴۱۲ | اتصال Gemini، پرامپت سیستم، حالت قطعی | store
`app.py` | ۳۶۰ | ۱۱ نقطهٔ پایانی FastAPI | store, copilot
`jalali.py` | ۹۲ | تبدیل تاریخ میلادی↔شمسی | —
`money.py` | ۲۲۰ | هزینهٔ پول، حاشیهٔ واقعی، آزمون قیمت‌گذاری اعتبار، منحنی وصول | pandas
`validate_cases.py` | ۱۵۵ | اعتبارسنجی دستی سه حساب | money
`period.py` | ۷۵۰ | موتور دوره: خلاصه، مقایسه، پیش‌بینی، بک‌تست، رسیدگی، مشکلات | pipeline, signals, jalali
`worklist.py` | ۲۹۰ | کارتابل: خانوادهٔ مشکل، هدف، کانال، دستور کار تماس و جلسه | pipeline
`verify.py` | ۴۷۰ | ۹۵ آزمون | همه
`static/index.html` | ۱٬۵۵۰ | داشبورد کامل (۶ تب + کشو + دستیار شناور) | charts.js, md.js
`static/charts.js` | ۳۳۶ | ۸ نوع نمودار SVG | —
`static/md.js` | ۱۲۶ | رندر Markdown | —

**اسکریپت‌های اکتشاف** (منبع اعدادی که در کد هاردکد شده‌اند): `explore_patterns.py`،
`explore2.py`، `explore3.py`، `explore4.py`، `calibrate_rates.py`. این‌ها یک‌بار اجرا
شدند و خروجی‌شان به ثابت‌های `CAL_X`/`CAL_Y`، جدول‌های نرخ پایه و `MODEL_CARD` تبدیل
شد. **نگه داشته شده‌اند تا هر عددی قابل بازتولید باشد.**

## ۲۵.۲ `pipeline.py` — تابع به تابع

| تابع | ورودی | خروجی | کار |
|---|---|---|---|
`load_raw()` | مسیر دو فایل اکسل | دیکشنری ۱۶ دیتافریم | خواندن خام |
`clean(raw)` | دیکشنری خام | دیکشنری پاک | ۵ تلهٔ داده + ۷ قاعدهٔ یکپارچه‌سازی + شاخص قیمت + ترجمهٔ مقادیر فارسی |
`as_of_view(D, date)` | داده + تاریخ | نمای آن تاریخ | فیلتر سطر + راست‌سانسور خروجی |
`build_profiles(D)` | داده | دیکشنری پروفایل | ۱۱ بخش برای هر مشتری |
`add_complaint_impact()` | پروفایل + داده | پروفایل | مقایسهٔ حجم ۹۰ روز پیش/پس از شکایت |
`portfolio_stats(D, profiles)` | داده + پروفایل | آمار سبد | جمع‌ها، سری ماهانه، مقایسهٔ بهار، ماه‌های ناقص |
`fa(key)` | کلید انگلیسی | متن فارسی | ترجمهٔ مقدارهای شمارشی |

## ۲۵.۳ `features.py` — ثابت‌های کلیدی

```python
LTV_HORIZON_MONTHS = 24
MONTHLY_DISCOUNT   = 0.015
DISCOUNT_FACTOR    = sum(1/(1+MONTHLY_DISCOUNT)**i for i in range(1, 25))  # ۲۰٫۰۳
CAL_X = [0.10, 0.275, 0.425, 0.575, 0.725, 0.905]   # امتیاز خام
CAL_Y = [0.287, 0.353, 0.518, 0.611, 0.815, 0.966]  # نرخ بازگشت مشاهده‌شده
```

`DISCOUNT_FACTOR` **محاسبه می‌شود نه هاردکد**. اگر افق یا نرخ عوض شود، ضریب خودش
به‌روز می‌شود و در UI هم همان مقدار نمایش داده می‌شود.

توابع اصلی: `build_features(D, profiles)` → دیتافریم ۸۴ ستونی؛ `add_rfm(F)`؛
`add_retention(F)` با تجزیهٔ اجزا؛ `add_ltv(F)`؛ `add_quadrant(F)`.

## ۲۵.۴ ترتیب اجرا و چرایی‌اش

```
load_raw → clean → build_profiles → add_complaint_impact → portfolio_stats
                                          ↓
                      build_features → add_rfm → add_retention → add_ltv → add_quadrant
                                          ↓
                          extract_signals → predict_decisions
                                          ↓
                          find_risks → find_opportunities → next_best_actions
                                          ↓
                          commercial_block → summarise → enrich
                                          ↓
                     Store(enriched, portfolio, periods) → cache
                                          ↑
                      period.build_all → ۷ دوره × (خلاصه، مقایسه، پیش‌بینی، بک‌تست،
                                          رسیدگی، سه مشکل) + انتخاب دورهٔ پیشنهادی
```

**چرا این ترتیب اجباری است:**

- ویژگی‌ها به پروفایل نیاز دارند (پروفایل سطح مشتری است، ویژگی سطح مشتری)
- RFM باید پیش از LTV باشد (بخش RFM در خلاصه استفاده می‌شود)
- ماندگاری باید پیش از LTV باشد (LTV آینده به ماندگاری ضرب می‌شود)
- چهارخانه باید پس از هر دو باشد (به حاشیه و ماندگاری نیاز دارد)
- سیگنال‌ها باید پیش از پیش‌بینی‌ها باشند (پیش‌بینی از کد سیگنال تعدیل می‌گیرد)
- ریسک و فرصت باید پیش از اقدام بعدی باشند (اقدام از هر دو تغذیه می‌شود)

## ۲۵.۵ کش

`build_cache()` همهٔ زنجیره را اجرا و در `cache/` می‌نویسد: `profiles.json` (پروفایل
غنی‌شدهٔ همهٔ مشتریان)، `portfolio.json` و `periods.json` (هر ۷ دوره). `load_store(rebuild=False)` از کش می‌خواند.

**زمان:** ساخت کامل حدود یک دقیقه (شامل ~۱۷ ثانیه محاسبهٔ ۷ دوره با بک‌تست)؛ بارگذاری از کش کمتر از یک ثانیه. برای دمو
حیاتی است.

**تلهٔ کش:** پس از هر تغییر منطق باید `build_cache()` دوباره اجرا شود، وگرنه سرور
دادهٔ قدیمی سرو می‌کند. یک‌بار به همین دلیل، پس از تغییر به مقایسهٔ بهار شمسی، سرور
هنوز دادهٔ نیم‌سال میلادی می‌داد (بخش ۲۸.۶).

---

# ۲۶. آزمون‌ها

`verify.py` — **۹۵ آزمون، همه سبز**. اجرا: `python verify.py`.

## ۲۶.۱ گروه‌های آزمون

| گروه | چه چیزی را تضمین می‌کند | نمونه |
|---|---|---|
**یکپارچگی ارجاعی** | کلید یتیم نیست، رابطه‌های متادیتا برقرارند | «هیچ کلید یتیمی در روابط اعلام‌شدهٔ متادیتا نیست» |
**تله‌های داده** | هر ۵ تله واقعاً بسته شده | «هیچ ردیف ردیابی شکایتی در فروش نمانده»؛ «بازهٔ فروش پیش از ردیف‌های تزریقی تمام می‌شود»؛ «CRM به یک ردیف برای هر تعامل کاهش یافته»؛ «ستون‌های درصدی آزمایشگاه به مقیاس درصد آمده‌اند»؛ «فیلد وضعیت نشتی تغییر نام یافته و قرنطینه است» |
**اتحادهای حسابداری** | ریاضی پول درست است | «معوق + سررسیدنشده = مانده باز، برای هر مشتری»؛ «مشارکت خالص = سود ناخالص منهای معوق»؛ «هیچ مانده باز منفی وجود ندارد» |
**موتور as-of** | نشتی آینده وجود ندارد | «برش زمانی هیچ فروش آینده‌ای را لو نمی‌دهد»؛ «نمای قدیمی‌تر فروش کمتری دارد»؛ «شکایت‌های بازِ آن تاریخ، رسیدگی‌شده نشان داده نمی‌شوند»؛ «آفرهای بی‌نتیجهٔ آن تاریخ، بی‌پاسخ علامت خورده‌اند» |
**کامل بودن پروفایل** | هیچ مشتری جا نمانده | «هر مشتری یک پروفایل دارد»؛ «هر مشتری خلاصه وضعیت فارسی دارد»؛ «هر مشتری با ریسک یا فرصت، اقدام بعدی دارد» |
**متریک‌ها** | فرمول‌ها با هم می‌خوانند | «LTV = محقق‌شده + آیندهٔ تنزیل‌شده»؛ «LTV آینده با ضریب تنزیل و ماندگاری می‌خواند»؛ «مجموع سهم اجزای ماندگاری با امتیاز خام می‌خواند»؛ «کالیبراسیون یکنواست» |
**اشکال‌های خاص** | رگرسیون نگیرد | «فاصلهٔ سفارش روی تاریخ‌های متمایز حساب شده (هیچ صفری نیست)»؛ «ماه‌های ناقص مرزی شناسایی شده‌اند» |
**رفرنس** | هیچ ادعای بی‌منبع نیست | «هر سیگنال حداقل یک رفرنس دارد»؛ «هر پیش‌بینی نرخ پایه و رفرنس دارد»؛ «هر ریسک و فرصت، استدلال و رفرنس دارد»؛ «هر رفرنس نام شیت معتبر دارد»؛ «تعداد کل رفرنس‌ها معنادار است» |
**صداقت** | اعتراف‌ها حذف نشده‌اند | «الگوهای ردشده در کتابخانهٔ الگو ثبت شده‌اند»؛ «کارت مدل، برتری پیش‌بین تک‌متغیره را صریح گزارش می‌کند» |
**نگاشت اقدام** | هیچ دلیلی بی‌اقدام نیست | «همهٔ دلایل آفر نگاشت اقدام دارند»؛ «اقدام آفر بر پایهٔ دلیل تولید می‌شود» |
**موتور دوره (۱۹ آزمون)** | پنجره، پیش‌بینی، اولویت و پول در خطر درست‌اند | «لنگر دوره روی ماه ناقص انتهایی نمی‌افتد»؛ «پنجرهٔ دوره و دورهٔ قبل هم‌طول و بدون همپوشانی‌اند»؛ «فروش دوره با جمع مستقیم شیت فروش می‌خواند»؛ «پیش‌بینی فقط از دادهٔ پیش از لنگر ساخته می‌شود»؛ «اصلاح اریبی فقط وقتی روشن است که بک‌تست بهترش کند»؛ «طبقهٔ اولویت با تقاطع ارزش و بحرانی بودن می‌خواند»؛ «پول در خطر هر مشکل با جمع سطرهای همان مشکل می‌خواند» |

## ۲۶.۲ سه آزمون که ارزش خاص دارند

**«برش زمانی هیچ فروش آینده‌ای را لو نمی‌دهد.»** این آزمون تضمین می‌کند اعتبارسنجی
خارج‌از‌زمان معتبر است. بدون آن، هر AUC گزارش‌شده بی‌معناست.

**«مجموع سهم اجزای ماندگاری با امتیاز خام می‌خواند.»** تضمین می‌کند نموداری که به
کارشناس نشان می‌دهیم، **واقعاً** تجزیهٔ همان امتیاز است — نه یک تصویر تزئینی.

**«کارت مدل، برتری پیش‌بین تک‌متغیره را صریح گزارش می‌کند.»** آزمونی که **صداقت را
اجبار می‌کند**: اگر کسی در آینده اعتراف «رکود تنها بهتر است» را از کارت مدل حذف
کند، آزمون قرمز می‌شود.

---

# ۲۷. راه‌اندازی

## ۲۷.۱ پیش‌نیاز

```
python >= 3.11
pandas, openpyxl, fastapi, uvicorn, google-genai, numpy, pydantic
```

## ۲۷.۲ اجرا

```bash
pip install -r requirements.txt

export GEMINI_API_KEY="کلید-شما"     # اختیاری؛ بدون آن حالت قطعی کار می‌کند
export GEMINI_MODEL="gemini-2.5-flash"   # اختیاری

python verify.py                      # ۴۸ آزمون
python -c "from store import build_cache; build_cache()"   # ساخت کش
./run.sh                              # سرور روی 127.0.0.1:8000
```

`run.sh` همین سه گام را با پیام‌های فارسی انجام می‌دهد و اگر کش وجود نداشت خودش
می‌سازد.

## ۲۷.۳ مسیر دمو

`DEMO.md` سه سناریوی آماده دارد. مسیر پیشنهادی ۵ دقیقه‌ای:

۱. **نمای کل** → کارت تورم: «رشد فروش واقعی نیست». سپس KPI معوق: ۱٫۲۶ برابر سود.
۲. **چهارخانه** → کلیک روی خانهٔ «نجات فوری» (۲۱۱ مشتری) → جدول فیلتر شد.
۳. **مرتب‌سازی بر ارزش نجات** → مشتری اول → پروفایل کامل.
۴. **چهار پنل** → سیگنال را باز کنید → رفرنس را باز کنید → رکورد منبع.
۵. **تاریخچهٔ یکپارچه** → سه سامانه در یک خط زمان.
۶. **تب الگوها** → الگوهای **ردشده** و اعتراف کارت مدل.
۷. **دستیار** → «کدام مشتری‌ها حاشیه سود خوبی دارند ولی در حال از دست رفتن‌اند؟» →
   فهرست ابزارهای فراخوانی‌شده زیر پاسخ.

---

# ۲۸. تله‌های فنی که با آن‌ها برخوردیم

این بخش برای کسی است که کد را ادامه می‌دهد. هر مورد **واقعاً** رخ داد و وقت گرفت.

## ۲۸.۱ در pandas 3، ستون متنی dtype `object` نیست

```python
# ❌ در pandas 3 همیشه False
if df[c].dtype == object: ...

# ✅
elif pd.api.types.is_string_dtype(df[c]) or pd.api.types.is_object_dtype(df[c]):
```

**اثر:** همهٔ ترجمه‌های مقدار فارسی→انگلیسی **بی‌صدا** رد می‌شدند. خطا نداد؛ فقط
مقادیر ترجمه‌نشده ماندند و قاعده‌هایی که به مقدار انگلیسی نگاه می‌کردند هیچ‌وقت فعال
نشدند. **بدترین نوع اشکال: بی‌صدا.**

## ۲۸.۲ `DataFrame.flags` رزرو شده است

```python
f.flags = ...      # ❌ خصیصهٔ رزروشدهٔ pandas
f["flags"] = ...   # ✅
```

## ۲۸.۳ ZWNJ (U+200C) جست‌وجوی دیکشنری فارسی را می‌شکند

«درخواست‌توسعه» با نیم‌فاصله و «درخواست توسعه» با فاصله، دو رشتهٔ متفاوت‌اند.
`_strip_zwnj()` پیش از هر جست‌وجوی دیکشنری اعمال می‌شود، و `norm()` در `store.py`
علاوه بر آن «ی/ک» عربی را هم یکسان می‌کند.

## ۲۸.۴ `??` را نمی‌توان با `||` بدون پرانتز ترکیب کرد

```javascript
const t = total ?? parts.reduce(...) || 1;      // ❌ SyntaxError
const t = (total ?? parts.reduce(...)) || 1;    // ✅
```

**اثر:** یک `SyntaxError` در `charts.js` کل فایل را از کار انداخت — و چون خطا در
کنسول بود نه در صفحه، همهٔ نمودارها **بی‌صدا** خالی شدند.

## ۲۸.۵ فاصلهٔ سفارش روی تاریخ‌های تکراری صفر می‌شود

هر فاکتور چند خط دارد و همهٔ خطوط یک تاریخ دارند. `diff()` روی تاریخ‌های خطی، عمدتاً
صفر می‌دهد → `order_gap ≈ 0` → `silence_ratio = recency / 0` عدد نجومی.

```python
def _gap(x):
    d = x.drop_duplicates().sort_values().diff().dt.days   # ← تاریخ‌های متمایز
    return d.mean() if d.notna().sum() >= 1 else np.nan

F["silence_ratio"] = np.where(F.order_gap >= 3, F.recency / F.order_gap, np.nan)
```

**نشانهٔ کشف:** مشتری‌ای با رکود ۲۴ روز، `silence_ratio = 112.2` نشان می‌داد.
آزمون «هیچ صفری نیست» برای همین اضافه شد.

## ۲۸.۶ کش قدیمی دادهٔ قدیمی سرو می‌کند

پس از تغییر مقایسهٔ نیم‌سال میلادی به بهار شمسی، سرور هنوز اعداد قدیمی می‌داد.
`build_cache()` باید پیش از راه‌اندازی مجدد اجرا شود. اگر عددی در UI با کد نمی‌خواند،
**اول کش را بسازید**.

## ۲۸.۷ اشکال نمودار پراکندگی — و اشکال دومی که فاش کرد

**نشانه:** نقاط چهارخانه در کارت مجاور پخش شده بودند.

**علت ریشه‌ای:** `niceTicks` گرد می‌کرد نه floor/ceil. برای دادهٔ ۰٫۰۲ تا ۰٫۹۷،
تیک‌ها ۰٫۲۵ تا ۰٫۷۵ شدند. تابع نگاشت `X()` بازهٔ تیک را مبنا می‌گیرد، پس `X(0.97)`
بیرون از ناحیهٔ ترسیم افتاد — و چون SVG پیش‌فرض `overflow: visible` دارد، روی کارت
بغلی کشیده شد.

**سه اصلاح:**
۱. `Math.floor(min/step)*step` و `Math.ceil(max/step)*step`
۲. `overflow: hidden` روی SVG نمودارها
۳. محدود کردن موقعیت برچسب‌ها به داخل ناحیه

**اشکال دومی که در جریان اصلاح فاش شد:** هر چهار برچسب گوشهٔ چهارخانه **آینه‌ای**
بودند. در `dir="rtl"` محور افقی معکوس است، پس ماندگاری بالا سمت **چپ** می‌نشیند —
و علاوه بر آن `text-anchor` هم معکوس عمل می‌کند. با اندازه‌گیری `getBBox()` و بررسی
چهار مشتری با وضعیت معلوم تأیید شد.

**نتیجهٔ نهایی:** ۰ نقطه از ۶۲۱ بیرون از کادر.

## ۲۸.۸ فرمول با عدد بزرگ در متن راست‌به‌چپ به‌هم می‌ریزد

الگوریتم دوجهته عدد لاتین بلند داخل متن فارسی را جابه‌جا می‌کند. راه‌حل: فرمول را
با **نسبت** بنویس نه با مبلغ خام، و اعداد را در `<span class="ltr">` بگذار.

## ۲۸.۹ `profile.py` نام یک ماژول استاندارد پایتون است

فایل موقتی به نام `profile.py` ماژول استاندارد را سایه انداخت و `nbconvert` را از کار
انداخت. اسکریپت‌های موقت به `scratch/` منتقل شدند.

## ۲۸.۱۰ ماه‌های مرزی ناقص، هر مقایسهٔ روندی را خراب می‌کنند

نخستین و آخرین ماه داده، ماه کامل نیستند. اگر در مقایسهٔ دوره بمانند، «سقوط حجم»
مصنوعی می‌سازند. ما اول ادعای «شاخص حجم ۱۰۰→۴۹» را داشتیم که **آلوده** بود؛ با
مقایسهٔ بهار شمسی به بهار شمسی، عدد درست **۱۰۰→۶۸** شد. `partial_months` صریح
نگه‌داری و در همهٔ نمودارها حذف می‌شود.

## ۲۸.۱۱ `NaN` در JSON نامعتبر است

`json.dumps(float('nan'))` رشتهٔ `NaN` می‌سازد که JSON معتبر نیست و `JSON.parse`
در مرورگر خطا می‌دهد. `_clean()` در `app.py` همه را به `None` تبدیل می‌کند.

---

# ۲۹. محدودیت‌ها و مسیر ادامه

## ۲۹.۱ محدودیت‌های داده

| محدودیت | اثر | کاهش اثر در سامانه |
|---|---|---|
**۶۸٪ خطوط بهای تمام‌شدهٔ برآوردی دارند** | حاشیه حدود ۵ واحد درصد خوش‌بینانه‌تر | سهم مبنای تحقق‌یافته در هر پروفایل و در پرامپت سیستم اعلام می‌شود |
**سهم از سبد برآورد کارشناس است** | مبلغ فرصت سهم، سقف است نه انتظار | در متن `logic` صریح گفته می‌شود؛ منبع برآورد در رفرنس |
**بازه ۳۱ ماه است** | افق LTV ۲۴ ماهه فراتر از دادهٔ مشاهده‌شده است | افق و نرخ تنزیل صریح؛ ضریب وصول اعمال می‌شود |
**تورم ۸ برابری** | هر تحلیل ریالی بین سال‌ها بی‌معناست | شاخص قیمت، فروش حقیقی، و حجم به‌عنوان مبنای روند |
**۲۰ مشتری بدون فروش** | پروفایل تجاری ندارند | صریح اعلام می‌شود، نه اینکه صفر نشان داده شود |
**هزینهٔ خدمت‌دهی نداریم** | سود ناخالص است نه سود عملیاتی مشتری | LTV به‌عنوان «ناخالص» برچسب خورده |

## ۲۹.۲ محدودیت‌های مدل

**۱. رکود تنها بهتر از مدل ترکیبی است.** AUC ۰٫۸۵۲ در برابر ۰٫۸۳۱. مدل ترکیبی را
نگه داشتیم چون **تجزیه‌پذیر** است — کارشناس باید ببیند «کدام نشانه چقدر سهم دارد» —
ولی این انتخاب هزینهٔ دقت داشت و در کارت مدل صریح اعلام شده. رگرسیون لجستیک ۰٫۸۶۸
می‌داد و آن هم رد شد به همان دلیل: ضرایب یادگرفته‌شده برای کارشناس فروش قابل روایت
نیستند.

**۲. اندازهٔ نمونه در باکت‌های انتهایی کوچک است.** «۴ شکایت یا بیشتر» ۶۹ مشاهده
دارد. نرخ ۷۱٪ بازهٔ اطمینان پهنی دارد.

**۳. یک نرخ پایه تجربی نیست** — درخواست تخفیف (بخش ۱۳.۷).

**۴. کالیبراسیون خطی درون‌یابی است نه مدل.** بیرون از بازهٔ `CAL_X` ثابت می‌شود.

**۵. تعدیل‌های پیش‌بینی، جز یکی، قضاوت کارشناسی‌اند** نه ضریب برآوردشده (بخش ۱۵.۳).

## ۲۹.۳ محدودیت‌های سامانه

- **کش دستی است.** تغییر منطق نیاز به بازسازی دارد؛ خودکار نیست.
- **بدون احراز هویت.** برای دمو ساخته شده، نه استقرار.
- **تک‌کاربره.** هیچ حالت هم‌زمانی یا قفلی وجود ندارد.
- **بدون پایگاه‌داده.** همه‌چیز در حافظه و JSON. برای ۶۴۴ مشتری کافی است؛ برای
  ۶۴٬۰۰۰ نه.
- **جست‌وجوی متن، تطبیق زیررشته است** نه معنایی. «شید» را پیدا می‌کند، «تفاوت رنگ»
  را نه.

## ۲۹.۴ مسیر ادامه، به ترتیب ارزش

**۱. اتصال زنده به ERP و CRM.** جایگزینی اکسل با اتصال مستقیم و بازسازی شبانهٔ کش.
موتور as-of از قبل برای این طراحی شده — فقط لایهٔ `load_raw()` عوض می‌شود.

**۲. ثبت بازخورد اقدام.** بزرگ‌ترین شکاف فعلی: نمی‌دانیم اقدام پیشنهادی انجام شد یا
نه و نتیجه چه بود. با ثبت آن، **حلقهٔ یادگیری** بسته می‌شود و می‌توان وزن‌های فوریت
را از داده آموخت نه از قضاوت.

**۳. هزینهٔ خدمت‌دهی در سطح مشتری.** با آن، LTV از ناخالص به عملیاتی تبدیل می‌شود
و ۳۱۰ مشتری با مشارکت خالص منفی دقیق‌تر رتبه‌بندی می‌شوند.

**۴. جست‌وجوی معنایی متن.** تعبیه‌سازی ۴٬۵۶۵ رکورد متنی، تا «مشکل رنگ» رکوردهای
«شید» را هم بیاورد.

**۵. مدل ماندگاری با ضرایب آموخته‌شده.** با نگه داشتن تجزیه‌پذیری — مثلاً مدل خطی
تک‌لایه با ضرایب مثبت‌شده — می‌توان هم ۰٫۸۶۸ گرفت هم سهم هر نشانه را نشان داد.

**۶. هشدار فعال.** به‌جای جست‌وجوی کارشناس، سامانه خودش وقتی `silence_ratio` از
آستانه گذشت یا آفری منقضی شد، اعلام کند.

## ۲۹.۵ جمع‌بندی: سه چیزی که این پروژه را متفاوت می‌کند

**۱. هر عدد رفرنس دارد.** ۱۶٬۷۹۱ رفرنس؛ صفر ادعای بی‌منبع. جایی که ادعا از تجمیع یا
نرخ پایه می‌آید نه یک رکورد، **همین را می‌گوید**.

**۲. هر احتمال از شمارش رویداد واقعی می‌آید.** ۱۰٬۳۳۹ مشاهدهٔ مشتری-ماه. مدل روی
پنجرهٔ خارج‌از‌زمان سنجیده شده، نه روی دادهٔ آموزش.

**۳. آنچه کار نکرد هم گزارش شده.** سه الگوی ردشده، اعتراف برتری پیش‌بین تک‌متغیره،
پنج تلهٔ داده، و ۱۱ تلهٔ فنی — همه در داشبورد و در همین مستند. تحلیلی که هیچ فرضیه‌ای
را رد نکرده، احتمالاً آزمون نکرده است.

---

# ۳۰. موتور دوره — طراحی، پیش‌بینی و اعتبارسنجی

`period.py` — لایه‌ای که «وضعیت یک بازهٔ زمانی» را می‌سازد: خلاصه، مقایسه با دورهٔ
قبل، پیش‌بینی دورهٔ بعد، فهرست رسیدگی و پرتکرارترین مشکلات. هفت طول دوره پشتیبانی
می‌شود: ۱ هفته، ۲ هفته، ۱ ماه، ۲ ماه، ۳ ماه، ۶ ماه، ۱ سال.

## ۳۰.۱ لنگر دوره — نخستین تصمیم، و مهم‌ترینش

تاریخ برش داده **۹ تیر ۱۴۰۱** است، ولی آخرین فروش ثبت‌شده **۱۶ خرداد ۱۴۰۱**، و ماه
خرداد فقط **۱۵ خط فروش** دارد در برابر **۱۶۰ خط مورد انتظار** به نسبت ماه‌های پیش.
یعنی ماه انتهایی داده ناقص است.

اگر پنجرهٔ دوره را روی تاریخ برش می‌بستیم، «۱ هفتهٔ اخیر» صفر فروش نشان می‌داد و
«۱ ماه اخیر» یک سقوط ۹۰ درصدی مصنوعی. `data_anchor()` این را تشخیص می‌دهد:

```python
typical  = میانهٔ تعداد خط شش ماه پیش از ماه آخر
expected = typical × روزهای سپری‌شدهٔ ماه آخر ÷ ۳۰
if  خطوط ماه آخر / expected < 0.35:
    لنگر = پایان ماه پیش از آن
```

نتیجه: لنگر **۱۰ خرداد ۱۴۰۱** و متن هشدار در بالای تب نمایش داده می‌شود. همان اصل
«ماه ناقص مرزی» بخش ۲۸.۱۰، این‌بار در سطح دوره.

## ۳۰.۲ حقایق دوره — بدون هیچ برآوردی

`_window_facts(a, b)` فقط می‌شمارد. فروش اسمی و حقیقی، حجم، سود ناخالص و حاشیه،
مشتری فعال، فاکتور، خط سفارش، **وصول در بازه**، **معوق کل در پایان بازه**،
سررسیدنشده، شکایت و شکایت با شدت زیاد/بحرانی.

نکتهٔ مطالبات: «معوق کل» یک **عکس لحظه‌ای در پایان دوره** است (انباشته)، ولی
«وصول‌شده» یک **جریان درون دوره** است. این دو ماهیت متفاوتی دارند و در UI هم جدا
نشان داده می‌شوند — همان تفکیکی که کاربر خواسته بود.

## ۳۰.۳ پیش‌بینی — چهار مدل، همه از نرخ تجربی

**فروش و سود، پایین‌به‌بالا در سطح مشتری:**

```
λ_c        = −ln(1 − p₉₀(رکود c)) ÷ ۹۰            ← نرخ پواسون از جدول بخش ۱۳.۲
p_فعال(c)  = ۱ − exp(−λ_c × D)                    ← تبدیل ۹۰ روز به طول این دوره
نرخ حقیقی  = فروش حقیقی c در ۳۶۵ روز گذشته ÷ ۳۶۵
E[فروش]    = Σ_c p_فعال(c) × نرخ حقیقی_c × D × سطح قیمت پیش‌رو
E[سود]     = E[فروش] × حاشیهٔ ۱۸۰ روز اخیر
```

سه تصمیم که پشت این فرمول است:

۱. **نرخ روی فروش حقیقی محاسبه می‌شود، نه اسمی.** با تورم ۸ برابری، میانگین ۱۲ ماه
   اسمی، دورهٔ بعد را شدیداً کم‌برآورد می‌کند.
۲. **سطح قیمت به جلو برون‌یابی می‌شود.** رشد لگاریتمی ماهانهٔ شاخص قیمت در ۱۲ ماه
   گذشته **۰٫۱۳۸** (حدود ۱۵٪ ماهانه) است و تا **میانهٔ دورهٔ آینده** اعمال می‌شود،
   با سقف ۳ برابر. در بک‌تست، همین یک جزء خطای دورهٔ ۳ ماهه را از ۳۳٪ به ۲۱٪ رساند.
۳. **تبدیل پواسون تنها فرض توزیعی کل موتور است** و صریح اعلام می‌شود.

**شکایت:** جمع احتمال برنولی تک‌تک مشتریان، از جدول سابقهٔ شکایت (بخش ۱۳.۳) با همان
تبدیل ۱۸۰ روز به D روز.

**وصول:**

```
E[وصول] = مانده باز × نرخ بازیابی تجربی(D)  +  E[فروش] × سهم وصول فاکتور نو(D)
```

جزء دوم حیاتی است: بدون آن، وصول روی **فروش جدید همان دوره** دیده نمی‌شود و در
دورهٔ ۳ ماهه خطا از ۱۹٪ به ۵۵٪ می‌پرید. هر دو نرخ روی ۴ پنجرهٔ تاریخی هم‌طول
اندازه‌گیری می‌شوند. برای دورهٔ ۲ ماهه: بازیابی **۳۷٪**، سهم فاکتور نو **۳۹٪**.

**معوق پایان دوره:**

```
E[معوق] = معوق فعلی × (۱ − نرخ بازیابی معوق) + سررسیدهای این دوره × سهم تاریخی تأخیر
```

برای دورهٔ ۲ ماهه: بازیابی معوق **۲۸٪**، سهم تأخیر **۸۱٪** (یعنی ۸۱٪ ارزش فاکتورها
در تاریخ سررسید هنوز کامل تسویه نشده — عدد تکان‌دهنده‌ای که با یافتهٔ «تأخیر
ساختاری است» بخش ۱۳.۶ می‌خواند).

## ۳۰.۴ اریبی — اندازه‌گیری‌شده، نه پنهان‌شده

مدل پایین‌به‌بالا **سیستماتیک کم‌برآورد می‌کند**: احتمال سفارش هر مشتری را ضرب
می‌کند، ولی مشتری تازه و سفارش خارج از الگو را نمی‌بیند. در بک‌تست، نسبت
پیش‌بینی به واقعیت در دوره‌های ۱ ماهه بین ۰٫۳۲ و ۰٫۵۷ بود.

`_bias()` این نسبت را از دوره‌های گذشته می‌گیرد و معکوسش را به‌عنوان ضریب اصلاح
اعمال می‌کند (بریده در بازهٔ ۰٫۷ تا ۲٫۵). **ضریب در خروجی API نمایش داده می‌شود**،
پنهان نمی‌ماند.

ولی این اصلاح همه‌جا کمک نمی‌کند:

| دوره | خطای میانگین بدون اصلاح | با اصلاح | تصمیم |
|---|---|---|---|
| ۱ هفته | ۶۵٫۱٪ | **۳۹٫۵٪** | اصلاح روشن |
| ۲ هفته | ۵۰٫۷٪ | **۲۵٫۹٪** | اصلاح روشن |
| ۱ ماه | ۴۷٫۵٪ | **۲۱٫۹٪** | اصلاح روشن |
| ۲ ماه | **۲۴٫۹٪** | ۶۰٫۷٪ | اصلاح خاموش |
| ۳ ماه | **۲۶٫۴٪** | ۶۵٫۹٪ | اصلاح خاموش |
| ۶ ماه | **۲۸٫۹٪** | ۸۲٫۶٪ | اصلاح خاموش |
| ۱ سال | **۴۲٫۶٪** | — | اصلاح خاموش |

دلیل: برای دورهٔ بلند، پنجره‌های اریبی چهار تا شش دوره به عقب می‌روند و به رژیم
دادهٔ متفاوتی می‌افتند. `choose_backtest()` هر دو حالت را **خارج از نمونه** اجرا
می‌کند و حالت بهتر را انتخاب و اعلام می‌کند. تصمیم به داده سپرده شده، نه به سلیقه.

## ۳۰.۵ بک‌تست خارج از نمونه

نکتهٔ ظریف: اگر ضریب اصلاح را از همان دوری بگیریم که خطایش را گزارش می‌کنیم، عدد
خوش‌بینانه و بی‌معنا می‌شود. `backtest()` برای هر دور k، اریبی را **فقط از دورهای
k+1 تا k+3** محاسبه می‌کند. `_FC_CACHE` پیش‌بینی‌های تکراری را نگه می‌دارد تا هزینه
منفجر نشود.

خطاهای گزارش‌شدهٔ دورهٔ پیشنهادی (۲ ماه، ۳ دور): **فروش ۲۷٫۰٪ · سود ۲۲٫۱٪ ·
وصول ۱۹٫۲٪ · شکایت ۳۱٫۵٪**. هر عدد در خود کارت پیش‌بینی نمایش داده می‌شود.

## ۳۰.۶ انتخاب دورهٔ پیش‌فرض — از داده، نه از سلیقه

```
کفایت رویداد = min(1, مشتری/۱۵۰) × min(1, شکایت/۱۵) × min(1, رویداد وصول/۳۰۰)
امتیاز       = کفایت رویداد ÷ (۱ + خطای میانگین بک‌تست ÷ ۱۰۰)
```

| دوره | مشتری | شکایت | رویداد وصول | خطای بک‌تست | کفایت | **امتیاز** |
|---|---|---|---|---|---|---|
| ۱ هفته | ۹ | ۳ | ۶۴ | ۳۹٫۵٪ | ۰٫۰۰۳ | ۰٫۰۰۲ |
| ۲ هفته | ۱۹ | ۵ | ۱۵۱ | ۲۵٫۹٪ | ۰٫۰۲۱ | ۰٫۰۱۷ |
| ۱ ماه | ۸۹ | ۹ | ۳۳۵ | **۲۱٫۹٪** | ۰٫۳۵۶ | ۰٫۲۹۲ |
| **۲ ماه** | ۱۷۱ | ۱۷ | ۶۶۲ | ۲۴٫۹٪ | ۱٫۰۰ | **۰٫۸۰۰** |
| ۳ ماه | ۲۰۱ | ۲۳ | ۹۶۶ | ۲۶٫۴٪ | ۱٫۰۰ | ۰٫۷۹۱ |
| ۶ ماه | ۲۶۰ | ۵۷ | ۲٬۱۹۸ | ۲۸٫۹٪ | ۱٫۰۰ | ۰٫۷۷۶ |
| ۱ سال | ۳۹۵ | ۱۷۸ | ۵٬۹۸۹ | ۴۲٫۶٪ | ۱٫۰۰ | ۰٫۷۰۱ |

**برنده: ۲ ماه.** دقیق‌ترین پیش‌بینی مال ۱ ماه است (۲۱٫۹٪) ولی با ۹ شکایت و ۸۹
مشتری، ماژول‌های «رسیدگی» و «سه مشکل پرتکرار» تقریباً خالی می‌شوند. ۲ ماه اولین
دوره‌ای است که کفایت رویداد به سقف می‌رسد، و بین دوره‌های اشباع‌شده کمترین خطا را
دارد. برچسب **پیشنهادی** روی همین دکمه می‌نشیند و دلیلش زیر نوار دوره نوشته
می‌شود. اگر داده عوض شود، انتخاب هم عوض می‌شود.

## ۳۰.۷ رویدادهای دوره — پاسخ به «چرا الان؟»

`_period_events()` پنج نوع رویداد **درون بازه** را با رفرنس استخراج می‌کند:

| رویداد | تعریف | کد ریسک متناظر |
|---|---|---|
| شکایت جدید | شکایت ثبت‌شده در بازه | `open_severe_complaint`، `quality_linked_decline` |
| چک برگشتی | وصول با پرچم برگشتی در بازه | `bounced_cheques` |
| سررسید تسویه‌نشده | فاکتوری که در بازه سررسید شد و مانده باز دارد | `overdue_exceeds_gp`، `over_credit_limit`، `overdue_aged`، `low_collection_rate` |
| افت حجم خرید | حجم بازه < ۷۵٪ حجم بازهٔ قبل | `volume_collapse`، `dormant` |
| انقضای آفر | آفر بی‌پاسخی که تاریخ اعتبارش در بازه گذشت | `competitor_dominant`، `dormant` |

اگر مشتری رویدادی در بازه داشته باشد، **مشکل اصلی از میان ریسک‌های متناظر همان
رویداد** انتخاب می‌شود؛ وگرنه از کل ریسک‌های مشتری. این تفاوت، «مشکل امروز» را از
«مشکل همیشگی» جدا می‌کند.

## ۳۰.۸ فهرست رسیدگی — چهار طبقهٔ اولویت

دو محور: **ارزش مشتری** و **بحرانی بودن مشکل**.

```
با ارزش  = LTV در ۳۰٪ بالای سبد  یا  رتبهٔ فروش ≤ ۱۰۰
بحرانی   = شدت مشکل بحرانی/زیاد  یا  شکایت در همین دوره  یا  ماندگاری < ۰٫۲۵
```

| طبقه | معنا | تعداد |
|---|---|---|
| **P1** | با ارزش · بحرانی | ۱۹۱ |
| P2 | با ارزش · عادی | ۴۶ |
| P3 | کم‌ارزش · بحرانی | ۳۲۱ |
| P4 | کم‌ارزش · غیربحرانی | ۶۵ |
| | **کل مشتریان دارای مشکل** | **۶۲۳** |

مرتب‌سازی: طبقه، سپس **پول در خطر** نزولی. سهمیهٔ نمایش ۴۰/۲۰/۲۰/۱۰ = ۹۰ سطر —
بدون سهمیه، ۱۹۱ سطر P1 کل جدول را می‌گرفت و طبقه‌های پایین‌تر هرگز دیده نمی‌شدند.
شمارش کامل هر طبقه بالای جدول نمایش داده می‌شود تا سهمیه، حقیقت را پنهان نکند.

**ستون «چرا اینجاست»** برای هر سطر یک جمله می‌سازد: چرا در این طبقهٔ ارزش است
(با رتبهٔ LTV یا رتبهٔ فروش)، چرا مشکلش بحرانی است، و چه رویدادی در همین دوره
رخ داده. اگر مشتری با رتبهٔ فروش وارد شده ولی رتبهٔ LTV بدی دارد، جمله همین را
می‌گوید: «مشتری بزرگ سبد است … هرچند رتبهٔ LTV آن ۶۲۱ است — یعنی معوق، سود این
رابطه را خورده.»

## ۳۰.۹ پول در خطر — یک فرمول، چهار خانواده

```
پول در خطر = مبلغ در معرض × احتمال تحقق
```

| خانوادهٔ ریسک | مبلغ در معرض | احتمال تحقق | منبع عدد |
|---|---|---|---|
| اعتباری | معوق | ۱ − نرخ بازیابی تجربی همین طول دوره | اندازه‌گیری روی ۴ پنجرهٔ تاریخی |
| ریزشی | سود ماهانه × ۱۲ | ۱ − احتمال ماندگاری | مدل کالیبره‌شدهٔ بخش ۱۰ |
| حاشیه‌ای | زیان خطوط زیان‌ده | **۱** — رخ داده | محاسبهٔ سطح خط |
| کیفی | سود ماهانه × ۱۲ | احتمال شکایت بعدی | جدول نرخ پایهٔ بخش ۱۳.۳ |
| رابطه‌ای | سود ماهانه × ۱۲ | ۰٫۲۵ (اثر غیرمستقیم) | ضریب محافظه‌کارانهٔ اعلام‌شده |

**چرا این شکل؟** جمع کردن اعداد ناهم‌جنس بی‌معناست. «معوق ۱۰۰ میلیون» و «سود
از‌دست‌رفتنی ۱۰۰ میلیون» یک چیز نیستند. ضرب در احتمال تحقق، هر دو را به یک واحد
مشترک می‌آورد: **ریال مورد انتظار**. تنها ضریب غیرتجربی، ۰٫۲۵ خانوادهٔ رابطه‌ای
است و صریح به‌عنوان محافظه‌کارانه علامت خورده.

برای دورهٔ ۲ ماهه، مجموع پول در خطر سبد: **۴۰۵٫۳ میلیون**.

## ۳۰.۱۰ سه مشکل پرتکرار

گروه‌بندی همهٔ ۶۲۳ سطر رسیدگی بر **کد مشکل**، مرتب بر تعداد مشتری. هر باکس:
عنوان **عمومی** (بدون عدد مخصوص یک مشتری)، تعداد مشتری درگیر، تعداد مشتری با
**رویداد تازه در همین دوره**، پول در خطر با فرمول، مسئول اصلی، و پنج مشتری نمونه
با شاهد و رفرنس.

برای دورهٔ ۲ ماهه:

| مشکل | مشتری | رویداد تازه | پول در خطر |
|---|---|---|---|
| مطالبات معوق بیشتر از سود ناخالص | ۲۴۱ | ۷۶ | ۲۲۹٫۹ م |
| معوق کهنهٔ بیش از یک سال | ۹۶ | ۳۰ | ۹۰٫۳ م |
| مشتری راکد (بیش از ۱۸۰ روز بی‌خرید) | ۹۰ | ۰ | ۳۷٫۷ م |

تفکیک «تعداد مشتری» از «رویداد تازه» عمدی است: مشکل راکدی ۹۰ مشتری دارد ولی هیچ‌کدام
در همین دوره تازه راکد نشده‌اند — یعنی مشکلی انباشته است، نه شوک این دوره.

---

# ۳۱. تب خلاصه و دستیار شناور

## ۳۱.۱ تب خلاصه — تب اول و پیش‌فرض

**نوار دوره.** هفت دکمه؛ روی دکمهٔ منتخبِ داده برچسب سبز «پیشنهادی» می‌نشیند و
جملهٔ دلیلش زیر نوار می‌آید. بالای نوار، لنگر دوره و هشدار ماه ناقص.

**۱. خلاصهٔ دوره (دورهٔ انتخاب‌شده).** چهار باکس:

| باکس | مقدار اصلی | جزئیات |
|---|---|---|
| مقدار فروش | فروش اسمی دوره | فروش حقیقی، حجم، تعداد فاکتور |
| سود ناخالص | سود دوره | حاشیهٔ دوره در برابر میانهٔ سبد |
| **مطالبات** | **معوق کل در پایان دوره** | دو خانهٔ جدا: **وصول‌شده در این دوره** و سررسیدنشده |
| شکایات | تعداد شکایت دوره | تعداد مشتری شاکی و تعداد با شدت زیاد/بحرانی |

**۲. مقایسه با دورهٔ قبل.** یازده سطر — همان چهار متریک به‌علاوهٔ حاشیه، حجم،
سررسیدنشده، شکایت بحرانی، مشتری فعال و فاکتور — با ستون‌های «این دوره»، «دورهٔ
قبل» و «تغییر». دورهٔ قبل **هم‌طول و بدون همپوشانی** است (هفته با هفتهٔ قبل، دو ماه
با دو ماه قبل).

دو ظرافت در نشانهٔ تغییر:

- برای معوق، سررسیدنشده و شکایت، **بالا رفتن خبر بد است**؛ رنگ برعکس می‌شود
  (کلاس `.delta.inv`).
- برای حاشیه که خودش درصد است، تفاوت به **واحد درصد** نوشته می‌شود نه درصدِ درصد.

**۳. پیش‌بینی دورهٔ بعد.** همان چهار باکس با قاب خط‌چین، هر یک با **خطای بک‌تست
خودش**. باکس مطالبات دو خانه دارد: **معوق کل پایان دورهٔ بعد** و **وصول
پیش‌بینی‌شدهٔ دورهٔ بعد**. زیر باکس‌ها، کارت روش: فرمول‌ها با اعداد زنده و جملهٔ
اعتبارسنجی خارج از نمونه.

**۴. رسیدگی.** شمارش چهار طبقه و مجموع پول در خطر، فیلتر اولویت، جست‌وجوی متنی،
و جدول هشت‌ستونی: ردیف · مشتری · اولویت · RFM · LTV · مشکل · پول در خطر ·
**چرا اینجاست**. کلیک روی هر سطر، کشوی جزئیات را باز می‌کند.

**۵. سه مشکل پرتکرار.** سه باکس عمودی کنار هم با نوار رنگی بالا (قرمز/نارنجی/زرد)،
تعداد مشتری، رویداد تازه، پول در خطر و میانگین هر مشتری، و دکمهٔ «نمایش بیشتر» که
فرمول، استدلال، اقدام نمونه، مسئول و پنج مشتری نمونهٔ قابل‌کلیک را باز می‌کند.

## ۳۱.۲ کشوی جزئیات رسیدگی

کشویی از سمت شروع صفحه با عرض حداکثر ۷۶۰ پیکسل — **نه تمام‌صفحه**، تا زمینه دیده
شود. محتوا به همان ترتیبی که کاربر خواسته بود:

۱. **نام مشتری** در بالا، و کنارش برچسب‌ها: بخش، RFM با کد، LTV با رتبه، طبقهٔ
   اولویت و چهارخانه.
۲. **مشکل اصلی** در کادر قرمز هایلایت‌شده: عنوان، پول در خطر، حوزه، شاهد عددی،
   استدلال، «چرا در این اولویت»، و **رویدادهای همین دوره با رفرنس**.
۳. **اقدام بعدی** — تا چهار اقدام با مسئول، مبلغ، استدلال، فرمول امتیاز و رفرنس.
۴. **سیگنال‌ها و تفسیر** — فیلترشده به **حوزهٔ مشکل اصلی**. اگر آن حوزه سیگنالی
   نداشت، سه سیگنال مهم‌تر مشتری با همین توضیح نمایش داده می‌شود. دکمهٔ «نمایش
   بیشتر (N سیگنال دیگر)» بقیه را باز می‌کند.
۵. **تصمیم‌های احتمالی دورهٔ بعد** — به‌صورت **جمع‌شده**؛ با کلیک باز می‌شود و همان
   نمودار واگرای تعدیل‌ها و رفرنس‌ها را نشان می‌دهد.

دکمهٔ «پروفایل کامل» کاربر را به تب پروفایل مشتری با همان مشتری انتخاب‌شده می‌برد.
بستن با دکمه، کلیک روی پس‌زمینه، یا `Esc`.

**تلهٔ راست‌به‌چپ که اینجا خوردیم:** کشو با `inset-inline-start:0` در RTL سمت راست
می‌نشیند، ولی `transform` جهت‌آگاه **نیست**. قاعدهٔ `html[dir="rtl"]{transform:
translateX(-105%)}` کشو را به‌جای بیرون بردن، به وسط صفحه می‌کشید. مقدار درست
`translateX(105%)` است، به‌علاوهٔ `visibility:hidden` در حالت بسته.

## ۳۱.۳ دستیار شناور

تب «دستیار هوشمند» حذف شد. جایش:

- **دکمهٔ شناور** در گوشهٔ پایین، روی **هر شش تب**.
- با کلیک، **پنل شناور** ۴۳۰ پیکسلی باز می‌شود — نه تمام‌صفحه — با دکمهٔ بزرگ‌نمایی
  به ۷۲۰ پیکسل برای پاسخ‌های بلند.
- نشان حالت دستیار (`gemini` یا `deterministic`) در سرصفحهٔ پنل.
- پرسش‌های نمونه پس از نخستین پرسش جمع می‌شوند تا گفت‌وگو جا داشته باشد؛ با
  دکمهٔ «پرسش‌های نمونه» برمی‌گردند.
- بستن با دکمه یا `Esc`. هنگام باز بودن پنل، دکمهٔ شناور پنهان می‌شود.

## ۳۱.۴ ابزار یازدهم دستیار

`period_summary(period)` به مجموعهٔ ابزارها اضافه شد — همان خروجی تب خلاصه به زبان
متن، با رفرنس. مسیریابی هم در Gemini (از طریق شرح ابزار و قاعدهٔ پرامپت سیستم) و هم
در حالت قطعی (کلیدواژه‌هایی مثل «این دوره»، «دورهٔ بعد»، «سراغ کدام مشتری»،
«پرتکرارترین مشکل») کار می‌کند و طول دوره را از خود پرسش استخراج می‌کند.

نمونه: «الان باید سراغ کدام مشتری‌ها بروم؟» → `period_summary(period="")` →
دورهٔ پیشنهادی. «پرتکرارترین مشکل مشتریان در سه ماه اخیر چیست؟» →
`period_summary(period="3m")`.

---

# ۳۲. هزینهٔ پول و حاشیهٔ واقعی

`money.py` — پاسخ به مرکزی‌ترین نکتهٔ راهنمای داوران: «حاشیهٔ ناخالص مشتریان شما را
از هم جدا نمی‌کند. پول رایگان نیست.»

## ۳۲.۱ مسئله در یک جدول

| | مبلغ | ٪ فروش |
|---|---|---|
| فروش اسمی | ۴٬۳۹۱٫۵ م | ۱۰۰٪ |
| سود ناخالص | ۴۴۳٫۲ م | ۱۰٫۰۹٪ |
| **هزینهٔ پول** | **−۴۶۸٫۶ م** | **−۱۰٫۶۷٪** |
| **سود واقعی** | **−۲۵٫۴ م** | **−۰٫۵۸٪** |
| ذخیرهٔ مطالبات مشکوک‌الوصول | −۱۶۷٫۶ م | −۳٫۸۲٪ |
| سود خالص | −۱۹۳٫۰ م | −۴٫۳۹٪ |

هزینهٔ تأمین مالی همین مشتریان از کل سود ناخالص بیشتر است. مشتری زیان‌ده از **۵۷**
روی حاشیهٔ ناخالص به **۳۱۹** روی حاشیهٔ واقعی می‌رسد، دامنهٔ حاشیه از ۲۶٫۷ به
**۴۸٫۶ واحد** باز می‌شود، و **۴۱۴ مشتری بیش از ۵۰ پله** بین دو رتبه‌بندی جابه‌جا
می‌شوند.

## ۳۲.۲ روزهای پول قفل‌شده — اندازه‌گیری، نه فرض

راهنما فرمول «مهلت پرداخت + تأخیر مشاهده‌شده» را پیشنهاد می‌دهد. در این داده
`Payment_Terms_Days` برای **۵۲۶ از ۶۲۴ مشتری فعال صفر** ثبت شده، در حالی که شرط
اعتباری واقعی در سطح خط فروش (`payment_type`) وجود دارد. پس به‌جای اتکا به فیلد
مهلت، فاصلهٔ واقعی را اندازه گرفتیم:

```
روزهای پول قفل‌شده (میانگین وزنی به مبلغ) =
    بخش وصول‌شده  → فاصلهٔ تاریخ فاکتور تا تاریخ وصول
    بخش باز       → فاصلهٔ تاریخ فاکتور تا تاریخ برش    ← کف واقعی
هزینهٔ پول٪ = نرخ ماهانه × روزها ÷ ۳۰
```

نتیجه به تفکیک شرط پرداخت — دقیقاً همان چیزی که فرمول راهنما می‌سازد، ولی از داده:

| شرط پرداخت | مهلت ثبت‌شده | **روز تا نقد** | تأخیر میانه | هزینهٔ پول |
|---|---|---|---|---|
| نقدی یا پیش‌پرداخت | ۰ | **۲۳** | ۲۳ | ۳٫۱٪ |
| کوتاه‌مدت | ۳۰ | **۵۲** | ۲۳ | ۶٫۹٪ |
| تعمیم‌یافته | ۴۵ | **۶۰** | ۱۸ | ۸٫۰٪ |
| بلندمدت | ۹۰ | **۱۱۳** | ۲۳ | **۱۵٫۱٪** |

بین نقدی و بلندمدت **۱۲ واحد درصد** اختلاف هزینهٔ پول است — در سبدی که میانهٔ
حاشیهٔ ناخالصش ۸٫۹۱٪ است.

## ۳۲.۳ آزمون قیمت‌گذاری اعتبار

راهنما صریح می‌خواهد: «قیمت واحد همان کالا در همان ماه را بین شرایط پرداخت مقایسه
کنید و ببینید مارک‌آپ واقعاً هست یا نه. بگویید کدام یک را یافتید.»

`credit_pricing_test()` همین کار را می‌کند: میانگین وزنی به مقدارِ قیمت واحد، برای
هر **کد کالا × ماه**، بین شرط‌های پرداخت.

| شرط | مارک‌آپ مورد انتظار | میانه | وزنی به مقدار | t | سلول |
|---|---|---|---|---|---|
| ۳۰ روزه | ۴٫۰٪ | −۰٫۲۷٪ | ۱٫۸۴٪ | ۵٫۰ | ۱٬۷۱۴ |
| **۹۰ روزه** | **۱۲٫۰٪** | −۱٫۸۶٪ | **۲٫۰۳٪** | **۱٫۵** | ۱٬۵۰۳ |

**نتیجه: اعتبار در قیمت لحاظ نشده است.** برای شرط ۹۰ روزه اثر حتی از نظر آماری
معنادار نیست. پس اعمال هزینهٔ پول **دوباره‌شماری نیست** — این هزینه جایی جبران نشده.
همین آزمون در تب «فهرست تمرکز» روی صفحه نمایش داده می‌شود.

## ۳۲.۴ ذخیرهٔ مطالبات مشکوک‌الوصول

راهنما می‌گوید «مطالبات سوخت‌شده هنوز وارد نشده». منحنی وصول تجربی همین داده:

| سن فاکتور | سهم وصول‌شده | تعداد فاکتور |
|---|---|---|
| ۰ تا ۶۰ روز | ۴۵٫۲٪ | ۲۳۱ |
| ۶۱ تا ۱۲۰ روز | ۸۴٫۵٪ | ۶۵۲ |
| ۱۲۱ تا ۱۸۰ روز | ۸۷٫۳٪ | ۴۶۲ |
| ۱۸۱ تا ۳۶۵ روز | ۸۶٫۵٪ | ۳٬۱۵۹ |
| ۳۶۶ تا ۷۳۰ روز | ۸۷٫۰٪ | ۶٬۶۵۸ |
| بیش از ۷۳۰ روز | ۸۶٫۶٪ | ۳٬۲۲۱ |

منحنی از ۱۲۰ روز به بعد **تخت** می‌شود و با گذر زمان بهتر نمی‌شود: حدود **۱۳٪** ارزش
فاکتورشده هرگز وصول نمی‌شود. این جزء **جدا** از هزینهٔ پول نگه داشته می‌شود تا با
فرمول راهنما قاطی نشود، و در UI هم جدا نمایش داده می‌شود.

## ۳۲.۵ نرخ ۴٪ یک پارامتر است، نه یافته

این تنها عدد کل پروژه است که **از داده در نمی‌آید**؛ فرض کسب‌وکاری راهنمای داوران
است. پس:

- در `money.py` ثابت `FINANCE_RATE_MONTHLY` و پارامتر همهٔ توابع است.
- در داشبورد **لغزنده** است و چون هزینهٔ پول در نرخ **خطی** است، کل رتبه‌بندی با
  روزهای ازپیش‌محاسبه‌شده درجا و بدون رفت‌وبرگشت با سرور بازمحاسبه می‌شود.
- در ۰٪ سود واقعی همان ۴۴۳ میلیون است؛ در ۸٪ به −۴۹۴ میلیون می‌رسد.
- در پرامپت سیستم دستیار به‌عنوان چهارمین هشدار دادهٔ اجباری قفل شده است.

آنچه به نرخ وابسته **نیست**: پول ۵۴ روز قفل می‌ماند، و مارک‌آپ اعتبار در قیمت ثبت
نشده. این دو اندازه‌گیری‌اند.

## ۳۲.۶ اثر بر بقیهٔ سامانه

| جای اثر | تغییر |
|---|---|
| بُعد **M** در RFM | از فروش حقیقی به **سود واقعی پس از هزینهٔ پول** — دام ۱ راهنما |
| ریسک‌ها | دو قاعدهٔ تازه: `negative_real_margin` و `high_cost_of_money` |
| فرصت‌ها | قاعدهٔ تازه: `terms_renegotiation` — بزرگ‌ترین اهرم این سبد |
| فهرست رسیدگی تب خلاصه | «سود واقعی منفی» حالا دومین مشکل پرتکرار سبد است |
| جدول مشتریان | سه ستون تازه: هزینهٔ پول، حاشیهٔ واقعی، پول در حرکت |
| پنل کامرشال پروفایل | روز پول قفل‌شده، هزینهٔ پول، سود واقعی، و جابه‌جایی رتبه |
| دستیار | ابزار دوازدهم `focus_list` و یافتهٔ اصلی در پرامپت سیستم |

---

# ۳۳. فهرست تمرکز — کدام حساب‌ها توجه کمتری بگیرند

راهنما این را «تصمیم ناراحت‌کننده» می‌نامد: «محصولی که فقط می‌گوید به همه زنگ بزنید
بی‌فایده است. به مدیر فروش راهی قابل دفاع بدهید که فهرست را کوتاه کند.»

## ۳۳.۱ دو محور

```
محور سود   : سود واقعی ≥ میانهٔ سبد ؟                    ← نه فروش، نه حاشیهٔ ناخالص
محور ظرفیت : ظرفیت رشد ≥ میانهٔ سبد ؟
محور سوم   : هزینهٔ خدمت‌دهی (فقط برای شاهد «کاهش بده»)
```

**ظرفیت رشد** با حاشیهٔ **قابل دستیابی** حساب می‌شود، نه حاشیهٔ خرابِ فعلی — وگرنه
هر حساب زیان‌ده به‌طور خودکار «بدون ظرفیت» می‌شد و خانهٔ «اصلاح کن» همیشه خالی
می‌ماند. مبنا: میانهٔ حاشیهٔ ناخالص منهای هزینهٔ پول در مبنای نقدی سبد = **۶٫۸۳٪**.

## ۳۳.۲ چهار خانه

| خانه | مشتری | سود ناخالص | سود واقعی | پول در حرکت | اقدام |
|---|---|---|---|---|---|
| **رشد بده** | ۹۴ | ۷٫۸ م | ۳٫۹ م | ۴٫۱ م | بهترین نیروها اینجا؛ فروش مکمل و سهم سبد |
| **حفظ کن** | ۲۱۸ | ۲۷۸٫۸ م | ۹۵٫۰ م | ۸۵٫۴ م | تلاش کم، مراقبت زیاد؛ فقط رصد هشدار زودهنگام |
| **اصلاح کن** | ۶۲ | ۱۰٫۰ م | −۷٫۳ م | ۱۵٫۱ م | بازمذاکرهٔ قیمت یا شرایط، با مهلت مشخص |
| **کاهش بده** | ۲۵۰ | ۱۴۶٫۷ م | **−۱۱۷٫۰ م** | ۱۳۸٫۰ م | تماس کم‌بسامد یا خودخدمتی؛ توقف پیگیری فعال |

خانهٔ «کاهش بده» ۱۴۶٫۷ میلیون سود ناخالص نشان می‌دهد و **−۱۱۷٫۰ میلیون** سود واقعی.
این تفاوت، کل استدلال محصول است.

## ۳۳.۳ برش: از ۶۴۴ به ۴۰

```
پول در حرکت = بیشینهٔ سه مسیر
    نگهداشت  : سود واقعی سالانه × (۱ − احتمال ماندگاری)
    رشد      : ظرفیت رشد سالانه
    اصلاح    : فروش سالانه × نرخ ماهانه × (روزهای فعلی − مبنای نقدی) ÷ ۳۰
```

سالانه‌سازی برای مشتری تازه به کل مقدار مشاهده‌شده محدود می‌شود تا اغراق نکند.

**نتیجه:** ۴۰ حساب که **۶۴٫۶٪ فروش** و **۱۴۹٫۳ از ۲۴۲٫۶ میلیون** پول در حرکت را
پوشش می‌دهند. مسیر غالب در این سبد **اصلاح شرایط پرداخت** است (۴۳۴ حساب)، نه رشد
حجم (۴۷ حساب) — یافته‌ای که بدون هزینهٔ پول اصلاً دیده نمی‌شد.

## ۳۳.۴ هر حذف دلیل دارد

| گروه حذف‌شده | مشتری | سود واقعی | دلیل و شرط بازگشت |
|---|---|---|---|
| کاهش بده | ۲۲۴ | −۴۶٫۹ م | سود واقعی زیر میانه و ظرفیت کم. **نظرمان عوض می‌شود اگر** شرایط پرداخت به نقدی برود، یا برآورد سهم سبد با منبع معتبرتری به‌روز شود |
| حفظ کن | ۲۰۵ | +۴۵٫۳ م | سودآور ولی بدون ظرفیت باز؛ تماس کم‌بسامد کافی است. **عوض می‌شود اگر** نشانهٔ ریزش ببینیم |
| رشد بده | ۹۴ | +۳٫۹ م | پول در حرکتشان زیر آستانهٔ فهرست است |
| اصلاح کن | ۶۱ | −۵٫۴ م | همان، با اولویت پایین‌تر |

## ۳۳.۵ حرکت RFM — «ارزشمندترین هشدار کل سامانه»

راهنما: «امتیاز امروز از تغییرش نسبت به فصل گذشته کم‌ارزش‌تر است.»

`add_rfm_movement()` امتیاز را در تاریخ برش و **۹۱ روز پیش از آن** مستقل محاسبه
می‌کند و اختلاف را گزارش می‌دهد:

| برچسب | مشتری |
|---|---|
| افت شدید (۳ پله یا بیشتر) | ۱۲ |
| افت | ۱۲۹ |
| بدون تغییر | ۳۶۹ |
| رشد | ۸۰ |
| رشد قوی | ۱۷ |
| بدون سابقهٔ مقایسه | ۱۷ |

**۱۹ هشدار طلایی**: مشتری با بُعد پولی ۴ یا ۵ که ۲ پله یا بیشتر افت کرده. نمونهٔ
شاخص: **C_535756 از ۵-۵-۵ به ۴-۵-۱** با ۱۶۶٫۸ میلیون فروش — دقیقاً همان مثالی که
راهنما می‌زند. این هشدار به‌صورت ریسک `rfm_drop` وارد موتور اقدام می‌شود.

---

# ۳۴. اعتبارسنجی دستی و نگاشت به راهنمای داوران

## ۳۴.۱ «سه حساب را با دست حساب کنید»

`validate_cases.py` سه حساب با سه پروفایل متفاوت انتخاب می‌کند و **ده عدد کلیدی** هر
کدام را مستقل از موتور محصول و مستقیم از شیت خام دوباره محاسبه می‌کند:

| حساب | نقش | خط فروش | فاکتور | نتیجه |
|---|---|---|---|---|
| C_937594 | بزرگ‌ترین حساب سبد | ۶۳۱ | ۳۷۰ | **۱۰/۱۰ منطبق** |
| C_242469 | گران‌ترین از نظر هزینهٔ پول (۸۹۹ روز) | ۱۵ | ۱ | **۱۰/۱۰ منطبق** |
| C_944253 | حساب سالم با سود واقعی مثبت | ۱۶ | ۶ | **۱۰/۱۰ منطبق** |

هر سطر می‌گوید **چگونه با دست حساب شد** — مثلاً «میانگین وزنی: بخش وصول‌شده با
فاصلهٔ فاکتور تا وصول، بخش باز با فاصله تا تاریخ برش». آستانهٔ تحمل نیم درصد
(گردکردن). جدول کامل در تب «الگوها، مدل و اعتبارسنجی» است — همان «یک کلیک فاصله»
که راهنما می‌خواهد.

## ۳۴.۲ نگاشت کامل به راهنمای داوران

| خواستهٔ راهنما | کجا پیاده شده |
|---|---|
| Customer 360 یکپارچه | `pipeline.build_profiles` + تاریخچهٔ یکپارچهٔ سه‌منبعی در تب پروفایل |
| سیگنال جدا از نویز | ۱۸ قاعده و ۲۱ کد سیگنال، مرتب بر جهت و شدت (بخش ۱۴) |
| تفسیر، نه فقط «چه شد» | هر سیگنال جملهٔ «چرا این مهم است» دارد؛ هر ریسک شاهد و استدلال جدا |
| اقدام بعدی مشخص، نه «با مشتری تماس بگیرید» | ۷ نگاشت دلیل آفر، ۶ نگاشت نوع درخواست، ۴ نگاشت شدت شکایت (بخش ۱۷) |
| شاهد و اعتماد | ۱۷٬۶۳۶ رفرنس؛ صفر ادعای بی‌منبع؛ فرض‌ها و عدم‌قطعیت روی صفحه |
| زمینهٔ تجاری، نه فقط حجم | **هزینهٔ پول، حاشیهٔ واقعی، ذخیرهٔ مطالبات** (بخش ۳۲) |
| **RFM: M روی سود، نه فروش** | بُعد پولی روی سود واقعی پس از هزینهٔ پول |
| **RFM: حرکت، نه سطح** | `add_rfm_movement` با ۱۹ هشدار طلایی (بخش ۳۳.۵) |
| **RFM: ریتم B2B هر حساب** | `silence_ratio` — رکود نسبت به فاصلهٔ معمول **خود** مشتری |
| **هزینهٔ پول ۴٪ ماهانه** | `money.py`، با لغزندهٔ نرخ در داشبورد |
| **آزمون مارک‌آپ اعتبار** | `credit_pricing_test` — نتیجه: قیمت‌گذاری نشده (بخش ۳۲.۳) |
| **کدام حساب‌ها توجه کمتر** | چهار خانه + برش ۶۴۴→۴۰ + دلیل هر حذف (بخش ۳۳) |
| **دام: چند-به-چند فاکتور** | جدول سطح فاکتور پیش از اتصال (بخش ۴) |
| **دام: زمان در دسترس بودن** | موتور as-of با راست‌سانسور (بخش ۵) |
| **دام: دو مبنای بهای تمام‌شده** | پرچم `cost_basis`، سهم ۳۲٪ تحقق‌یافته روی صفحه |
| **دام: نسخهٔ رکورد CRM** | فقط آخرین نسخه، زمان مؤثر = بیشینهٔ دو تاریخ |
| **دام: برآورد بودن سهم سبد** | منبع برآورد در رفرنس؛ متن «عدد را سقف بدانید» |
| **آزمون دستی سه حساب** | `validate_cases.py` — ۳۰/۳۰ منطبق (بخش ۳۴.۱) |
| مدل زبانی حساب نکند | ۱۲ ابزار؛ هیچ عددی از مدل نمی‌آید (بخش ۲۳) |
| گزارش آنچه کار نکرد | سه الگوی ردشده + اعتراف کارت مدل (بخش ۲۰) |

## ۳۴.۳ سه پرسشی که راهنما گفت تمرین کنید

**«چرا این مشتری بر آن یکی اولویت دارد؟»** چون پول در حرکتش بیشتر است، و مسیرش
(نگهداشت / رشد / اصلاح شرایط) روی صفحه نوشته شده. فرمول در بخش ۳۳.۳.

**«چه داده و منطقی این پیشنهاد را پشتیبانی می‌کند و چه چیزی آن را غلط می‌کند؟»**
هر اقدام رفرنس رکورد منبع دارد؛ و برای «کاهش بده» صریح نوشته‌ایم چه چیزی نظرمان را
عوض می‌کند. غلط‌کنندهٔ اصلی: نرخ ۴٪ — لغزنده‌اش روی صفحه است.

**«اگر مالک حساب مخالف باشد، چه چیزی را می‌تواند بررسی کند؟»** رفرنس هر ادعا،
تاریخچهٔ یکپارچه، متن کامل پروفایلی که به مدل داده می‌شود، جدول آزمون دستی، و
لغزندهٔ نرخ برای دیدن حساسیت رتبه‌بندی.

---

# ۳۵. کارتابل — کار امروز، دستور کار تماس و جلسه

تب اول داشبورد. پاسخ به تنها پرسشی که کارشناس فروش صبح دوشنبه دارد: **الان دقیقاً
چه کاری باید انجام دهم و با چه کسی؟**

## ۳۵.۱ کارت‌های مشکل

بالای صفحه، مشتریان بر خانوادهٔ مشکلشان دسته می‌شوند؛ هر خانواده یک کارت، مرتب از
پرتعدادترین به کم‌تعدادترین:

| خانواده | مشتری درگیر | مشکل اصلی همین است | اولویت اول | هدف ارتباط |
|---|---|---|---|---|
| ریزش | ۴۹۳ | ۱۶۸ | ۸۵ | بازگرداندن جریان سفارش |
| رابطه | ۴۵۱ | ۲ | ۷۶ | بازسازی رابطه و گسترش سبد |
| سودآوری | ۴۳۹ | ۱۵۰ | ۱۰۲ | اصلاح شرایط پرداخت یا قیمت |
| وصول | ۴۱۱ | ۲۴۴ | ۱۱۴ | وصول مطالبات معوق |
| رقابت | ۲۸۵ | ۱۹ | ۵۹ | پس‌گرفتن سهم از رقیب |
| کیفیت | ۴۰ | ۴۰ | ۲۸ | بستن پروندهٔ کیفی |

**چرا جمع ستون اول از ۶۲۳ بیشتر است:** یک مشتری می‌تواند هم‌زمان مشکل وصول و کیفیت
داشته باشد، و کارت «۴۱۱ وصول» یعنی «۴۱۱ مشتری مشکل وصول دارند» — نه «۴۱۱ مشتری فقط
مشکل وصول دارند». ستون دوم نشان می‌دهد برای چند نفر این **مشکل اصلی** است.

**یک قاعدهٔ ترتیب:** اگر مشتری پروندهٔ کیفی باز با شدت بحرانی یا زیاد دارد، مشکل
اصلی‌اش «کیفیت» است، حتی اگر ریسک پولی‌تری بالاتر نشسته باشد. پروندهٔ کیفی باز هر
گفت‌وگوی دیگری را مسدود می‌کند.

## ۳۵.۲ سه ستون هر سطر

مشتریان با **لیبل اولویت** مرتب می‌شوند (P1 تا P4)، و هر سطر فقط سه ستون دارد:

| ستون | چه می‌گوید | از کجا |
|---|---|---|
| **هدف از ارتباط** | هدف خانوادهٔ مشکل + مبلغی که در گیر است | خانوادهٔ ریسک اصلی و بیشینهٔ مبلغ در معرض یا پول در حرکت |
| **مشتری کیست** | بخش، رتبهٔ فروش، بخش RFM، ماه فعال، LTV، حاشیهٔ واقعی | مستر مشتری و ویژگی‌های محاسبه‌شده |
| **بهترین ارتباط** | کانال پیشنهادی و **چرا** | تقاطع «کانالی که نوع مشکل می‌طلبد» با «کانالی که تاریخاً با این مشتری استفاده شده» |

**کانال از داده می‌آید، نه از حدس.** فیلد `Next_Action` در CRM چهار کانال واقعی این
کسب‌وکار را نشان می‌دهد: پیگیری تلفنی (۱٬۱۴۰ رکورد)، بازدید فنی (۷۱۴)، جلسهٔ قیمت
(۶۷۳)، ارسال نمونه (۵۷۷). کانال پیشنهادی از نوع مشکل تعیین می‌شود و متن ستون
می‌گوید سابقهٔ خود مشتری چه بوده — اگر هم‌راستا بودند، همین را می‌گوید.

## ۳۵.۳ کارت مشتری

کلیک روی هر سطر، کارت کامل را باز می‌کند:

۱. **هدف** در کادر هایلایت‌شده، با مبلغ و شرح مشکل.
۲. **هشت کارت عدد**: مشکل باز · شکایت (باز/کل) · LTV و رتبه · RFM با حرکت
   (`۵۵۵ → ۴۵۱`) · حاشیهٔ واقعی و هزینهٔ پول · معوق و فروش · رکود و ماندگاری ·
   خانهٔ تمرکز و پول در حرکت.
۳. **کارها** — فقط عنوان، با مسئول زیر هر عنوان. بدون توضیح.
۴. **همین حالا** — یک اقدام، در کادر سبز، با مسئول و کانال. این اقدام همیشه با هدف
   همان سطر می‌خواند (آزمون‌شده در `verify.py`).
۵. دکمهٔ **دستور کار جلسه** یا **دستور کار تماس** — بسته به کانال.

## ۳۵.۴ دستور کار — از رکورد، نه از قالب

هر دستور کار پنج بخش دارد و **نکته‌هایش از دادهٔ همان مشتری** استخراج می‌شود:

| بخش | منبع |
|---|---|
| شروع گفت‌وگو | جملهٔ افتتاحیه بر پایهٔ خانوادهٔ مشکل |
| **چه چیزهایی را مطرح کنید** | شمارهٔ فاکتور معوق و قدیمی‌ترین روز · عنوان و شدت شکایت باز · شناسه و دلیل و تخفیف آفر بی‌پاسخ · نوع درخواست فنی بی‌پاسخ · شکاف سهم سبد و نام رقیب · روزهای بی‌سفارش · چرخهٔ نقد و هزینهٔ پول |
| با چه چیزی ببندید | تعهد قابل اندازه‌گیری متناسب با خانوادهٔ مشکل |
| **رفتار و لحن** | از لیبل اولویت: P1 حضور مدیر فروش و شنیدن پیش از عدد؛ P4 پیگیری مکتوب و بدون مذاکره |
| ثبت | یادآوری ثبت تعهد و تاریخ در CRM |

نمونهٔ واقعی برای C_245948 (اولویت اول، مشکل کیفیت، کانال بازدید فنی):

> **شروع:** «دربارهٔ پروندهٔ کیفی باز تماس گرفتم و می‌خواهم تکلیفش را روشن کنم»
> **مطرح کنید:** مانده معوق ۲۳٫۷ میلیون — فاکتور T_837322 و T_262926، قدیمی‌ترین
> ۸۱۶ روز · شکایت باز «کریمپ / حجم ناهمگون» با شدت بحرانی · آفر باز OFF-0000434 با
> دلیل افزایش سهم از سبد و تخفیف ۷٫۸٪
> **ببندید با:** مالک رسیدگی و مهلت پاسخ، و تاریخ اعلام نتیجه به مشتری
> **لحن:** مدیر فروش شخصاً برود؛ اول گوش کنید و یادداشت بردارید؛ عدد را وسط جلسه
> بیاورید نه در دقیقهٔ اول

**مرز صریح:** جملهٔ افتتاحیه، شیوهٔ بستن و توصیهٔ رفتاری **از داده در نمی‌آیند** —
قاعدهٔ ارتباط مؤثر‌اند و به لیبل اولویت و خانوادهٔ مشکل گره خورده‌اند. نکته‌های
گفت‌وگو همه از رکورد می‌آیند و رفرنس دارند.

## ۳۵.۵ مینیمال‌سازی داشبورد

سه تغییر هم‌زمان:

۱. **تب خلاصه و فهرست تمرکز ادغام شدند** به یک صفحه: دوره، چهار باکس، مقایسه،
   پیش‌بینی، هزینهٔ پول، چهار خانهٔ تمرکز، برش ۶۴۴→۴۰، حذف‌شده‌ها، سه مشکل پرتکرار.
۲. **جدول رسیدگی حذف شد** — کارتابل جای آن را با سه ستون عملیاتی و دستور کار گرفت.
۳. **همهٔ توضیح‌های صفحه جمع شدند.** تابع `collapseNotes()` پس از هر رندر، هر
   `.hint` و `.caveat` را داخل یک `<details class="note">` با عنوان «؟ توضیح»
   می‌گذارد. صفحه به‌طور پیش‌فرض فقط **عدد و اقدام** نشان می‌دهد؛ استدلال و روش یک
   کلیک فاصله دارد — نه حذف شده، نه جلوی چشم.

## ۳۵.۶ ابزار سیزدهم دستیار

`work_card(customer_id, family, limit)` — بدون شناسه، کارت‌های مشکل و سطرهای اول
کارتابل؛ با شناسه، کارت کامل به‌علاوهٔ دستور کار تماس یا جلسه با همان نکته‌های
رکوردی و رفرنس. مسیریابی حالت قطعی روی کلیدواژه‌های «چه کار»، «جلسه»، «تماس»،
«دستور کار»، «کارتابل» و «امروز» کار می‌کند.

---

# ۳۶. موتور آفر — انتخاب و زمان‌بندی، نه اقناع

فایل: `offer_engine.py` · تب «آفر» · ابزار چهاردهم `offer_plan`.

پرسش این تب: **به چه مشتری، چه آفری، با چه تخفیفی، با چه مهلتی — و چرا؟**
شرط طراحی این بود که پاسخ هر چهار پرسش از داده بیاید و روی صفحه قابل اثبات باشد.

## ۳۶.۱ پنج فرضیه را آزمودیم؛ چهارتا رد شد

۲٬۵۰۰ آفر تاریخی این شرکت در شیت «آفرها» ثبت شده: ۶۵۱ پذیرفته، ۶۶۲ رد، ۶۴۲
منقضی، ۵۴۵ بی‌پاسخ. نرخ پذیرش میان آفرهای **تصمیم‌گرفته‌شده** ۴۹٫۵۸٪ است
(۱٬۳۱۳ مورد). روی همین پایه پنج فرضیهٔ رایج را آزمودیم:

| فرضیه | آزمون | نتیجه | حکم |
|---|---|---|---|
| عمق تخفیف پذیرش را بالا می‌برد | χ² روی پنجک تخفیف | χ²=۱٫۱، df=۴، **p=۰٫۹۰** | رد |
| دلیل آفر مهم است | χ² روی ۱۳ دلیل | χ²=۱۰٫۶، df=۱۲، **p=۰٫۵۶** | رد |
| نوع آفر مهم است | χ² روی حجمی/قیمتی/مدت‌دار | χ²=۴٫۰، df=۲، **p=۰٫۱۴** | رد |
| مشتریِ قبلاً پذیرنده باز می‌پذیرد | χ² روی نرخ پذیرش گذشتهٔ خودش | χ²=۳٫۳، df=۳، **p=۰٫۳۵** | رد |
| **طول اعتبار آفر مهم است** | χ² روی چهار باند مهلت | **χ²=۳۴٫۴، df=۳، p<۰٫۰۰۰۱** | **ماند** |

نکتهٔ عمق تخفیف را مستقیم هم می‌شود دید: **میانهٔ تخفیف آفر پذیرفته‌شده ۴٫۷۴٪ و
آفر ردشده ۴٫۸۶٪** است. همبستگی نقطه‌ای−دورشته‌ای بین عمق تخفیف و پذیرش −۰٫۰۲
است. تخفیف عمیق‌تر چیزی نمی‌خرد؛ فقط حاشیه را می‌برد.

نرخ پذیرش گذشتهٔ خود مشتری فقط با **نگاه به عقب** ساخته شد (هر آفر تنها با
آفرهای پیش از خودش) تا نشتی زمانی رخ ندهد.

## ۳۶.۲ تنها اهرم: مهلت آفر

پذیرش بر حسب طول اعتبار، میان آفرهای تصمیم‌گرفته‌شده:

| باند مهلت | آفر تصمیم‌شده | پذیرش |
|---|---|---|
| ۴ تا ۷ روز | ۱۶۰ | ۵۳٫۱٪ |
| **۸ تا ۱۰ روز** | ۱۲۷ | **۶۰٫۶٪** |
| **۱۱ تا ۱۴ روز** | ۲۱۴ | **۶۳٫۱٪** |
| ۱۵ تا ۱۸ روز | ۱۹۸ | ۴۶٫۵٪ |
| ۱۹ تا ۲۱ روز | ۱۶۴ | ۴۲٫۷٪ |
| ۲۲ روز و بیشتر | ۴۵۰ | ۴۲٫۷٪ |

شکل، یک **U وارونه** است با اوج در ۱۱ تا ۱۴ روز: مهلت خیلی کوتاه فرصت تصمیم
نمی‌دهد، مهلت خیلی بلند فوریت را می‌کشد. جمع‌بندی: داخل پنجرهٔ ۸ تا ۱۴ روزه
**۶۲٫۲٪** (۳۴۱ آفر) در برابر **۴۵٫۲٪** بیرون از آن (۹۷۲ آفر).

سه بررسی که این را از یک هم‌بستگی اتفاقی جدا می‌کند:

۱. **در هر سه نوع آفر جداگانه تکرار می‌شود** — حجمی ۶۰٫۹٪ در برابر ۴۲٫۷٪،
   قیمتی ۶۱٫۵٪ در برابر ۴۳٫۶٪، مدت‌دار ۶۳٫۷٪ در برابر ۴۹٫۴٪.
۲. **با عمق تخفیف هم‌بسته نیست** — میانگین تخفیف در چهار باند بین ۴٫۶٪ و ۴٫۹٪
   است، یعنی باندهای برنده تخفیف بیشتری نداشته‌اند.
۳. **ترکیب نوع آفر در باندها یکسان است** (χ² باند×نوع، p=۰٫۶۷)، پس اثر از
   جابه‌جایی ترکیب نمی‌آید.

شرکت امروز **میانهٔ مهلت ۱۸ روز** می‌دهد و فقط **۲۶٫۱٪** آفرهایش داخل پنجرهٔ
درست است. بردن همهٔ آفرها به مهلت ۱۰ روزه — وسط پنجره — **۱٫۷ میلیون تومان**
ارزش انتظاری اضافه می‌کند، **بدون یک ریال تخفیف بیشتر**.

## ۳۶.۳ سیاست تخفیف: کمینهٔ قابل‌قبول، سقف از حاشیهٔ واقعی

چون عمق تخفیف پذیرش نمی‌خرد، بیشینه‌کردنش بی‌معناست. پس:

```
تخفیف پیشنهادی = min(۲٫۸۳٪ , سقف)
سقف            = max(0, min(حاشیهٔ واقعی − ۰ , ۸٫۵٪))
حاشیهٔ واقعی   = حاشیهٔ ناخالص − هزینهٔ پول        ← بخش ۱۲
```

- **۲٫۸۳٪** صدک ۲۵ تخفیفِ آفرهایی است که در گذشته پذیرفته شده‌اند. پایین‌تر از
  این، آفر در تاریخ این شرکت تقریباً دیده نشده.
- **۸٫۵٪** بیشترین تخفیف مشاهده‌شده در تاریخ آفرهاست؛ فراتر از آن برون‌یابی است.
- سقف از **حاشیهٔ واقعی پس از هزینهٔ پول** می‌آید، نه حاشیهٔ ناخالص. تخفیف دادن
  به مشتری‌ای که پول را ۱۱۳ روز نگه می‌دارد گران‌تر از چیزی است که به‌نظر می‌رسد.

نتیجهٔ مستقیم این قاعده: **۵۰۸ آفر برای ۳۷۰ مشتری مسدود می‌شود** — حاشیهٔ
واقعی‌شان فضای تخفیف ندارد. مسیر این‌ها آفر نیست؛ **اصلاح شرایط پرداخت** است.
این عدد، همان تز مرکزی پروژه را از سمت دیگری تأیید می‌کند.

## ۳۶.۴ سه بازی، و ماشهٔ هرکدام

| بازی | ماشه (از داده) | آفر | در ازای | مشتری |
|---|---|---|---|---|
| بازگشت | ≥۶ ماه سابقه **و** (افت حجم ≤−۲۵٪ **یا** رکود > ۲× فاصلهٔ معمول سفارش خودش) | پلهٔ حجمی بازگشت | تعهد تناژ دورهٔ بعد، کتبی | ۵۳ |
| فروش مکمل | گروه کالایی که هم‌بخشی‌ها می‌خرند و او نمی‌خرد، و رکود ≤۱۸۰ روز | قیمت معرفی نخستین سفارش | یک سفارش آزمایشی + بازخورد فنی | ۸۷ |
| افزایش سهم | سهم ما از سبد او > ۵ واحد زیر میانهٔ بخش، و برآورد خرید کل موجود | پلهٔ قیمتی | جابه‌جایی نصف شکاف در دو دوره | ۱۳۳ |

فروش هدف هر بازی:

```
بازگشت      = فروش سالانه × clip(قدرمطلق افت حجم , ۰٫۱۵ , ۰٫۶۰)
فروش مکمل   = فروش سالانه × clip(سهم دومین گروه کالای خودش , ۰٫۰۸ , ۰٫۳۵)
افزایش سهم  = خرید کل برآوردی × (میانهٔ بخش − سهم ما) ÷ ۲ × ۱۲ ماه × قیمت واحد خودش
```

هیچ‌کدام ضریب دلخواه نیست: افت حجم از داده، سهم دومین گروه کالا از سبد خود
مشتری، و شکاف سهم از شیت «سهم_سبد». «نصف شکاف» تنها انتخاب محافظه‌کارانهٔ ماست
و روی صفحه نوشته می‌شود.

## ۳۶.۵ رتبه‌بندی و ارزش انتظاری

```
آوردهٔ هدف     = فروش هدف × (حاشیهٔ واقعی − تخفیف) ÷ ۱۰۰
ارزش انتظاری   = آوردهٔ هدف × ۰٫۶۲۱۷
```

چون احتمال پذیرش برای همهٔ مشتریان یکسان است (هیچ عامل مشتری‌محوری پیش‌بین
نبود)، رتبه‌بندی عملاً روی **اهمیت مشتری، سپس آوردهٔ هدف** انجام می‌شود — دقیقاً
همان دو معیاری که خواسته شده بود. ۰٫۶۲۱۷ نه یک مدل، بلکه **نرخ مشاهده‌شدهٔ
پنجرهٔ درست** است.

## ۳۶.۶ صداقت لازم: آوردهٔ آفر «هدف» است، نه لیفت

آزمون ششمی هم انجام دادیم که هیچ‌کس از ما نخواسته بود: **آیا پذیرش آفر به خرید
بیشتر منجر می‌شود؟** برای هر آفر بررسی کردیم که مشتری در ۳۰ روز پس از تصمیم،
همان گروه کالا را خریده یا نه:

| گروه | خرید همان گروه کالا در ۳۰ روز | n |
|---|---|---|
| آفر پذیرفته‌شده | ۷۰٫۲٪ | ۶۵۱ |
| آفر ردشده | ۶۹٫۶٪ | ۶۶۲ |

اختلاف ۰٫۶ واحد درصد، **Fisher p=۰٫۸۶** — یعنی در این داده **اثر افزایشی آفر
اندازه‌گیری نشده است**. پس ستون پول در تب آفر با برچسب **«آوردهٔ هدف در صورت
پذیرش»** نوشته می‌شود، نه «لیفت»، و همین قید زیر هر کارت آفر تکرار می‌شود. این
عدد سقفِ چیزی است که می‌خواهیم بگیریم و در جلسه تعهد کنیم — نه پیش‌بینی.

## ۳۶.۷ ماژول‌های تب آفر

۱. **پنجرهٔ آفر** — نمودار میله‌ای U وارونه با دو میلهٔ سبزِ پنجرهٔ برنده، به‌علاوهٔ
   جدول تفکیک بر نوع آفر که نشان می‌دهد اثر در هر سه نوع تکرار می‌شود.
۲. **آنچه آزمودیم** — هر شش آزمون با χ²، df، p و حکم، شامل آزمون رد‌شدهٔ لیفت.
۳. **شبیه‌ساز سیاست تخفیف** — لغزنده‌ای که تخفیف را از ۰ تا ۸٫۵٪ حرکت می‌دهد و
   درجا سه عدد را بازمحاسبه می‌کند. چون پذیرش در عمق تخفیف تخت است، ارزش
   انتظاری **همیشه** با افزایش تخفیف پایین می‌آید — این ادعا روی صفحه قابل
   آزمودن است، نه فقط قابل خواندن. شبیه‌ساز روی **هر ۲۷۳ آفر** حساب می‌کند، نه
   ۱۲۰ سطر نمایش‌داده‌شده، تا با عددهای بالای صفحه یکی باشد.
۴. **آفرهای مسدود** — ۵۰۸ موردی که فضای تخفیف ندارند، با حاشیهٔ واقعی، هزینهٔ
   پول و روزهای قفل هرکدام.

روی هر سطر که بزنید، کشوی دلیل باز می‌شود: چرا این آفر به این مشتری، با کدام
عدد، از کدام رکورد.

## ۳۶.۸ ابزار چهاردهم دستیار

`offer_plan(customer_id, play, limit)` — بدون شناسه، سرفصل پنجره، سه بازی و
آزمون‌ها؛ با شناسه، آفرهای همان مشتری با دلیل، سقف، مهلت و رفرنس. در پرامپت
دستیار صریح نوشته شده که **هرگز نگوید تخفیف بیشتر پذیرش را بالا می‌برد**، چون
روی این داده آزموده و رد شده است.

---

# ۳۷. لایهٔ ارجاع — کارتابل مدیر فروش و کارتابل کارشناس

فایل: `assignments.py` · انتخابگر «نقش» در سربرگ · تب «کارتابل کارشناس».

## ۳۷.۱ منطق پیشنهاد کارشناس: چه چیزی در داده هست و چه چیزی نیست

**هست — مالکیت.** هر ۶۲۴ مشتری در فایل اصلی دقیقاً یک `Sales_Rep_ID` دارند، و
**هر ۴٬۱۸۴ تعامل CRM را همان کارشناس مالک ثبت کرده است**؛ هیچ مشتری‌ای در کل
تاریخ داده دو کارشناس نداشته. پس «کارشناس مالک» یک واقعیت اندازه‌گیری‌شده است.

**هست — بار کاری.** تعامل ۹۰ روز اخیر بین ۲۴ و ۵۹ در نوسان است:

| کارشناس | مشتری | تعامل ۹۰ روزه | برچسب |
|---|---|---|---|
| REP-001 | ۷۱ | ۲۴ | سبک |
| REP-005 | ۸۱ | ۲۷ | سبک |
| REP-006 | ۷۴ | ۲۸ | متعادل |
| REP-002 | ۸۹ | ۳۵ | متعادل |
| REP-008 | ۷۷ | ۴۱ | متعادل |
| REP-004 | ۷۷ | ۴۳ | متعادل |
| REP-003 | ۹۰ | ۵۱ | پرکار |
| REP-007 | ۸۵ | ۵۹ | پرکار |

**نیست — تخصص.** ترکیب نوع تعامل در هر هشت کارشناس تقریباً یکسان است: سهم
«کیفیت محصول» بین ۱۲٫۱٪ و ۱۷٫۳٪ و سهم «وصول مطالبات» بین ۱۲٫۴٪ و ۱۷٫۶٪. در این
داده **کسی متخصص کیفیت یا متخصص وصول نیست**. اگر بنویسیم «این کار را به REP-004
بدهید چون متخصص کیفیت است»، عددی پشتش نیست — پس نمی‌نویسیم، و همین جمله را روی
خود پنل ارجاع اعلام می‌کنیم.

قاعدهٔ نهایی، به همین ترتیب:

| # | شرط | پیشنهاد | اطمینان |
|---|---|---|---|
| ۱ | پروندهٔ حساس: اولویت P1 **یا** شکایت باز | **کارشناس مالک** — تاریخچه نزد اوست | بالا |
| ۲ | بار ۹۰ روزهٔ مالک بالای صدک ۷۵ و کار عمومی | سبک‌ترین کارتابل، با ذکر هر دو عدد | متوسط |
| ۳ | باقی موارد | **کارشناس مالک** | بالا |

در هر سه حالت، **جایگزین** هم پیشنهاد می‌شود با دلیل خودش، و مدیر می‌تواند
انتخاب را تغییر دهد — تغییر دستی در پرونده ثبت و در «پیگیری کارشناسان» با
برچسب «انتخاب دستی مدیر» نمایش داده می‌شود.

## ۳۷.۲ چه کاری ارجاع‌پذیر است

فقط کار **تماسی**: `phone_followup` و `send_sample` (۷۲ از ۱۲۰ سطر کارتابل).
جلسهٔ قیمت، بازدید حضوری و بازدید فنی در کارتابل مدیر می‌ماند، چون تصمیم قیمت و
تعهد تجاری کار مدیر فروش است. دکمهٔ **«ارجاع به کارشناس»** فقط کنار
«دستور کار تماس» ظاهر می‌شود، نه کنار «دستور کار جلسه».

## ۳۷.۳ آنچه با ارجاع منتقل می‌شود

ارجاع، یک لینک خالی نیست؛ کل بستهٔ کار می‌رود: هدف و مبلغش، «مشتری کیست»، کانال
و دلیل انتخاب کانال، چهار کار با مسئولشان، «همین حالا»، **کل دستور کار تماس**
(شروع گفت‌وگو، نکته‌ها، جملهٔ بستن، رفتار و لحن)، اولویت، پول در گیر، و یادداشت
مدیر. کارشناس دقیقاً همان چیزی را می‌بیند که مدیر دیده — بدون بازنویسی.

## ۳۷.۴ قالب گزارش: نوع گزارش مشخص است

فرم گزارش کارشناس **پنج فیلد الزامی** دارد و بدون آن‌ها ثبت نمی‌شود
(اعتبارسنجی سمت سرور، نه فقط سمت مرورگر):

| فیلد | نوع | چرا الزامی است |
|---|---|---|
| نتیجهٔ تماس | چهار گزینه | بدون آن معلوم نیست تماس اصلاً برقرار شده |
| آنچه مشتری گفت | نقل قول کوتاه | تفسیر کارشناس جای حرف مشتری را نگیرد |
| عدد یا تعهد گرفته‌شده | متن | مبلغ، تاریخ، تناژ — یا صریحاً «تعهدی گرفته نشد» |
| مانع باقی‌مانده | یک جمله | چیزی که نگذاشت کار بسته شود |
| تصمیم پیشنهادی به مدیر | پنج گزینه | کارشناس پیشنهاد می‌دهد، مدیر تأیید یا تغییر |

فیلد اختیاری: تاریخ پیگیری بعدی (به تقویم جلالی تبدیل و ذخیره می‌شود).

هدف صریح روی فرم نوشته شده: «مدیر فروش باید بدون تماس دوباره بتواند تصمیم
بگیرد.» گزارش به‌صورت **فایل قابل دانلود** هم در دسترس است:
`GET /api/referrals/{id}/report.md`.

## ۳۷.۵ «پیگیری کارشناسان» در کارتابل مدیر

جدولی با دقیقاً چهار ستون خواسته‌شده:

| ستون | محتوا |
|---|---|
| نوع کار | خانوادهٔ مشکل، کانال، مشتری، هدف و اولویت |
| کدام کارشناس | نام، «پیشنهاد سامانه» یا «انتخاب دستی مدیر»، زمان ارجاع |
| فایل ریپورت کارشناس | خلاصهٔ چهار فیلد کلیدی + لینک دانلود فایل کامل |
| تصمیم بعدی | یکی از پنج تصمیم، با تاریخ چک بعدی |

پنج تصمیم: «مشکل حل شد — بسته شود»، «هفتهٔ بعد دوباره چک شود»، «خودم پیگیری
می‌کنم»، «جلسهٔ حضوری بگذاریم»، «ارجاع به کمیتهٔ اعتباری / کیفیت».

## ۳۷.۶ چرخهٔ وضعیت و ماندگاری

```
ارجاع‌شده → (کارشناس گزارش می‌دهد) → گزارش ارسال شد
          → (مدیر تصمیم می‌گیرد) → بسته شد   یا   در دست اقدام
```

حالت در `cache/assignments.json` ذخیره می‌شود و از بازسازی کش جان سالم به در
می‌برد — برخلاف بقیهٔ کش که از داده بازتولید می‌شود، این لایه **حافظهٔ اقدام**
است و باید بماند. `seed_referrals.py` سه ارجاع نمونه در سه وضعیت می‌سازد تا دمو
از صفر شروع نشود؛ متن گزارش‌های نمونه صریحاً برچسب «نمونهٔ دمو» دارند.

## ۳۷.۷ جداسازی نقش

انتخابگر «نقش» در سربرگ. در نقش کارشناس، همهٔ تب‌های مدیریتی پنهان می‌شوند و
فقط **کارتابل کارشناس** می‌ماند: چهار کاشی وضعیت (ارجاع باز، منتظر گزارش من،
بسته‌شده، پول در گیر) و فهرست ارجاعات **مرتب بر اهمیت مشتری**. کارشناس نه سبد
را می‌بیند، نه فهرست تمرکز، نه موتور آفر.

---

# ۳۸. مرکز سیگنال — سه منبع، و یک عدد پول که ادعای علّی نمی‌کند

فایل: `signal_center.py` · تب «مرکز سیگنال» · ابزار پانزدهم `signal_center`.

سه منبع سیگنال در این داده وجود دارد و هر سه با کلید مشتری قابل نگاشت‌اند:

| منبع | رکورد | مشتری | باز | شیت |
|---|---|---|---|---|
| شکایت | ۴۶۶ | ۱۴۲ | ۱۴۹ پروندهٔ باز | «شکایات» + پل «اتصال_شکایت» |
| درخواست توسعه | ۷۸۹ | ۲۰۱ | ۴۳۵ بی‌پاسخ | «درخواست_توسعه» |
| تعامل CRM | ۳٬۸۳۵ | ۶۲۴ | ۳٬۱۰۴ اقدام ثبت‌شده | «تعاملات_CRM» |

شکایت‌ها با شناسهٔ `CUST-xxx` ثبت می‌شوند و فروش با `C_xxxxxx`. پل، شیت
«اتصال_شکایت» است که هر شکایت را به `Sales_Line_ID` می‌بندد و از آنجا به مشتری
ERP می‌رسد. این نگاشت **یک‌به‌یک تمیز** است: هیچ‌کدام از ۱۴۹ شناسهٔ `CUST-x` به
بیش از یک مشتری ERP نمی‌خورد.

## ۳۸.۱ چرا این ماژول عدد «افزایش سود» نمی‌سازد

پرسش طبیعی این است: «رسیدگی به این شکایت چقدر فروش را بالا می‌برد؟» پیش از
ساختن هر فرمولی، آن را **آزمودیم**:

| ادعا | آزمون | نتیجه | p |
|---|---|---|---|
| رسیدگی، حجم ۹۰ روز بعد را بالا می‌برد | تفاضل‌در‌تفاضل در برابر روند سبد | میانهٔ ‎−۲۲٫۸ در برابر ‎−۱۴٫۰ واحد درصد | ۰٫۴۰ |
| رسیدگی سریع‌تر، اثر بیشتر | همبستگی رتبه‌ای سرعت رسیدگی با تغییر حجم | ρ=۰٫۰۴۴ | ۰٫۴۵ |
| شکایت باز مشتری را می‌برد | بازگشت خرید در ۱۸۰ روز | ۹۰٫۱٪ در برابر ۹۱٫۰٪ | — |
| تأیید نمونه، خرید همان گروه کالا را بالا می‌برد | تفاضل‌در‌تفاضل روی همان گروه کالا | ‎−۲۶٫۸ در برابر ‎−۳۰٫۱ واحد درصد | ۰٫۵۵ |

**هر چهار آزمون منفی.** تفاضل‌در‌تفاضل لازم بود چون خامِ «قبل و بعد» گمراه‌کننده
است: شکایت معمولاً **پس از** یک محمولهٔ بزرگ ثبت می‌شود، پس پنجرهٔ بعدی به‌طور
طبیعی کوچک‌تر است. با کسر روند سبد در همان پنجرهٔ تقویمی، این اثر حذف می‌شود —
و باز هم چیزی باقی نمی‌ماند.

پس هر عددی که به‌عنوان «افزایش فروش» بنویسیم ساختگی است. چیزی را می‌سنجیم که
اندازه‌گیری‌پذیر است: **آنچه با نرسیدگی از دست می‌رود**.

## ۳۸.۲ ارزش کمینه — سه لایه، و ما کف را گزارش می‌کنیم

```
۱. کف اندازه‌گیری‌شده   احتمال بازگشتی × میانگین ارزش بازگشتی، برای همان
                        نوع شکایت — از ۵۳۰ خط فاکتور پیوندخورده
۲. درآمد در معرض        درآمد سالانهٔ خود مشتری در همان گروه کالا (سقف)
۳. ارزش در خطر          درآمد در معرض × حاشیهٔ واقعی × احتمال ریزش

ارزش کمینه = کف اندازه‌گیری‌شده + ارزش در خطر
```

چهار انتخاب که این عدد را عمداً کوچک نگه می‌دارد: فقط **گروه کالای درگیر** (نه
کل حساب)، حاشیهٔ **واقعی** پس از هزینهٔ پول (نه فروش)، احتمال ریزش
**کالیبره‌شده** از مدل AUC ۰٫۸۳۱ (نه ۱۰۰٪)، و **هیچ** فرضی دربارهٔ خرید بیشتر
پس از رسیدگی.

**یک‌بار، نه سه‌بار.** کف اندازه‌گیری‌شده جمع‌پذیر است چون رویدادهای مجزاست، ولی
ارزش در خطر در سطح **مشتری** حساب می‌شود، روی اجتماع گروه‌های کالای درگیر — یک
مشتری یک‌بار می‌رود، نه یک‌بار به‌ازای هر منبع. نسخهٔ اول این ماژول همین را
اشتباه می‌کرد و ارزش کل را ۳۱٫۶ میلیون نشان می‌داد؛ پس از اصلاح **۱۹٫۰ میلیون**
شد. تعامل باز هم «مشکل محصول» نیست و گروه کالای درگیری ندارد، پس هیچ ارزش در
خطری نمی‌گیرد — نقشش نشان دادن کار معلق است، نه پول.

نتیجهٔ جانبی که خودش یک یافته است: **۳۱۹ مشتری** حاشیهٔ واقعی منفی دارند، پس ارزش
در خطرشان صفر می‌شود — نگه‌داشتنشان سود واقعی نمی‌سازد. برای همین‌ها عدد دوم
می‌دهیم: **۳۵٫۲ میلیون**، ارزش رسیدگی **اگر شرایط پرداخت اصلاح شود** و حاشیه به
سطح دست‌یافتنی سبد (۶٫۸۳٪) برسد. یعنی همان تز مرکزی پروژه، از سمت سوم.

## ۳۸.۳ جدول اکچوئری بازگشتی

شیت «اتصال_شکایت» ستون **مقدار برگشتی** دارد — پولی که واقعاً برگشته. با قیمت
واحد خود همان مشتری، برای هر نوع شکایت احتمال و هزینهٔ انتظاری ساخته می‌شود:

| نوع شکایت | شکایت | احتمال بازگشتی | هزینهٔ انتظاری |
|---|---|---|---|
| بدپیچی و ریزش نخ | ۹ | ۹٫۱٪ | ۷٬۴۷۳ |
| تلرانس نمره | ۱۰ | ۷٫۷٪ | ۵٬۵۶۳ |
| استحکام پایین | ۱۱ | ۳۰٫۸٪ | ۴٬۴۷۹ |
| بازشدن لا / جدایش | ۱۴ | ۲۱٫۴٪ | ۲٬۸۶۷ |

در کل، **۱۴٫۶٪** شکایت‌ها بازگشتی داشته‌اند و مجموع ارزش بازگشتی **۳۸۸ هزار**
است. این عدد کوچک است — و همین صادقانه‌ترین بخش ماجراست: هزینهٔ مستقیم شکایت در
این داده ناچیز است؛ آنچه بزرگ است، **درآمد در معرض** است.

## ۳۸.۴ تقاضای انباشته — اولویت تحقیق‌وتوسعه

برای درخواست توسعه، پرسش درست «این یک درخواست چقدر می‌ارزد» نیست؛ **«کدام تغییر
مشخصات را بیشترین درآمد پشتیبانی می‌کند»** است:

| نوع درخواست | مشتری | درآمد پشت آن | بی‌پاسخ | رد فنی |
|---|---|---|---|---|
| بهبود استحکام | ۷۵ | ۲٬۶۷۷ م | ۷۶ | ۱۹٫۳٪ |
| تغییر تعداد فیلامنت | ۷۵ | ۲٬۶۶۸ م | ۷۸ | ۲۰٫۳٪ |
| بسته‌بندی اختصاصی | ۷۷ | ۲٬۶۴۵ م | ۸۱ | ۲۴٫۵٪ |
| بهبود شید رنگ | ۵۱ | ۲٬۰۱۴ م | ۵۵ | ۱۶٫۳٪ |
| کاهش پرز | ۷۴ | ۱٬۸۹۴ م | ۷۵ | ۲۶٫۴٪ |
| تغییر دنیر | ۷۶ | ۱٬۷۷۰ م | ۷۰ | ۲۳٫۷٪ |

مشتریان در انواع مختلف هم‌پوشانی دارند، پس این ستون **جمع‌پذیر نیست** و همین قید
زیر جدول روی صفحه نوشته می‌شود.

## ۳۸.۵ شکاف داده در CRM — و اینکه چرا لایهٔ ارجاع لازم شد

از ۳٬۸۳۵ تعامل، **۳٬۱۰۴ مورد** یک «اقدام بعدی» ثبت کرده‌اند و **همهٔ ۶۲۴ مشتری**
دست‌کم یکی دارند. میانهٔ عمر این اقدام‌ها **۴۶۱ روز** است و **۹۰٪** بیش از ۹۰ روز
عمر دارند.

اما نکتهٔ اصلی این است: شیت تعاملات **هیچ فیلدی برای بستن اقدام ندارد** — تنها
وضعیت موجود «ثبت اولیه» و «اصلاح‌شده» است. پس **نمی‌توان گفت این اقدام‌ها انجام
شده‌اند یا نه**؛ فقط می‌دانیم ثبت شده‌اند. روی صفحه هم دقیقاً همین نوشته می‌شود، نه
«۳٬۱۰۴ کار انجام‌نشده».

همین شکاف بود که **لایهٔ ارجاع و گزارش کارشناس** (بخش ۳۷) را لازم کرد: آنجا
وضعیت و گزارش، هر دو ثبت می‌شوند و حلقه بسته می‌شود.

## ۳۸.۶ روند — دو نیم‌سال متوالی

هر دسته در هر منبع، دو پنجرهٔ ۱۸۲ روزهٔ **متوالی و بدون هم‌پوشانی** را مقایسه
می‌کند. آستانهٔ ±۱۵٪ جهت را تعیین می‌کند.

جهت، در هر منبع معنای متفاوتی دارد و روی صفحه اعلام می‌شود: **شکایتِ کمتر خبر
خوبی است**؛ **درخواست توسعهٔ بیشتر یعنی تقاضای بیشتر، نه مشکل بیشتر**؛ و
**تعامل کمتر یعنی تماس کمتر با مشتری، نه مشکل کمتر**. به همین دلیل رنگ فقط جایی
گذاشته می‌شود که جهتش روشن است.

نمونه‌های خواندنی: شکایت کل از ۱۲۱ به ۵۵ افتاده (رو به کاهش) ولی «مینگل کمتر از
حد» از ۲ به ۴ رفته؛ در CRM، «کیفیت محصول» تنها نوعی است که نیفتاده، در حالی که
«پیگیری سفارش» ‎−۳۱٪ و «قیمت و تخفیف» ‎−۳۴٪ افت کرده‌اند.

## ۳۸.۷ ابزار پانزدهم دستیار

`signal_center(source, customer_id, limit)` — بدون شناسه، سه منبع با روند و
دسته‌ها و تقاضای انباشته و آزمون‌های اثر؛ با شناسه، اکتیویتی همان مشتری در هر سه
منبع با اجزای ارزش کمینه. در پرامپت دستیار صریح نوشته شده که **هرگز نگوید رسیدگی
فروش را بالا می‌برد**.

---

# پیوست الف — جدول مرجع همهٔ اعداد

همهٔ اعداد از کش زندهٔ `cache/portfolio.json` و `cache/profiles.json` در تاریخ برش
**۹ تیر ۱۴۰۱** استخراج شده‌اند.

## الف.۱ دامنهٔ داده

| سنجه | مقدار |
|---|---|
| شیت | ۱۶ (در دو فایل، ۵ سامانهٔ منبع) |
| مشتری | ۶۴۴ |
| مشتری با سابقهٔ خرید | ۶۲۴ |
| خط فروش | ۵۲٬۹۳۵ |
| رکورد متن آزاد | ۴٬۵۶۵ (۳٬۶۵۲ CRM + ۵۵۰ توسعه + ۳۶۳ شکایت) |
| بازه | آذر ۱۳۹۸ تا خرداد ۱۴۰۱ (۳۱ ماه) |
| ماه ناقص مرزی | ۲ (حذف‌شده از همهٔ مقایسه‌ها) |

## الف.۲ تجاری

| سنجه | مقدار |
|---|---|
| فروش اسمی | ۴٬۳۹۱٬۴۸۵٬۲۰۹ |
| فروش حقیقی (تعدیل تورم) | ۱٬۷۶۰٬۹۲۳٬۰۹۸ |
| نسبت اسمی به حقیقی | ۲٫۴۹ برابر |
| سود ناخالص | ۴۴۳٬۲۱۳٬۱۱۷ |
| حاشیه سود ناخالص | ۱۰٫۰۹٪ |
| **هزینهٔ پول (۴٪ ماهانه)** | **۴۶۸٬۶۴۱٬۸۳۸ (۱۰٫۶۷٪)** |
| **سود واقعی** | **−۲۵٬۴۲۸٬۷۲۱ (−۰٫۵۸٪)** |
| ذخیرهٔ مطالبات مشکوک‌الوصول | ۱۶۷٬۵۵۵٬۶۳۰ (۳٫۸۲٪) |
| سود خالص | −۱۹۲٬۹۸۴٬۳۴۳ (−۴٫۳۹٪) |
| روزهای پول قفل‌شده (میانه) | ۵۴٫۳ روز |
| مبنای نقدی سبد (صدک ۱۰) | ۱۵٫۶ روز |
| مشتری زیان‌ده: ناخالص / واقعی | ۵۷ / ۳۱۹ |
| دامنهٔ حاشیه: ناخالص / واقعی | ۲۶٫۷ / ۴۸٫۶ واحد |
| میانهٔ حاشیه در سطح مشتری | ۸٫۹۱٪ |
| سهم مبنای هزینهٔ تحقق‌یافته | ۳۲٫۲٪ |
| تمرکز ۱۰ مشتری اول | در KPI نمای کل |

## الف.۳ اعتباری

| سنجه | مقدار |
|---|---|
| مطالبات معوق (سررسیدگذشته) | ۵۵۷٬۰۹۸٬۳۵۲ |
| سررسیدنشده (سرمایه در گردش) | ۱۶۶٬۳۶۰٬۵۰۸ |
| معوق به فروش | ۱۲٫۷٪ |
| **معوق به سود ناخالص** | **۱٫۲۶ برابر** |
| مشارکت خالص سبد | −۱۱۳٬۸۸۵٬۲۳۵ |
| مشتری با مشارکت خالص منفی | ۳۱۰ (۵۴٫۹٪ فروش) |

## الف.۴ ارزش و ماندگاری

| سنجه | مقدار |
|---|---|
| LTV کل | ۲۴۲٬۱۳۶٬۲۰۴ |
| LTV محقق‌شده | −۱۱۳٬۸۸۵٬۲۴۳ |
| LTV آیندهٔ تنزیل‌شده | ۳۵۶٬۰۲۱٬۴۴۷ |
| مشتری با LTV منفی | ۲۰۶ |
| افق LTV | ۲۴ ماه |
| نرخ تنزیل ماهانه | ۱٫۵٪ |
| ضریب تنزیل | ۲۰٫۰۳ |
| ماندگاری میانه | ۰٫۲۹۱ |
| مشتری راکد (>۱۸۰ روز) | ۴۰۱ در آمار سبد / ۳۸۱ با فروش |

## الف.۵ چهار خانه

| خانه | مشتری | سود ناخالص | LTV | حاشیه میانه | ماندگاری میانه |
|---|---|---|---|---|---|
| رشد بده | ۱۰۱ | ۳۰۲٫۰ م | ۲۷۳٫۷ م | ۱۲٫۲۸٪ | ۰٫۷۰ |
| **نجات فوری** | **۲۱۱** | ۶۹٫۸ م | ۵۵٫۹ م | ۱۳٫۴۶٪ | ۰٫۲۹ |
| اصلاح قیمت | ۱۲۴ | ۵۷٫۲ م | −۷۷٫۸ م | ۶٫۰۲٪ | ۰٫۷۴ |
| بازبینی رابطه | ۱۸۸ | ۱۴٫۲ م | −۹٫۶ م | ۵٫۱۰٪ | ۰٫۲۹ |
| **جمع** | **۶۲۴** | ۴۴۳٫۲ م | ۲۴۲٫۱ م | — | — |

## الف.۶ خروجی تحلیلی

| سنجه | مقدار |
|---|---|
| ویژگی مهندسی‌شده | ۸۴ |
| ستون در فریم سبد | ۵۷ |
| سیگنال تولیدشده | ۴٬۴۰۴ (۲۱ کد از ۱۸ قاعده) |
| پیش‌بینی تولیدشده | ۳٬۶۸۸ (۶ نوع) |
| ریسک شناسایی‌شده | ۲٬۴۸۷ (۱۴ کد فعال از ۱۵ قاعده) |
| فرصت شناسایی‌شده | ۱٬۶۷۸ (۱۶ کد از ۱۰ قاعده) |
| اقدام بعدی | ۱٬۸۷۵ (۱٬۲۸۰ ریسک + ۵۹۵ فرصت) |
| **رفرنس** | **۱۷٬۶۳۶** |
| میانگین رفرنس هر مشتری | ۲۶٫۱ |
| ادعای بدون رفرنس | **۰** |
| نقاط نمودار چهارخانه | ۶۲۱ از ۶۲۴ (۳ مورد با قدرمطلق حاشیهٔ بیش از ۶۰٪ حذف) |
| نقطهٔ بیرون از کادر | **۰** |
| آزمون | **۹۵ / ۹۵ سبز** (۴۸ پایه + ۱۹ دوره + ۱۸ هزینهٔ پول + ۱۰ کارتابل) |
| آزمون دستی سه حساب | **۳۰ / ۳۰ عدد منطبق** |
| طول دورهٔ پشتیبانی‌شده | ۷ (۱ هفته تا ۱ سال) |
| دورهٔ پیش‌فرض | **۲ ماه** — انتخاب‌شده از داده (بخش ۳۰.۶) |
| لنگر دوره | ۱۰ خرداد ۱۴۰۱ (پایان آخرین ماه کامل) |

## الف.۷ کارت مدل ماندگاری

| سنجه | مقدار |
|---|---|
| پنجرهٔ آموزش | ۱۳۹۹/۱۰/۱۰ → نتیجه ۱۱ ماه بعد |
| پنجرهٔ آزمون | ۱۴۰۰/۰۴/۰۹ → نتیجه تا پایان داده |
| AUC مدل جمعی (استفاده‌شده) | **۰٫۸۳۱** |
| AUC رکود تنها | ۰٫۸۵۲ ← **بهتر** |
| AUC رگرسیون لجستیک | ۰٫۸۶۸ ← بهترین، رد شد |
| خطای کالیبراسیون پیش از اصلاح | ۰٫۰۹۸ |
| خطای کالیبراسیون پس از اصلاح | **۰٫۰۴۰** |

## الف.۸ پخش اقدام‌ها بر واحد مسئول

| واحد | اقدام |
|---|---|
| واحد فروش | ۸۱۵ |
| کمیتهٔ اعتباری | ۳۸۴ |
| واحد وصول مطالبات | ۲۷۹ |
| کمیتهٔ قیمت‌گذاری | ۱۴۹ |
| کنترل کیفیت | ۱۰۷ |
| تحقیق‌وتوسعه | ۸۱ |
| برنامه‌ریزی تولید | ۶۰ |

---

# پیوست ب — واژه‌نامهٔ فنی

| اصطلاح | معنا در این پروژه |
|---|---|
| **as-of / در تاریخ** | بازسازی وضعیت داده در یک لحظهٔ گذشته، شامل راست‌سانسور فیلدهای خروجی |
| **راست‌سانسور** | پنهان کردن نتیجه‌ای که در تاریخ برش هنوز رخ نداده بود (مثلاً شکایتی که بعداً بسته شد، در آن تاریخ «باز» است) |
| **خارج‌از‌زمان** | آموزش روی پنجرهٔ قدیمی و سنجش روی پنجرهٔ تازه‌تر — تنها اعتبارسنجی معتبر برای دادهٔ زمانی |
| **مخدوش‌کننده** | متغیری که هم بر علت و هم بر اثر تأثیر دارد و رابطهٔ کاذب می‌سازد؛ در این داده: عمق رابطه |
| **کالیبراسیون** | نگاشت امتیاز خام به احتمال واقعی، به‌گونه‌ای که «۷۰٪» یعنی ۷ مورد از ۱۰ |
| **AUC** | احتمال اینکه مدل به یک مشتری بازگشته امتیاز بالاتر از یک مشتری رفته بدهد؛ ۰٫۵ تصادفی |
| **نرخ پایه** | نرخ رخداد یک رویداد در گروه مشابه، از شمارش واقعی |
| **RFM** | تازگی (Recency)، تکرار (Frequency)، مبلغ (Monetary) — سه‌بعدی، هر بعد ۱ تا ۵ |
| **LTV** | ارزش طول عمر: سود محقق‌شده منهای معوق، به‌علاوهٔ سود آیندهٔ تنزیل‌شده |
| **ارزش نجات** | `سود ماهانه × (۱ − ماندگاری) × ۱۲` — سودی که با از دست دادن مشتری در یک سال از بین می‌رود |
| **مشارکت خالص** | سود ناخالص منهای **معوق** (نه منهای کل مانده باز) |
| **معوق** | سررسیدگذشته و وصول‌نشده — ریسک اعتباری |
| **سررسیدنشده** | فاکتور وصول‌نشده که هنوز سررسید نشده — سرمایه در گردش، نه ریسک |
| **فروش حقیقی** | فروش اسمی تقسیم بر شاخص قیمت — قابل مقایسه بین سال‌ها |
| **نسبت سکوت** | رکود تقسیم بر فاصلهٔ معمول سفارش همان مشتری |
| **ZWNJ** | نیم‌فاصلهٔ فارسی (U+200C) — دو رشتهٔ ظاهراً یکسان را برای کامپیوتر متفاوت می‌کند |
| **رفرنس** | شیت + شناسهٔ رکورد + تاریخ + فیلد + مقدار (و در صورت لزوم، توضیح مبنا) |
